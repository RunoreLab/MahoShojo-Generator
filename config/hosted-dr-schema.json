{
  "schemaVersion": 1,
  "status": "external-baseline-required",
  "authority": "d1",
  "sources": [
    {
      "path": "apps/web/lib/db/schema/business.ts",
      "kind": "drizzle-source"
    },
    {
      "path": "apps/web/lib/database/schema.sql",
      "kind": "sql-snapshot"
    }
  ],
  "requiredTables": [
    {
      "name": "battle_report_generations",
      "columns": ["id", "status", "generation_mode", "endpoint", "extra_json"]
    },
    {
      "name": "battle_report_generation_combatants",
      "columns": ["id", "generation_id", "sort_index"]
    },
    {
      "name": "large_objects",
      "columns": ["id", "kind", "owner_ref_id", "r2_key"]
    },
    {
      "name": "arena_ratings",
      "columns": ["entity_type", "entity_id", "queue", "rating", "games"]
    },
    {
      "name": "arena_rating_events",
      "columns": ["id", "generation_id", "queue", "status"]
    }
  ],
  "migrationPolicy": {
    "status": "unchanged-for-g25e2",
    "directory": "drizzle",
    "note": "G25E-2 不改变既有 D1 schema 或 migration；物理表存在性必须在隔离 D1 上另行核验。"
  },
  "physicalProbe": {
    "status": "deferred",
    "requires": "isolated-preview-d1",
    "evidencePath": "config/hosted-dr-schema-evidence.json",
    "activation": "require-external-evidence"
  }
}
