# 治理后台剩余工作 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking. User explicitly chose Inline Execution + Subagent Review, directly on `feat/admin`, without creating a worktree.

**Goal:** 在 `feat/admin` 分支补齐治理后台剩余可写动作与消息后台交互改造，同时确保管理员能力不暴露到生产分支 `feature/v0.2.0_Battle_Growth_MahoShojo`。

**Architecture:** 本轮不新增治理管理员动作表，不补页面级测试。通过共享中文标签/消息模板目录、`report-cases` admin action 层、`crowd-review` admin action 层实现剩余功能；数据卡处罚复用现有内容管理后台的 `batchUpdateDataCards` 与 `sendDataCardModerationMessages`。

**Tech Stack:** Next.js Pages Router, Edge Runtime API handlers, Cloudflare D1 + Drizzle, Bun test, Tailwind 4.

---

## Branch And Deployment Guardrails

- 当前工作分支：`feat/admin`
- 不创建 worktree，直接在当前分支执行
- 管理员相关能力仅限 `feat/admin` 后台部署
- 禁止把以下路径 cherry-pick 到 `feature/v0.2.0_Battle_Growth_MahoShojo`：
  - `pages/admin/**`
  - `pages/api/admin/**`
  - `components/admin/**`
  - `lib/admin/**`
- 如有需要 pick 到生产分支的内容，只能是明确的共享非后台修复，例如：
  - `lib/messages/**` 中不依赖后台的通用文案/helper
  - `lib/db/repositories/**` 中修复生产用户侧已有 bug 的共享 helper
  - 与治理后台无关的通用测试修复
- 每个任务完成后都要记录是否存在生产分支 pick 候选；默认结论应为“无”。

## Planned File Structure

- Create: `lib/admin/governance-labels.ts`
  - 统一治理后台中文文案与枚举 label。
- Create: `lib/admin/message-catalog.ts`
  - 统一后台消息模板目录，供 `admin/messages` 表单使用。
- Create: `lib/messages/display.ts`
  - 将消息优先级等非组件展示 helper 从 `components/messages/messageUi.ts` 抽到共享层。
- Modify: `components/messages/messageUi.ts`
  - 继续向用户侧组件暴露现有函数，避免改动用户侧 import。
- Modify: `components/admin/messages/AdminMessagesPage.tsx`
  - 使用模板目录与中文标签，减少手填 `templateKey`。
- Create: `lib/admin/report-cases.ts`
  - 承担举报案件管理员动作编排。
- Modify: `lib/db/repositories/data-card-reports.ts`
  - 补共享 `updateReportCaseDecision(...)`，供申诉、众查、后台动作复用或逐步迁移。
- Create: `pages/api/admin/report-cases/[caseId]/decision.ts`
  - 举报案件正式处理 API。
- Modify: `pages/admin/report-cases.tsx`
  - 增加正式处理表单，复用中文 label。
- Create: `lib/admin/crowd-review.ts`
  - 承担众查轮次接管、撤销、改判的管理员动作编排。
- Modify: `lib/db/repositories/crowd-review.ts`
  - 增加撤销 active 派单等 repo helper。
- Create: `pages/api/admin/crowd-review/cases/[roundId]/take-over.ts`
  - 管理员接管众查轮次 API。
- Create: `pages/api/admin/crowd-review/cases/[roundId]/cancel.ts`
  - 管理员撤销众查轮次 API。
- Create: `pages/api/admin/crowd-review/cases/[roundId]/override.ts`
  - 管理员改判众查轮次 API。
- Modify: `pages/admin/crowd-review/cases.tsx`
  - 将待接入按钮改为真实表单动作。
- Test: `tests/admin-governance-labels.test.ts`
  - 覆盖中文 label 与消息模板目录。
- Test: `tests/report-case-admin-actions.test.ts`
  - 覆盖举报案件管理员动作 service/helper。
- Test: `tests/crowd-review-admin-actions.test.ts`
  - 覆盖众查管理员动作 service/helper。
- Modify: `tests/admin-report-cases-api.test.ts`
  - 覆盖正式处理 API。
- Modify: `tests/admin-crowd-review-api.test.ts`
  - 覆盖接管、撤销、改判 API。

## Task 1: Shared Labels And Message Catalog

**Files:**
- Create: `lib/admin/governance-labels.ts`
- Create: `lib/admin/message-catalog.ts`
- Create: `lib/messages/display.ts`
- Modify: `components/messages/messageUi.ts`
- Test: `tests/admin-governance-labels.test.ts`

- [ ] **Step 1: Write label/catalog tests**

Add tests for:

- `getAdminMessagePriorityLabel('normal')` returns `普通优先级`
- `getReportCaseStatusLabel('under_review')` returns `人工复核中`
- `getCrowdReviewRoundStatusLabel('pending_dispatch')` returns `待派单`
- message catalog contains `site.generic.notice`, `site.issue.update`, `user.generic.notice`, `user.moderation.data_card_rejected`, `user.moderation.data_card_banned`, `user.moderation.data_card_reported`, `user.moderation.report_case_resolved`

Run:

```bash
bun test tests/admin-governance-labels.test.ts
```

Expected: fail because files/functions do not exist yet.

- [ ] **Step 2: Implement label helpers**

Implement `lib/admin/governance-labels.ts` with pure functions:

```ts
export function getReportCaseStatusLabel(value: string | null | undefined): string;
export function getReportResolutionCodeLabel(value: string | null | undefined): string;
export function getCrowdReviewRoundStatusLabel(value: string | null | undefined): string;
export function getCrowdReviewResultCodeLabel(value: string | null | undefined): string;
export function getCrowdReviewInspectorStatusLabel(value: string | null | undefined): string;
```

Unknown values should return `未知` or `未知（${value}）` without throwing.

- [ ] **Step 3: Move message display helpers**

Create `lib/messages/display.ts` with:

```ts
export function getMessageScopeLabel(scope: MessageScope): string;
export function getMessagePriorityClassName(priority: MessagePriority): string;
export function getMessagePriorityLabel(priority: MessagePriority): string;
export function formatMessageTime(iso: string): string;
```

Then update `components/messages/messageUi.ts` to re-export or delegate to avoid breaking user-side imports.

- [ ] **Step 4: Implement admin message catalog**

Create `lib/admin/message-catalog.ts`:

```ts
export type AdminMessageTemplateCatalogItem = {
  scope: 'site' | 'direct';
  messageType: string;
  templateKey: string;
  label: string;
  description: string;
  defaultPriority: 'low' | 'normal' | 'high';
  defaultActionUrl?: string;
  payloadHint: string;
};

export const ADMIN_MESSAGE_TEMPLATE_CATALOG: AdminMessageTemplateCatalogItem[];
export function getAdminMessageTemplateCatalogItem(templateKey: string): AdminMessageTemplateCatalogItem | null;
```

- [ ] **Step 5: Verify task**

Run:

```bash
bun test tests/admin-governance-labels.test.ts tests/messages-page.test.tsx
```

Expected: pass.

- [ ] **Step 6: Cherry-pick assessment**

Record whether any shared non-admin changes should be picked to `feature/v0.2.0_Battle_Growth_MahoShojo`. Expected: likely no, unless a shared message display bug is fixed.

## Task 2: Admin Messages UX

**Files:**
- Modify: `components/admin/messages/AdminMessagesPage.tsx`
- Reference: `lib/admin/message-catalog.ts`
- Reference: `lib/messages/display.ts`
- Test: `tests/admin-messages-api.test.ts`
- Test: `tests/admin-governance-labels.test.ts`

- [ ] **Step 1: Replace free-form template inputs**

In `AdminMessagesPage`, replace primary `templateKey` free-text inputs with `<select>` controls populated from `ADMIN_MESSAGE_TEMPLATE_CATALOG`.

Behavior:

- Site form only shows `scope === 'site'`
- Direct form only shows `scope === 'direct'`
- Selecting a template updates `messageType`, `templateKey`, `priority`, `actionUrl`, and `payloadText` hint/default if appropriate
- Keep raw `templateKey` visible as read-only help text for debugging

- [ ] **Step 2: Chinese priority labels**

Use `getMessagePriorityLabel` for priority select labels and table cells.

Expected visible labels:

- `低优先级`
- `普通优先级`
- `高优先级`

- [ ] **Step 3: Improve payload guidance**

Replace generic JSON placeholder with catalog `payloadHint`.

Keep JSON text area because payload remains schema-specific and should stay explicit for admin operators.

- [ ] **Step 4: Verify API compatibility**

Run:

```bash
bun test tests/admin-messages-api.test.ts tests/admin-governance-labels.test.ts
```

Expected: pass; no API contract changes.

- [ ] **Step 5: Cherry-pick assessment**

Expected: no production pick. This is admin UI only.

## Task 3: Report Case Admin Action Layer

**Files:**
- Create: `lib/admin/report-cases.ts`
- Modify: `lib/db/repositories/data-card-reports.ts`
- Test: `tests/report-case-admin-actions.test.ts`

- [ ] **Step 1: Write service/helper tests**

Cover:

- `updateReportCaseDecision` writes `resolved + confirmed_violation + closedAt`
- `updateReportCaseDecision` writes `dismissed + no_violation + closedAt`
- `updateReportCaseDecision` writes `under_review + null + closedAt null`
- admin action rejects invalid status/resolution combinations
- data card punishment payload is forwarded to existing content-management helpers

Run:

```bash
bun test tests/report-case-admin-actions.test.ts
```

Expected: fail because action layer does not exist yet.

- [ ] **Step 2: Add shared report case decision helper**

Add to `lib/db/repositories/data-card-reports.ts`:

```ts
export async function updateReportCaseDecision(
  db: AppDrizzleDb,
  input: {
    reportCaseId: string;
    status: ReportCaseStatus;
    resolutionCode: ReportResolutionCode | null;
    closedAt: string | null;
    now: string;
  },
): Promise<boolean>;
```

Implementation should mirror existing update logic from `report-appeals` / `crowd-review`, but not refactor those callers in the same step unless needed for type safety.

- [ ] **Step 3: Implement admin action service**

Create `lib/admin/report-cases.ts` with:

```ts
export type AdminReportCaseDecisionInput = {
  db: AppDrizzleDb | null;
  caseId: string;
  adminUserId: number;
  nextStatus: 'resolved' | 'dismissed' | 'under_review';
  resolutionCode: string | null;
  resolutionNote?: string | null;
  notifyCreator?: boolean;
  creatorMessageReason?: string | null;
  cardModerationAction?: {
    action: 'reject' | 'set_public_status';
    value?: 0 | -1;
    messageOptions?: {
      send?: boolean;
      defaultReason?: string | null;
    };
  } | null;
};

export async function decideAdminReportCase(input: AdminReportCaseDecisionInput): Promise<{
  reportCaseId: string;
  status: string;
  resolutionCode: string | null;
  closedAt: string | null;
  notifiedCreator: boolean;
  dataCardModerationApplied: boolean;
}>;
```

Rules:

- `resolved` allows `confirmed_violation`, `content_removed`, `self_remediated`
- `dismissed` allows `no_violation`, `malicious_report`
- `under_review` requires `resolutionCode === null`
- data card punishment only allowed for `resolved` violation-style outcomes
- data card punishment reuses `batchUpdateDataCards`, `getDataCardNotificationTargets`, `sendDataCardModerationMessages`
- no new database table is created

- [ ] **Step 4: Verify task**

Run:

```bash
bun test tests/report-case-admin-actions.test.ts tests/admin-report-cases-api.test.ts
```

Expected: service tests pass after implementation; existing API tests remain pass.

- [ ] **Step 5: Cherry-pick assessment**

Expected: no production pick. If `updateReportCaseDecision` fixes a duplicated shared bug, document it as a possible non-admin pick candidate only after inspection.

## Task 4: Report Case Decision API And UI

**Files:**
- Create: `pages/api/admin/report-cases/[caseId]/decision.ts`
- Modify: `pages/admin/report-cases.tsx`
- Modify: `tests/admin-report-cases-api.test.ts`
- Reference: `lib/admin/report-cases.ts`
- Reference: `lib/admin/governance-labels.ts`

- [ ] **Step 1: Add failing API tests**

Extend `tests/admin-report-cases-api.test.ts`:

- requires login
- rejects invalid `resolutionCode`
- rejects data card punishment on `dismissed + no_violation`
- forwards valid payload to `decideAdminReportCase`
- maps not found/conflict/validation errors to correct status codes

Run:

```bash
bun test tests/admin-report-cases-api.test.ts
```

Expected: fail because `decision.ts` does not exist yet.

- [ ] **Step 2: Implement API handler**

Create `pages/api/admin/report-cases/[caseId]/decision.ts`.

Use the same handler style as:

- `pages/api/admin/report-cases/[caseId]/notify-creator.ts`
- `pages/api/admin/report-appeals/[appealId]/review.ts`

Runtime must remain Edge-compatible.

- [ ] **Step 3: Update report-cases UI labels**

In `pages/admin/report-cases.tsx`:

- Replace raw status options labels with `getReportCaseStatusLabel`
- Replace raw `resolutionCode` display with `getReportResolutionCodeLabel`
- Keep raw code as small secondary text if useful

- [ ] **Step 4: Add formal decision form**

In selected case detail panel, add a “正式处理” section:

- decision select
- resolution note textarea
- optional creator notification checkbox
- optional data card punishment checkbox/select
- submit button calls `/api/admin/report-cases/[caseId]/decision`
- on success reload selected detail and list

Do not implement page test in this round.

- [ ] **Step 5: Verify task**

Run:

```bash
bun test tests/admin-report-cases-api.test.ts tests/report-case-admin-actions.test.ts
```

Expected: pass.

- [ ] **Step 6: Cherry-pick assessment**

Expected: no production pick. All changes are admin-only except possible shared helper.

## Task 5: Crowd Review Admin Action Layer

**Files:**
- Create: `lib/admin/crowd-review.ts`
- Modify: `lib/db/repositories/crowd-review.ts`
- Test: `tests/crowd-review-admin-actions.test.ts`

- [ ] **Step 1: Write service/helper tests**

Cover:

- take-over writes round `escalated + escalated` and case `under_review + null`
- cancel writes round `cancelled + null`, revokes assigned assignments, and case `under_review + null`
- override violation writes round `concluded + admin_override` and case `resolved + confirmed_violation`
- override no-violation writes case `dismissed + no_violation`
- override reopen writes case `under_review + null`
- result summary JSON keeps admin metadata

Run:

```bash
bun test tests/crowd-review-admin-actions.test.ts
```

Expected: fail because action layer does not exist yet.

- [ ] **Step 2: Add repo helper**

Add to `lib/db/repositories/crowd-review.ts`:

```ts
export async function revokeAssignedAssignmentsByRound(
  db: AppDrizzleDb,
  input: { roundId: string; now: string },
): Promise<number>;
```

It should only update `status = 'assigned'` rows to `revoked`, set `completedAt`, and preserve existing completed votes.

- [ ] **Step 3: Implement admin crowd review service**

Create `lib/admin/crowd-review.ts` with:

```ts
export async function takeOverAdminCrowdReviewRound(input: {
  db: AppDrizzleDb | null;
  roundId: string;
  adminUserId: number;
  reasonDetail?: string | null;
}): Promise<AdminCrowdReviewActionResult>;

export async function cancelAdminCrowdReviewRound(input: {
  db: AppDrizzleDb | null;
  roundId: string;
  adminUserId: number;
  reasonDetail?: string | null;
}): Promise<AdminCrowdReviewActionResult>;

export async function overrideAdminCrowdReviewRound(input: {
  db: AppDrizzleDb | null;
  roundId: string;
  adminUserId: number;
  caseDecision: 'violation' | 'no_violation' | 'reopen_under_review';
  reasonDetail?: string | null;
}): Promise<AdminCrowdReviewActionResult>;
```

Result type should include:

- `roundId`
- `reportCaseId`
- `roundStatus`
- `roundResultCode`
- `reportCaseStatus`
- `reportCaseResolutionCode`
- `revokedAssignmentsCount`

- [ ] **Step 4: Use existing case decision helper**

Use `updateReportCaseDecision(...)` from `lib/db/repositories/data-card-reports.ts` rather than duplicating case update SQL.

- [ ] **Step 5: Verify task**

Run:

```bash
bun test tests/crowd-review-admin-actions.test.ts tests/crowd-review-service.test.ts
```

Expected: pass.

- [ ] **Step 6: Cherry-pick assessment**

Expected: no production pick unless repo helper fixes a user-side bug. Admin service itself must not be picked.

## Task 6: Crowd Review Action APIs And UI

**Files:**
- Create: `pages/api/admin/crowd-review/cases/[roundId]/take-over.ts`
- Create: `pages/api/admin/crowd-review/cases/[roundId]/cancel.ts`
- Create: `pages/api/admin/crowd-review/cases/[roundId]/override.ts`
- Modify: `pages/admin/crowd-review/cases.tsx`
- Modify: `tests/admin-crowd-review-api.test.ts`
- Reference: `lib/admin/crowd-review.ts`
- Reference: `lib/admin/governance-labels.ts`

- [ ] **Step 1: Add failing API tests**

Extend `tests/admin-crowd-review-api.test.ts`:

- take-over requires login and forwards reason
- cancel maps not-found/conflict errors
- override validates `caseDecision`
- override forwards admin user id and payload

Run:

```bash
bun test tests/admin-crowd-review-api.test.ts
```

Expected: fail because new API files do not exist yet.

- [ ] **Step 2: Implement API handlers**

Follow style from:

- `pages/api/admin/crowd-review/inspectors/[userId]/status.ts`
- `pages/api/admin/report-appeals/[appealId]/review.ts`

Each handler:

- checks method `POST`
- requires auth
- extracts `roundId`
- reads JSON body
- maps admin governance errors to 400/404/409/503
- returns no-store JSON

- [ ] **Step 3: Update crowd-review cases UI labels**

In `pages/admin/crowd-review/cases.tsx`:

- Replace raw status options with Chinese labels
- Replace result code display with `getCrowdReviewResultCodeLabel`
- Keep raw code as secondary text only

- [ ] **Step 4: Replace disabled placeholders**

Replace disabled placeholder buttons with a real action section:

- “接管轮次” button with reason textarea
- “撤销轮次” button with confirmation text
- “管理员改判” select with `violation / no_violation / reopen_under_review`
- After success, refresh selected detail and list

Do not implement page test in this round.

- [ ] **Step 5: Verify task**

Run:

```bash
bun test tests/admin-crowd-review-api.test.ts tests/crowd-review-admin-actions.test.ts
```

Expected: pass.

- [ ] **Step 6: Cherry-pick assessment**

Expected: no production pick. New API/UI files are admin-only.

## Task 7: Final Verification And Production Branch Pick Audit

**Files:**
- Reference: all changed files from previous tasks
- Reference: `docs/2026-04-11_101808_治理后台剩余工作调研与实施设计.md`

- [ ] **Step 1: Run targeted governance tests**

Run:

```bash
bun test \
  tests/admin-governance-labels.test.ts \
  tests/admin-messages-api.test.ts \
  tests/admin-report-cases-api.test.ts \
  tests/admin-crowd-review-api.test.ts \
  tests/report-case-admin-actions.test.ts \
  tests/crowd-review-admin-actions.test.ts
```

Expected: pass.

- [ ] **Step 2: Run broader affected tests**

Run:

```bash
bun test \
  tests/messages-page.test.tsx \
  tests/messages-templates.test.ts \
  tests/report-appeals-service.test.ts \
  tests/crowd-review-service.test.ts \
  tests/admin-content-management-messages-api.test.ts
```

Expected: pass.

- [ ] **Step 3: Run full verification**

Run:

```bash
bun test
bun run lint
bun run build
```

Expected: all pass.

- [ ] **Step 4: Audit production branch pick candidates**

Inspect changed file list:

```bash
git status --short
git diff --name-only
```

Rules:

- If changes are under `pages/admin/**`, `pages/api/admin/**`, `components/admin/**`, or `lib/admin/**`, do not pick to production.
- If changes are shared helpers under `lib/messages/**` or `lib/db/repositories/**`, inspect whether they are required for production user-side correctness.
- If no shared production fix exists, record “无需 pick 到 feature/v0.2.0_Battle_Growth_MahoShojo” in the final summary.
- If a shared production fix exists, prepare a separate cherry-pick plan that excludes admin-only files before switching branches.

- [ ] **Step 5: Inline execution review checkpoint**

Request subagent review after implementation but before final completion.

Review context must include:

- this plan path
- changed file list
- test output summary
- explicit guardrail: admin-only changes must remain off production branch

Do not merge, reset, or cherry-pick without an explicit reviewed file list.
