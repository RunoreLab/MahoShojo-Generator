# MahoShojo-Generator 平台重整目标架构

状态：`active`
日期：2026-08-22
最近修订：2026-08-23（补充 Hosted 主执行面、Cloudflare DR、数据权威与故障切换边界）
适用范围：Web、Admin、香港 Hono、D1 Gateway、Arena Room Worker、Desktop、Mobile、本地库与多人基础设施
规范性：本文件解释长期结构；可测试强制要求以实施规格为准。

## 1. 架构目标

本次重整同时重划：

1. **部署边界**：哪些代码独立构建、部署和回滚。
2. **执行边界**：哪些操作在设备、Cloudflare、香港服务器、Room Durable Object 或数据库网关执行。
3. **信任边界**：哪些结果可以成为官方签名、排位、多人与治理事实。
4. **数据所有权**：哪些数据属于设备、账号或服务器权威。
5. **资源责任**：哪一方承担 AI、CPU、连接、数据库和存储成本。

总原则：

> 本地能够完成的默认在本地完成；只有需要项目资源、全局共享状态或服务器信任根的能力才联网。

## 2. 上下文视图

```text
                         ┌─────────────────────────┐
                         │ Cloudflare Access / IdP │
                         └────────────┬────────────┘
                                      │
                              admin.<domain>
                                      │
                         ┌────────────▼────────────┐
                         │ apps/admin              │
                         │ RBAC / audit / step-up  │
                         └────────────┬────────────┘
                                      │
                                      │ internal/admin API
                                      │
┌──────────────────┐       ┌─────────▼────────────────────┐
│ apps/web         │──────▶│ apps/api / Hono HK          │
│ public web       │       │ hosted execution            │
└───────┬──────────┘       │ native signing              │
        │                  │ authoritative settlement     │
        │ room ticket/RPC  └─────────┬────────────────────┘
        ▼                            │
┌──────────────────┐                 │
│ apps/arena-room  │                 │
│ Worker + DO      │                 │
│ coordination     │                 │
└──────────────────┘                 │
                                      ▼
┌──────────────────┐       ┌──────────────────────────────┐
│ apps/desktop     │       │ apps/d1-gateway             │
│ direct executor  │       │ internal parameterized DB   │
│ local library    │       └────────────┬─────────────────┘
└────────┬─────────┘                    │
         │                              ▼
         ├────────▶ user provider      D1
         └────────▶ local model

┌──────────────────┐
│ apps/mobile      │
│ direct/local     │
└──────────────────┘
```

Direct 与 local 路径不经过项目 API。Hosted、签名、严格排位、多人与线上数据才进入服务器路径。

Arena Room 的跨部署调用分两类：

- `apps/web -> apps/arena-room`：Cloudflare Worker 内优先使用 Service Binding / typed RPC；
- `apps/api / Hono -> apps/arena-room`：Hono 不具备 Worker Service Binding，使用 generation-scoped authenticated `GenerationBridge`。首选长连接 publisher（例如 WSS）批量发布流事件，避免每 50–100ms 建立一次跨公网 HTTP publish。

### 2.1 Hosted 运行时容灾视图

目标态把“业务入口”和“物理执行节点”分离：

```text
Web / Installed Client
        │
        ▼
stable logical Hosted API endpoint
        │
        ├──────── primary ───────▶ apps/api / Hono HK
        │                              │
        │                              ▼
        │                         D1 Gateway
        │                              │
        └──────── DR ────────────▶ apps/web / Next/OpenNext adapter
                                       │
                                  native D1 binding
        │                              │
        └──────────────────────┬───────┘
                               ▼
                              D1
```

- Hono HK 是默认 Hosted 主执行面；Cloudflare Next/OpenNext 只对显式 DR manifest 中的能力提供长期灾备 adapter。
- 客户端不应把 `homura` 等物理 Hono origin 编码为目标架构中的唯一入口；切流由 Cloudflare 或等价独立控制面完成。
- Hono/Next 共享业务 core，只保留运行时 adapter 差异；legacy app-to-app 动态 import 必须移除。
- DR Next 在可行时直接使用 D1 binding/Sessions，避免与 Hono 同时依赖 D1 Gateway；两种 DatabaseProvider 必须保持同一一致性 contract。
- safe/read 请求可按健康状态自动选择 DR；generation/stream/settlement 等非幂等操作在开始前选定 runtime，执行可能已经开始后不得盲目跨 runtime 重放。

具体 Cloudflare Load Balancing、Worker routing 或其他切流产品由实施阶段依据账号能力、成本和长流 CPU 数据选择；不得为了容灾把全部长流无条件重新代理进高 CPU Worker。

## 3. Monorepo 结构

```text
.
├─ apps/
│  ├─ web/
│  ├─ admin/
│  ├─ api/
│  ├─ d1-gateway/
│  ├─ arena-room/
│  ├─ desktop/
│  └─ mobile/
├─ packages/
│  ├─ domain/
│  ├─ contracts/
│  ├─ ai-core/
│  ├─ ai-direct/
│  ├─ local-library/
│  ├─ cloud-client/
│  ├─ multiplayer-core/
│  ├─ ui-web/
│  ├─ config/
│  └─ test-utils/
├─ docs/
├─ scripts/
├─ pnpm-workspace.yaml
└─ package.json
```

### 3.1 依赖方向

```text
domain                contracts
   ▲                      ▲
   │                      │
ai-core  local-library  cloud-client  multiplayer-core
   ▲          ▲              ▲              ▲
   └──────────┴──────────────┴──────────────┘
                         apps
```

必须满足：

- `packages/*` 不导入 `apps/*`；
- `domain` 不依赖框架、DOM、Node 数据库、Tauri 或 Cloudflare；
- `contracts` 不读取环境变量；
- 客户端包不导入服务器密钥、D1 管理能力或管理 API 实现；
- Web、Desktop、Mobile 不直接互相导入源码；
- 不通过 `@/*` 跨 package 穿透内部文件，使用 package exports。

### 3.2 运行时端口

上层业务依赖端口，而不是平台判断：

```ts
interface AiExecutionPort {
  execute(
    request: AiExecutionRequest,
    signal: AbortSignal,
  ): Promise<AiExecutionResult>;
  stream(
    request: AiExecutionRequest,
    signal: AbortSignal,
  ): AsyncIterable<AiStreamEvent>;
}

interface CardRepository {
  get(id: string): Promise<LocalCardRecord | null>;
  list(query: LocalCardQuery): Promise<LocalCardPage>;
  put(record: LocalCardRecord): Promise<void>;
  delete(id: string): Promise<void>;
}

interface SecureVault {
  setSecret(ref: string, value: string): Promise<void>;
  getSecret(ref: string): Promise<string | null>;
  deleteSecret(ref: string): Promise<void>;
}
```

实现示例：

- `BrowserIndexedDbCardRepository`
- `SqliteCardRepository`
- `TauriDirectAiExecutor`
- `ElectronDirectAiExecutor`
- `HostedAiExecutor`
- `TauriStrongholdVault`
- `PlatformKeychainVault`

组件不得散落 `if (isDesktop)` / `if (offline)` 来决定业务语义。

### 3.3 Shared core 与 runtime adapter

跨 Hono/Next 的 Hosted 业务复用同样遵循端口化原则：service/domain/contracts 不导入 Hono Context、NextRequest、Cloudflare binding 或具体 Redis client；`apps/api` 与 `apps/web` 各自提供 adapter。DR adapter 的存在不得成为 app-to-app 源码导入或复制业务 handler 的理由。

## 4. 执行模式

| 模式 | 执行位置 | 项目资源 | 官方签名 | Strict | 典型场景 |
| --- | --- | --- | --- | --- | --- |
| `direct-local` | 用户设备/localhost | 否 | 否 | 否 | Ollama、LM Studio |
| `direct-remote` | 用户设备到自定义 Endpoint | 否 | 否 | 否 | BYOK、自建代理 |
| `hosted` | 项目服务器 | 是 | 可 | 按资格 | Web 官方通道 |
| `authoritative` | 项目服务器/Room 服务 | 是 | 是/验证 | 是 | 签名、排位、多人结算 |

`hosted` 和 `authoritative` 可以由同一服务器承载，但权限、审计和结果语义必须区分。

## 5. Direct Provider 架构

### 5.1 Catalog 与 Profile

Catalog 只提供预设：

```text
provider preset
  -> display name
  -> adapter
  -> default endpoint
  -> documentation
  -> model suggestions
```

用户保存的是本地 Profile：

```ts
type DirectProviderProfileV1 = {
  version: 1;
  id: string;
  name: string;
  adapter: 'openai-compatible' | 'anthropic' | 'google';
  baseUrl: string;
  modelId: string;
  apiKeyRef?: string;
  secretHeaderRefs?: Record<string, string>;
  publicHeaders?: Record<string, string>;
  generationDefaults?: {
    maxOutputTokens?: number;
    temperature?: number;
    thinking?: Record<string, unknown>;
  };
  transport?: {
    allowPublicHttp?: boolean;
    maxRedirects?: number;
  };
  createdAt: string;
  updatedAt: string;
};
```

MVP 必须支持 `openai-compatible + arbitrary baseUrl`。其他 adapter 渐进实现。

### 5.2 Native Executor

Renderer 只能调用窄命令：

```text
listProviderProfiles()
saveProviderProfile()
testProviderProfile()
streamGeneration(profileId, normalizedRequest)
cancelGeneration(requestId)
```

不得暴露：

```text
fetchAny(url, headers, body)
readAnyFile(path)
runShell(command)
getAllSecrets()
```

### 5.3 Endpoint 安全

- Profile 只能由用户设置页显式创建或确认导入。
- 卡片、Prompt、场景、网页链接和多人 payload 不得静默创建/修改 Profile。
- 远端默认要求 HTTPS。
- `localhost`、loopback 与私有网段允许 HTTP，以支持本地模型和局域网服务。
- 公网 HTTP 需要醒目警告和逐 Profile 确认。
- 拒绝 `file:`、`data:`、`javascript:` 等非 HTTP(S) scheme。
- 不提供“忽略所有 TLS 错误”的全局开关。
- 重定向默认最多 3 次；跨 Origin 不转发 Authorization、API Key、Cookie 或 secret headers。
- 禁止覆盖 `Host`、`Content-Length`、`Connection` 等传输层 header。
- Direct 请求不携带项目 Cookie、Legacy Bearer、Better Auth session 或 activity token。
- 日志、错误和导出必须脱敏秘密。

## 6. 本地库架构

### 6.1 记录模型

```ts
type LocalCardRecord = {
  id: string;
  schemaVersion: number;
  cardType: 'character' | 'scenario' | 'history' | 'questionnaire';
  title: string;
  data: unknown;
  contentDigest: string;
  provenance: {
    kind: 'official-signed' | 'unsigned' | 'signature-invalid';
    signature?: string;
    signatureVersion?: number;
    signatureKeyId?: string;
    execution?:
      | 'downloaded'
      | 'direct-local'
      | 'direct-remote'
      | 'imported'
      | 'edited';
  };
  cloudRef?: {
    cardId: string;
    cloudRevision?: string;
    copiedAt: string;
  };
  createdAt: string;
  updatedAt: string;
  deletedAt?: string;
};
```

`cloudRef` 只描述来源或显式副本关系，不代表自动同步。

### 6.2 Web

- 使用独立 IndexedDB；
- schema upgrade 只向前迁移；
- 调用 `navigator.storage.persist()` 与 `navigator.storage.estimate()`；
- 提供导出、导入和恢复；
- 明确提示清除站点数据会删除本地库。

### 6.3 Installed APP

- SQLite 保存可查询记录；
- 大对象进入应用数据目录；
- 文件写入采用临时文件 + 原子替换；
- 高风险迁移前做备份或可回滚 snapshot；
- API Key 进入 Secure Vault，不进普通数据库。

### 6.4 备份格式

推荐版本化 archive：

```text
manifest.json
cards/*.json
assets/<digest>.<ext>
```

`manifest.json` 至少包含格式版本、导出时间、条目数、摘要和 checksum，不包含 API Key、session、authkey 或设备秘密。

## 7. Cloud 与权威边界

### 7.1 上传

```text
local record
  -> contract validation
  -> auth / account state
  -> slot / size budget
  -> content policy
  -> metadata normalization
  -> signature verify
  -> cloud persistence
```

### 7.2 原生签名

- 服务器私钥/秘密永不进入客户端；
- 客户端只可展示和携带签名；
- 服务器是验签和签发的最终权威；
- 本地编辑可能使签名失效；
- 是否升级到非对称签名另立设计。

### 7.3 Strict

Strict 至少要求：

- 服务器可解析的在线实体 ID；
- 服务器生成或验证的 generation record；
- 服务器资格检查；
- 服务器计算 winner/eligibility；
- 服务器写入幂等 rating event。

本地库记录即使携带有效旧签名，MVP 也必须先显式进入线上库并获得服务器实体 ID，才能参与 Strict。

### 7.4 Hosted 运行时容灾与数据层级

服务器计算节点与业务事实源分离：

| 层级 | 典型状态 | 故障/清空后的要求 |
| --- | --- | --- |
| authoritative | D1 业务事实、DO room lifecycle、按领域定义的 R2 最终对象 | 不得由 Redis/VPS 推断或替代 |
| reconstructable | Redis cache、查询快照、可重建索引 | 回源 authority 或明确降级 |
| ephemeral/lossy | rate limit window、lease、presence、Pub/Sub、允许损失 telemetry 聚合 | 可以丢失，但不得改变已成立业务事实 |

判断状态能否只放 Redis 的标准是：`FLUSHALL` 后是否仍能从权威状态恢复正确业务语义，而不是简单按“重要/不重要”或“高频/低频”分类。

VPS 本地磁盘只保存部署包、临时文件、日志和可重建运行态，不保存唯一用户资产、rating、settlement 或 audit truth。香港服务器整机丢失后，应能以新实例 + secret + D1/DO/R2/Redis 连接恢复服务，而不依赖旧磁盘数据。

D1 本身仍是 Hono 与 Cloudflare DR 的共同依赖故障域。本轮不引入第二套 SQL 双写；D1 outage 时线上权威能力显式降级，Local/Direct 能力继续可用，并使用 Time Travel / 长期导出 runbook 恢复数据库。

## 8. Arena 多人协作架构

当前 Arena 多人 v1 是领域权威设计，平台重整必须兼容。产品/UI 与实时部署边界分开：

```text
Host/Member existing Arena UI
  -> short-lived room ticket
  -> apps/arena-room (Worker + one-room-one-DO)
  -> shared config / proposal / revision / membership
  -> generation reservation

Host generation intent + required host-local generation payload
  -> apps/api existing Arena generation
  -> one authoritative generation
  -> generation-scoped authenticated GenerationBridge
  -> apps/arena-room
  -> batched safe delta / terminal result projection
  -> all Arena viewers
```

强制边界：

- Arena 多人仍使用现有 `/arena`；独立 `apps/arena-room` 是运行时/部署边界，不是第二套产品。
- 完整 Arena store、UI state 与 host-local 原始数据以房主设备为本地权威；完整私有 payload 不得进入 Room DTO、DO、D1 或成员广播。
- authoritative generation 若需要 host-local 内容，可由房主按既有 generation contract 将所需 generation payload 直接临时发送给 `apps/api`；不得通过 Room 中转或持久化。
- Room Shared Config 是白名单投影；`userProviderConfig`、API credential、完整本地卡、战报运行态不得进入 Room plane。
- host-local 卡只产生受限 stub。
- 成员在本地修改 working copy；只有 Proposal 提交、房间同步和生成广播产生网络流量。
- Proposal 是 typed semantic change，不是任意 JSON Patch 或代码；除 `baseRevision` 外必须有足以验证 BASE 的 typed precondition。
- server/DO 重新校验 Proposal、依赖、当前 revision、引用版本并原子应用接受结果。
- online DataCard ref 必须携带服务器可验证的 `versionToken`，不得在 room revision 不变时静默漂移到最新版。
- 开始生成时冻结 shared config、participant snapshot、revision、`collaborativeInfluence` 与引用版本，并建立稳定 `generationRequestId`。
- 只有房主能发起 v1 权威生成；浏览器/API 重试不得产生第二次 generation。
- 房主浏览器不是 AI stream 中继；generation service 直接通过 GenerationBridge 向 Room 发布。
- 故事增量批处理后广播，不按 token 调 DO；delta 是临时传输，最终结果以 existing generation persistence / R2 为权威。
- Durable Object 使用 Hibernation WebSocket API，不使用固定轮询、高频 ping 或 per-room `setInterval`。
- reconnect 使用持久化 `roomEpoch + control seq`；story stream 使用独立 `generationId + chunkSeq`。
- 每个 DO 只有一个物理 alarm；多个 host-offline/idle deadline 使用最早截止时间调度器。
- D1 room directory 是派生索引；单房间 lifecycle 最终由 DO 判定。
- 旧 PVP 去留不影响该 v1 继续推进。

Monorepo 迁移前可以使用 `workers/arena-room` 等临时路径；迁移后部署单元为 `apps/arena-room`，共享 schema/投影/Proposal 进入 `packages/multiplayer-core` 或 `packages/contracts`。具体实时部署、GenerationBridge、恢复与 rollout 规则遵循 Arena Room ADR。

## 9. Admin 架构

Cloudflare Access 是入口控制，origin isolation 按部署形态选择：

```text
Worker-hosted Admin:
Browser -> Cloudflare Access -> apps/admin Worker -> application RBAC

Self-hosted/VPS Admin:
Browser -> Cloudflare Access -> Tunnel / equivalent private ingress
        -> origin validates Access assertion
        -> application RBAC
```

强制边界：

- Cloudflare Access 不是应用层授权的替代品；
- 自托管 origin 校验 Access assertion 的 signature/JWKS、issuer、audience、expiry；
- 自托管/VPS origin 必须有 Tunnel 或等价的 direct-origin bypass 防护；Worker 本身作为 origin 时不为形式一致强制增加 Tunnel；
- 管理浏览器不得持有 D1 管理 Token；
- 高风险操作必须有 reason、幂等、版本检查和审计；
- 数据导出和批量操作有额外权限；
- `feat/admin` 中的本地可信 handler 在上线前逐一威胁建模。

## 10. 资源与超时

服务器预算由服务端配置控制：

- user/IP concurrency；
- request burst；
- daily quota；
- max output；
- idle timeout；
- total timeout；
- provider timeout；
- memory/socket budget；
- D1/R2/DO limit。

Hono 主执行面还必须观察 Node/进程级容量信号，而不能只看请求成功率：process CPU、RSS/heap、event-loop utilization/delay、active request/stream/socket、AI upstream active/TTFB/abort、D1 round trip/rows、Redis latency/error/memory/eviction，以及 runtime/DR selection。容量告警阈值在真实生产基线后冻结，不在设计文档中凭空指定。

Direct 执行：

- 不使用项目额度；
- 默认不强制项目 total timeout；
- 支持用户 Stop；
- 可提供用户自定义 idle/total guard；
- 防止 APP 自身内存无限增长，使用流式落盘或窗口化累积。

多人房间：

- idle socket 使用 Hibernation；
- presence 不按固定频率写 D1；
- story chunk 不写 D1；
- room list 使用 limit/cursor 和索引；
- 资源指标不记录用户正文、API Key 或完整本地卡。

## 11. 发行与供应链

### Desktop

```text
version tag
  -> GitHub Actions matrix build
  -> tests
  -> platform bundle
  -> updater signature
  -> SHA-256
  -> artifact attestation
  -> draft release
  -> publish release（仓库启用时使用 immutable release）
```

- Updater public key 在客户端；
- private key 在受保护环境并有离线备份；
- workflow actions 尽量 pin 到完整 commit SHA；
- 已发布 release 资产不得覆写；
- 失败发布使用新版本修复，不替换既有二进制。

### Android

- 使用固定 signing key；
- GitHub Release 上传 APK、checksum 和 attestation；
- APP 可以提醒和下载，但安装流程遵循 Android 权限与用户确认；
- 不把 Desktop Updater 协议直接复制到 Android。

### iOS

独立处理 App Store、TestFlight、Web Distribution 或其他符合资格的路径。

## 12. Legacy 与兼容

- 本轮保持 Legacy 登录行为；
- Local 功能不要求登录；
- Online API 继续兼容当前认证；
- Monorepo 搬迁只能做行为等价适配；
- Auth 收口必须单独 ADR、迁移指标和回滚。

## 13. 旧 PVP 与 Multiplayer Core

旧 PVP 去留不阻塞结构重整，也不阻塞 Arena 多人 v1。

若低使用决定下线，优先保留/抽取：

- room lifecycle；
- participant/role；
- optimistic version；
- idempotency；
- simultaneous decisions；
- event log；
- presence/chat；
- recovery/stalled handling；
- audit。

牌堆、隐藏手牌、PVP winner glue、PVP 专用 Bot 和结算 UI 可以作为产品层移除。抽取时应优先兼容 Arena 多人的已冻结契约，而不是把 PVP schema 原样提升成通用层。

## 14. 外部设计指南

截至 2026-08-22，实现时优先参考官方资料：

- pnpm Workspaces
  https://pnpm.io/workspaces
- Tauri Capabilities / Scopes
  https://v2.tauri.app/security/capabilities/
  https://v2.tauri.app/security/scope/
- Tauri HTTP / SQL / Stronghold / Updater
  https://v2.tauri.app/plugin/http-client/
  https://v2.tauri.app/plugin/sql/
  https://v2.tauri.app/plugin/stronghold/
  https://v2.tauri.app/plugin/updater/
- Electron Security Checklist
  https://www.electronjs.org/docs/latest/tutorial/security
- Capacitor Storage Guidance
  https://capacitorjs.com/docs/guides/storage
- MDN Storage Persistence / Quotas
  https://developer.mozilla.org/en-US/docs/Web/API/StorageManager/persist
  https://developer.mozilla.org/en-US/docs/Web/API/Storage_API/Storage_quotas_and_eviction_criteria
- Cloudflare Access JWT Validation / Tunnel Origin Parameters
  https://developers.cloudflare.com/cloudflare-one/access-controls/applications/http-apps/authorization-cookie/validating-json/
  https://developers.cloudflare.com/tunnel/advanced/origin-parameters/
- Cloudflare Workers Pricing / Limits
  https://developers.cloudflare.com/workers/platform/pricing/
  https://developers.cloudflare.com/workers/platform/limits/
- Cloudflare Load Balancing / Monitors / Adaptive Routing
  https://developers.cloudflare.com/load-balancing/load-balancers/common-configurations/
  https://developers.cloudflare.com/load-balancing/monitors/
  https://developers.cloudflare.com/load-balancing/understand-basics/adaptive-routing/
- Cloudflare D1 Read Replication / Time Travel
  https://developers.cloudflare.com/d1/best-practices/read-replication/
  https://developers.cloudflare.com/d1/reference/time-travel/
- RFC 9110 Idempotent Methods
  https://www.rfc-editor.org/rfc/rfc9110#section-9.2.2
- Node.js 24 Performance Hooks
  https://nodejs.org/docs/latest-v24.x/api/perf_hooks.html
- GitHub Artifact Attestations / Immutable Releases / Push Protection
  https://docs.github.com/en/actions/how-tos/secure-your-work/use-artifact-attestations/use-artifact-attestations
  https://docs.github.com/en/code-security/concepts/supply-chain-security/immutable-releases
  https://docs.github.com/en/code-security/concepts/secret-security/push-protection
- GitHub Actions Security Hardening
  https://docs.github.com/en/actions/security-for-github-actions/security-guides/security-hardening-for-github-actions
- Android App Signing
  https://developer.android.com/studio/publish/app-signing
- Apple Website Distribution
  https://developer.apple.com/documentation/marketplacekit/distributing-your-app-from-your-website
- RFC 2119 / RFC 8174
  https://www.rfc-editor.org/rfc/rfc2119
  https://www.rfc-editor.org/rfc/rfc8174
