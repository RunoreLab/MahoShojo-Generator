# 低成本 Hosted DR 与客户端预检切换主题

更新时间：2026-08-29

状态：`active`

主题标识：`hosted-dr-low-cost-client-preflight`

## 权威性说明

本页是 Hosted Hono/Next DR、运行时选择、付费控制面、故障切换与重放边界的当前专项入口。

在本专项范围内，以下文档自合入起优先于较早文档中关于“客户端只消费 stable logical endpoint”“必须先纳管 stable control plane”“Cloudflare Load Balancing 是默认生产前置”以及“缺少 stable control plane 阻断默认分支并轨”的口径：

1. [低成本 Hosted DR 与客户端预检切换决策](../decisions/2026-08-29_070100_低成本HostedDR与客户端预检切换决策.md)；
2. [客户端预检 Hosted DR 选择规范](../specs/2026-08-29_070200_客户端预检HostedDR选择规范.md)；
3. [低成本 Hosted DR 范围调整与客户端预检实施计划](../plans/2026-08-29_070300_低成本HostedDR范围调整与客户端预检实施计划.md)。

较早的 Hosted DR ADR、G25E-1/G25E-2 设计、实现、演练与审计记录继续保留其历史、共享契约、数据权威、故障模型和 non-idempotent no-replay 价值；只有与本主题明确冲突的流量控制面和成本前置被取代。

## 当前决定

### 1. 免费资源与既有资源优先

成本基线以变更合入时 GitHub 默认分支已经依赖、且已明确承认的产品与资源为准。本决定形成时，默认分支为 `feature/v0.2.0_Battle_Growth_MahoShojo`，观察 HEAD 为 `07d8edf1544cf30867e81f929748a73ecd3cd362`。feature/refactor 分支中仅作为候选设计出现、尚未购买或授权的产品，不因“代码已经存在”而进入成本基线。

默认生产架构不得相对于该成本基线新增必须购买的产品、固定付费附加项或新的付费控制面依赖。Cloudflare Load Balancing 或等价托管流量产品不进入默认部署、默认 CI、默认 production build 或默认分支并轨门禁。

默认分支已经使用且已承认的服务器、Cloudflare 能力及其可用免费额度可以继续使用；但“已有”或“有免费额度”不等于无限预算。任何预期显著增加现有计量开支、资源规格或运营负担的变更仍需由人工审阅。

### 2. 保留双运行时，简化流量选择

- `apps/api` / Hono 继续是 Hosted HTTP 主执行面；
- `apps/web` / Next.js/OpenNext 继续为 manifest 明确列出的 capability 提供长期 DR adapter；
- 两侧继续共享 service/domain/contracts，不恢复两套业务逻辑；
- D1/R2/Redis 等权威与非权威边界不因流量选择方式改变；
- 默认生产选择从“外部付费 control plane”改为“客户端在首次 dispatch 前预检并选择一次 runtime”。

### 3. 新生成意图的选择顺序

```text
用户发起一个新的 generation intent
          │
          ▼
GET Hono /api/health/ready（单次、短超时）
          │
          ├─ ready ───────────────→ 将该 operation 固定到 Hono，再发送唯一一次 POST
          │
          └─ unavailable/timeout
                    │
                    ▼
            检查 manifest eligibility
                    │
          ┌─────────┴──────────┐
          │                    │
new-request-only / safe-read   fail-closed / 未登记
          │                    │
          ▼                    ▼
验证 Next DR readiness       明确降级，不发送 DR POST
          │
          ├─ ready ───────────────→ 将该 operation 固定到 Next DR，再发送唯一一次 POST
          └─ unavailable ─────────→ 明确降级
```

预检失败发生在 generation POST 之前，因此允许为 `new-request-only` operation 选择 DR；一旦 POST 已经开始 dispatch，客户端看见 timeout、断线或 `connection reset` 时不得改向另一 runtime 重放。

### 4. manifest 是 capability 资格的唯一机器事实

`config/hosted-dr-capabilities.json` 继续决定 route、method、request class、`drMode` 和 replay policy。当前已有一批常规生成接口为 `new-request-only`，可以进入客户端预检切换；Arena generation、部分 companion route 等仍为 `fail-closed`，在完成独立权威、Redis/恢复与幂等证明前不得被客户端强行切到 DR。

### 5. Cloudflare LB 成果保留但不激活

低维护成本的 manifest、selector seam、validator、双 adapter、readiness、故障演练、版本兼容与 CI 契约继续保留。只服务于托管 stable control plane 的代码和文档可以留在仓库中作为 `optional-disabled` / `reference-only` 能力，但必须满足：

- 默认不创建、购买、配置或调用付费产品；
- 默认构建和合并不因 `controlPlane.provisioning=not-provisioned` 失败；
- 不要求维护生产 secret、DNS/LB pool/monitor 或生产 evidence；
- 不允许可选路径静默改变当前客户端预检路径；
- 未来启用前必须另立 ADR、预算、授权、演练和回滚计划。

现有实现与恢复点见 [Cloudflare LB 与 stable control plane 实现归档恢复指南](../references/2026-08-29_070400_CloudflareLB与StableControlPlane实现归档恢复指南.md)。

## 当前生产与合并口径

- “Cloudflare LB / stable control plane 尚未纳管”不再单独构成默认分支并轨或常规 production build blocker；
- production build 仍必须对当前选择的 `client-preflight` 模式做配置、origin、CORS、manifest、DR artifact 与 secret/binding fail-closed 校验；
- 管理后台 Access/direct-origin bypass、真实 secret、D1/R2/Redis、schema/version、用户可感知契约和其他独立 blocker 不因本决定自动关闭；
- 仓库已在 `34d79bec` 原子实现客户端 selector 与新 production gate；这不等于 production 已部署或已观察到真实切换；
- 本轮没有执行或授权 production deploy、DNS、LB、Access、secret、数据或服务器修改。

## 当前实现状态

- manifest 明确 `defaultMode=client-preflight`、`managedControlPlane=optional-disabled`，并生成公开 routing 与最小
  route/method policy；完整 manifest 的 secret/binding 元数据不会进入客户端投影；
- Hono readiness 已补齐 `service`、`placement`、`contractVersion`、`no-store` 与 CORS contract；
- DR readiness 接收 generated canonical capability/method，复用同一 capability guard 校验目标 operation 的 secret、binding、
  database provider/consistency，并要求响应精确回显；通用 readiness 不会被客户端误当成 operation ready；
- Web 每个显式 generation intent 先探测、后固定唯一 placement；React intent latch 跨 rerender/重复 callback 保持同一
  intent 到 response body 终态；`fail-closed`/未登记 operation 不进入 DR，业务 dispatch 后写操作 transport/未知 5xx、
  stream/cancel 或 SSE EOF-before-done 不跨 runtime 重放，明确 SSE `done` / `error` 分别收敛成功/失败；readiness 复用共享
  contract version 兼容窗口；
- `pnpm verify:hosted-dr:client-preflight` 使用隔离 loopback HTTP transport 验证 primary ready、eligible DR、fail-closed 与
  POST disconnect 四场景，并输出 operation identity、placement、探测/POST 次数、latency 与 terminal class；
- Next/OpenNext production build 在生成静态产物后强制扫描 client bundle，拒绝缺失 routing projection、manifest
  secret/binding 名称及任意 client JavaScript 的静态 `.internal` / `.local` / localhost/无点/IP endpoint；framework URL parser
  只允许精确 synthetic fixture，hostname 先移除尾随点并规范化 IPv6；scheme 按大小写无关识别，含
  credential/query/fragment 的静态 URL fail closed 且不在错误中回显；contract gate 同时比较 route ID、规范化 pattern 与 methods；
- Cloudflare LB/stable control plane、`dr-candidate` 与旧 runbook 保留为 `optional-disabled` / `reference-only`，没有进入默认
  workflow、production build 或付费成本基线；
- production deploy、真实生产 fault drill、客户端真实浏览器网络切换旅程和生产观测仍为 `DEFERRED`，不得用本地证据冒充。

## 文档关系

| 层级 | 文档 | 当前地位 |
| --- | --- | --- |
| 决策 | [低成本 Hosted DR 与客户端预检切换决策](../decisions/2026-08-29_070100_低成本HostedDR与客户端预检切换决策.md) | `accepted`；当前成本与路由选择权威 |
| 规范 | [客户端预检 Hosted DR 选择规范](../specs/2026-08-29_070200_客户端预检HostedDR选择规范.md) | `accepted`；实现和验收契约 |
| 计划 | [低成本 Hosted DR 范围调整与客户端预检实施计划](../plans/2026-08-29_070300_低成本HostedDR范围调整与客户端预检实施计划.md) | `completed`；代码、本地验证与独立审查已收口 |
| 实施日志 | [低成本 Hosted DR 客户端预检实施与审查日志](../logs/2026-08-29_085000_低成本HostedDR客户端预检实施与审查日志.md) | 本地证据、成本/外部动作、审查与回滚记录 |
| 归档 | [Cloudflare LB 与 stable control plane 实现归档恢复指南](../references/2026-08-29_070400_CloudflareLB与StableControlPlane实现归档恢复指南.md) | `historical / optional / reference-only` |
| 综合主题 | [平台重整与本地优先架构主题](./2026-08-22_022000_平台重整与本地优先架构.md) | 除本专项被取代段落外继续有效 |
| 较早 ADR | [Hosted 运行时容灾与 Cloudflare 灾备决策](../decisions/2026-08-23_104000_Hosted运行时容灾与Cloudflare灾备决策.md) | 部分被取代；双 runtime、共享核心、数据权威、no-replay 等继续有效 |
| 较早设计 | [G25E-1 Hosted DR 契约与稳定入口设计](../specs/2026-08-26_175654_G25E-1_Hosted_DR契约与稳定入口设计.md) | stable control plane/client-only-stable-endpoint 部分被取代；manifest、双 provider、契约门禁继续有效 |
| 可选 Runbook | [Hosted DR 生产演练授权前置与回滚](../runbooks/2026-08-27_000000_Hosted_DR生产演练授权前置与回滚.md) | 已归档为未来托管 control plane 可选 Runbook，不是当前默认路径 |

## 开发入口

涉及 Hosted API origin、generation fetch、readiness、DR capability、production build gate、Cloudflare LB/DNS/Worker router 或跨 runtime retry 的 PR，必须：

- 在描述中引用 `ADR-hosted-dr-low-cost-client-preflight` 与 `SPEC-hosted-dr-client-preflight-v1`；
- 明确请求在 dispatch 前还是 dispatch 后；
- 明确 capability 的 `drMode` 与 replay policy；
- 证明不会因 timeout/断线创建第二次 Provider 调用；
- 说明是否新增产品、固定费用、计量开支或生产资源；
- 若引入付费控制面，先形成新的 accepted ADR 与预算授权，不能在 feature flag、workflow 或环境变量中暗中激活。
