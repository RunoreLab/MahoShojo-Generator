# 提交 6345a51a 审查整改 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 核实并关闭提交 `6345a51a` 审查中确认属实的 client bundle URL 扫描绕过与 Node 22 测试兼容问题，并用最终 HEAD 的本地门禁和独立复审恢复可信的完成证据。

**Architecture:** 保持现有 Hosted DR client-preflight、route pin 与 no-replay 架构不变，只修复安全门禁的 URL candidate 提取和 Arena Room 测试事件 fixture。规范性语义继续以 `SPEC-hosted-dr-client-preflight-v1` 为准；实施日志只记录当前证据，不用本地结果冒充未运行的远端 CI。

**Tech Stack:** Node.js、TypeScript、Vitest、pnpm workspace、Hono WebSocket types。

---

## 文件职责

- `tests/hosted-dr-client-bundle.test.ts`：固定带 path、端口、合法 path 分隔符和 percent-encoded metadata value 的 URL mutation，以及错误不回显。
- `apps/web/scripts/check-hosted-dr-client-bundle.mjs`：提取完整静态 URL candidate，再用 WHATWG `URL` 判断 credential/query/fragment 与 internal hostname。
- `apps/api/tests/room-websocket-gateway.test.ts`：提供不依赖 Node 全局 `CloseEvent` 的 close event fixture，保持生产 gateway 不变。
- `docs/logs/2026-08-29_085000_低成本HostedDR客户端预检实施与审查日志.md`：更正旧 HEAD 远端 CI 失败事实，记录新 checkpoint、实际验证和未验证边界。
- `docs/plans/2026-08-29_070300_低成本HostedDR范围调整与客户端预检实施计划.md`：补充本次审查整改 checkpoint，不改变 accepted 目标。

### Task 1：关闭带 path 的 URL metadata 扫描绕过

- [ ] **Step 1：增加失败 mutation**

在 `tests/hosted-dr-client-bundle.test.ts` 的 URL metadata 表中至少加入：

```ts
['path + query', 'https://example.com/path?token=bundle-secret', 'bundle-secret'],
['nested path + fragment', 'https://example.com/a/b#bundle-secret', 'bundle-secret'],
['path + port + encoded query', 'https://example.com:8443/a;b?token=bundle%2Dsecret', 'bundle%2Dsecret'],
['comma path + encoded fragment', 'https://example.com/a,b#bundle%2Dsecret', 'bundle%2Dsecret'],
```

同时把 routing fixture 表达为真实 JavaScript 字符串，避免用裸分号人为截断 URL candidate。

- [ ] **Step 2：运行 RED 测试**

Run: `pnpm exec vitest run --config vitest.config.ts tests/hosted-dr-client-bundle.test.ts`

Expected: 新增的 path + query/fragment cases 因 checker 返回 `0` 而失败，原有 case 继续通过。

- [ ] **Step 3：最小修复 candidate 提取**

在 `apps/web/scripts/check-hosted-dr-client-bundle.mjs` 中将 authority-only pattern 改为直到 JavaScript 字符串边界为止的完整 candidate：

```js
const urlCandidatePattern = /https?:\/\/[^\s"'`]+/giu;
```

`scanInternalOrigins` 对每个完整 candidate 执行 `new URL(candidate)`；精确更新 `http://localhost/` location fallback fixture。credential/query/fragment、非法 URL 与 internal endpoint 错误只报告类别和文件，不回显 candidate。

- [ ] **Step 4：运行 GREEN 与 contract 验证**

Run: `pnpm exec vitest run --config vitest.config.ts tests/hosted-dr-client-bundle.test.ts tests/hosted-dr-contract.test.ts`

Expected: 全部 PASS，错误输出不含 mutation secret marker。

- [ ] **Step 5：原子提交**

```bash
git add apps/web/scripts/check-hosted-dr-client-bundle.mjs tests/hosted-dr-client-bundle.test.ts
git commit -m "fix(web): 闭合客户端产物URL路径扫描绕过"
```

### Task 2：消除 Node 22 `CloseEvent` 测试依赖

- [ ] **Step 1：在无全局 `CloseEvent` 环境复现**

用临时 Node preload 删除 `globalThis.CloseEvent` 后运行：

```bash
pnpm --filter @mahoshojo/api exec vitest run --config vitest.config.ts tests/room-websocket-gateway.test.ts
```

Expected: 现有 close lifecycle cases 抛出 `ReferenceError: CloseEvent is not defined`。

- [ ] **Step 2：增加 Node-compatible fixture**

在 `apps/api/tests/room-websocket-gateway.test.ts` 增加结构上满足 Hono `WSEvents.onClose` 的 helper：

```ts
const closeEvent = (code = 1000): CloseEvent => Object.assign(
  new Event('close'),
  { code, reason: '', wasClean: code === 1000 },
);
```

将当前文件所有 `new CloseEvent(...)` 改为 `closeEvent()`；不修改生产 gateway，因为其 `onClose` 不读取 event，且官方 Node adapter 已负责生产事件适配。

- [ ] **Step 3：运行 GREEN、API typecheck 与 lint**

Run: `pnpm --filter @mahoshojo/api exec vitest run --config vitest.config.ts tests/room-websocket-gateway.test.ts`

Run: `pnpm --filter @mahoshojo/api run build`

Run: `pnpm --filter @mahoshojo/api run lint`

Expected: 普通环境和删除全局 `CloseEvent` 的模拟环境均 PASS；类型检查、lint 无新警告。

- [ ] **Step 4：原子提交**

```bash
git add apps/api/tests/room-websocket-gateway.test.ts
git commit -m "test(api): 移除Room测试的浏览器CloseEvent依赖"
```

### Task 3：更正实施证据

- [ ] **Step 1：更新现有计划与日志**

记录两项责任边界：URL scanner 是 `f76861b4` 引入的实现缺口；`CloseEvent` 早于 Hosted DR 范围，但阻断了 `6345a51a` 的 Repository CI。将旧远端 HEAD 的失败、最终本地 HEAD 的实际结果、尚未 push 前无法声称的新远端 CI 结果分别写清。

- [ ] **Step 2：校验文档引用和差异**

Run: `git diff --check`

Run: `rg -n "6345a51a|CloseEvent|path.*query|open Important|ci:verify" docs/plans/2026-08-29_070300_低成本HostedDR范围调整与客户端预检实施计划.md docs/logs/2026-08-29_085000_低成本HostedDR客户端预检实施与审查日志.md`

Expected: checkpoint、状态、验证环境与未验证项无互相矛盾。

- [ ] **Step 3：原子提交**

```bash
git add docs/plans/2026-08-29_070300_低成本HostedDR范围调整与客户端预检实施计划.md docs/logs/2026-08-29_085000_低成本HostedDR客户端预检实施与审查日志.md
git commit -m "docs(platform): 更新客户端预检审查整改证据"
```

### Task 4：最终验证与复审

- [ ] **Step 1：运行 targeted 和 workspace 门禁**

Run: `pnpm run check:hosted-dr`

Run: `pnpm --filter @mahoshojo/api run test`

Run: `pnpm run ci:verify`

Expected: 全部 exit `0`，无新增 warning；若既有 warning 仍存在，记录来源和影响。

- [ ] **Step 2：运行生产构建与隔离 fault evidence**

Run: `pnpm run build:cf`

Run: `pnpm run verify:hosted-dr:client-preflight`

Expected: OpenNext artifact 通过完整 bundle safety gate；四个 client-preflight loopback 场景继续 PASS。

- [ ] **Step 3：独立复审最终差异**

复审 accepted spec、candidate extraction 边界、错误不回显、Node 22 兼容、提交原子性和文档证据。只有在 `Critical=0 / Important=0` 且实际运行的门禁通过后，才保留 `DONE`；新远端 Repository CI 在 push 前必须列为待验证，而不是写成 PASS。
