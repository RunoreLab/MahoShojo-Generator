# Gemma 4 模型接入 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为现有 Gemma 系列入口补充 `gemma-4-31b-it` 与 `gemma-4-26b-a4b-it`，并同步更新严格排位默认回退链、最小测试与站内推荐文案。

**Architecture:** 直接沿用现有 Gemma 配置结构，在 `lib/ai/constants.ts` 的两个模型目录中插入新模型条目；严格排位策略仅提升 `gemma-4-31b-it` 为默认回退首选，避免把 `a4b` 版本纳入核心默认链路。测试保持最小化，只覆盖回退顺序和目录去重相关行为。

**Tech Stack:** TypeScript, Bun test, Next.js, Tailwind, 项目现有 AI provider catalog

---

### Task 1: 更新严格排位测试

**Files:**
- Modify: `tests/strict-ranked-model-policy.test.ts`

- [ ] **Step 1: 写失败测试**

更新默认回退顺序断言，要求包含 `gemma-4-31b-it`，并保持黑名单校验通过。

- [ ] **Step 2: 运行测试确认失败**

Run: `bun test tests/strict-ranked-model-policy.test.ts`
Expected: 由于实现尚未更新，顺序断言失败。

- [ ] **Step 3: 写最小实现**

在 `lib/arena/ranked-model-policy.ts` 中更新默认回退链。

- [ ] **Step 4: 运行测试确认通过**

Run: `bun test tests/strict-ranked-model-policy.test.ts`
Expected: PASS

### Task 2: 更新模型目录与最小目录断言

**Files:**
- Modify: `lib/ai/constants.ts`
- Modify: `tests/ai-provider-catalog.test.ts`

- [ ] **Step 1: 写失败测试**

增加轻量断言，确认系统默认目录与 KouriChat 目录均包含两个新的 Gemma 4 模型。

- [ ] **Step 2: 运行测试确认失败**

Run: `bun test tests/ai-provider-catalog.test.ts`
Expected: 因目录尚未补充新模型而失败。

- [ ] **Step 3: 写最小实现**

在两个已有 Gemma 模型目录中插入 `gemma-4-31b-it` 与 `gemma-4-26b-a4b-it`。

- [ ] **Step 4: 运行测试确认通过**

Run: `bun test tests/ai-provider-catalog.test.ts`
Expected: PASS

### Task 3: 更新站内文案与完成校验

**Files:**
- Modify: `public/encyclopedia/community-rules.md`

- [ ] **Step 1: 更新轻量推荐文案**

将百科中的稳定轻量模型推荐补充 Gemma 4 新模型。

- [ ] **Step 2: 运行相关测试与静态校验**

Run: `bun test tests/strict-ranked-model-policy.test.ts tests/ai-provider-catalog.test.ts`
Expected: PASS

Run: `bun run lint`
Expected: exit 0
