# 众查违规自动未通过并封禁实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让未来新增的众查违规结论在写回案件后，自动把目标数据卡设为未通过并封禁，并在单条处理结果通知中明确该自动处置。

**Architecture:** 保持现有 `report_cases` 状态机不变，新增一个系统侧“结果治理执行器”负责把不利最终结论映射到 `data_cards.review_status='rejected'` 与 `is_public=-1`。当前分支只在众查 `violation -> confirmed_violation` 链路接入该 helper，但 helper 的接口设计预留给未来 `feat/admin` 入口复用。

**Tech Stack:** Next.js, TypeScript, Drizzle ORM, Bun test

---

### Task 1: 扩展数据卡系统治理仓储

**Files:**
- Modify: `lib/db/repositories/data-cards-core.ts`
- Test: `tests/crowd-review-service.test.ts`

- [x] **Step 1: 定义系统侧更新入口**

在 `lib/db/repositories/data-cards-core.ts` 增加系统侧更新函数签名，按 `cardId` 直接更新卡片治理状态，不要求 `userId`。

目标接口：

```ts
export const enforceDataCardModerationOutcome = async (
  db: AppDrizzleDb,
  input: {
    cardId: string;
    reviewStatus: 'rejected';
    isPublic: -1;
    now: string;
  },
): Promise<{
  found: boolean;
  changed: boolean;
}>;
```

- [x] **Step 2: 最小实现仓储更新**

在该函数内部执行：

```ts
await db
  .update(dataCards)
  .set({
    reviewStatus: input.reviewStatus,
    isPublic: sql`${input.isPublic}`,
    publicSince: null,
    updatedAt: input.now,
  })
  .where(and(eq(dataCards.id, input.cardId), isNull(dataCards.deletedAt)))
  .returning({
    id: dataCards.id,
  });
```

并按“是否找到卡”和“是否原本已是 `rejected + -1`”返回 `found/changed`。

- [x] **Step 3: 保持实现幂等**

要求：

```ts
// 原本就是 rejected + -1
return { found: true, changed: false };

// 本次首次落地治理
return { found: true, changed: true };

// 卡不存在或已删除
return { found: false, changed: false };
```

### Task 2: 先写众查治理失败测试

**Files:**
- Modify: `tests/crowd-review-service.test.ts`
- Reference: `lib/crowd-review/service.ts`

- [x] **Step 1: 为众查违规自动封禁添加失败测试**

在 `tests/crowd-review-service.test.ts` 新增一个测试，覆盖“众查 `violation` 结案后同时写卡片治理状态”。

测试骨架：

```ts
test('concluded violation round enforces target card to rejected and banned', async () => {
  const writes: Array<Record<string, unknown>> = [];
  const service = buildService({
    repo: {
      getAssignmentByIdForInspector: async () => makeAssignmentRow(),
      getRoundById: async () => makeRoundRow(),
      listAssignmentsByRound: async () => [
        makeAssignmentRow({ id: 'assignment-1', status: 'voted', decision: 'violation' }),
        makeAssignmentRow({ id: 'assignment-2', status: 'voted', decision: 'violation', inspectorUserId: 8 }),
      ],
        finalizeAssignment: async () => true,
        updateRound: async () => true,
        updateReportCaseResolution: async () => true,
      },
      enforceTargetDataCardModerationOutcome: async (_db, input) => {
        writes.push(input);
        return { found: true, changed: true };
      },
    });

  await service.submitCrowdReviewDecision({
    db: {} as never,
    userId: 7,
    assignmentId: 'assignment-1',
    decision: 'violation',
    note: null,
  });

  expect(writes).toContainEqual(
    expect.objectContaining({
      cardId: 'card-1',
      reviewStatus: 'rejected',
      isPublic: -1,
    }),
  );
});
```

- [x] **Step 2: 为非不利结论不治理添加失败测试**

新增一个 `no_violation` 场景，确认不会调用卡片治理 helper。

```ts
test('concluded no_violation round does not enforce target card moderation outcome', async () => {
  let called = false;
  // ... arrange no_violation round
  expect(called).toBe(false);
});
```

- [x] **Step 3: 运行定向测试并确认先失败**

Run:

```bash
bun test tests/crowd-review-service.test.ts
```

Expected:

- 新增测试失败
- 失败原因是缺少治理 helper 依赖或未发生预期调用

### Task 3: 实现结果治理 helper 与众查接线

**Files:**
- Create: `lib/data-card-reports/outcome-enforcement.ts`
- Modify: `lib/crowd-review/service.ts`
- Modify: `lib/db/repositories/data-cards-core.ts`

- [x] **Step 1: 新增统一结果治理 helper**

在 `lib/data-card-reports/outcome-enforcement.ts` 新增实现：

```ts
import type { AppDrizzleDb } from '@/lib/db/drizzle';
import type { ReportResolutionCode } from '@/lib/db/schema';

const ADVERSE_FINAL_RESOLUTION_CODES: ReportResolutionCode[] = [
  'confirmed_violation',
  'content_removed',
  'self_remediated',
];

export const isAdverseFinalReportResolutionCode = (
  resolutionCode: ReportResolutionCode | null,
): resolutionCode is 'confirmed_violation' | 'content_removed' | 'self_remediated' =>
  resolutionCode != null && ADVERSE_FINAL_RESOLUTION_CODES.includes(resolutionCode);
```

再补主 helper：

```ts
export async function enforceResolvedReportCaseTargetCard(
  input: {
    db: AppDrizzleDb;
    targetEntityType: string;
    targetEntityId: string;
    resolutionCode: ReportResolutionCode | null;
    now: string;
    enforceDataCardModerationOutcome: (/* ... */) => Promise<{ found: boolean; changed: boolean }>;
  },
) {
  if (input.targetEntityType !== 'data_card') return { applied: false, found: false, changed: false };
  if (!isAdverseFinalReportResolutionCode(input.resolutionCode)) return { applied: false, found: false, changed: false };
  const result = await input.enforceDataCardModerationOutcome(input.db, {
    cardId: input.targetEntityId,
    reviewStatus: 'rejected',
    isPublic: -1,
    now: input.now,
  });
  return { applied: true, ...result };
}
```

- [x] **Step 2: 在众查服务依赖中挂上治理入口**

在 `lib/crowd-review/service.ts` 的 repo/deps 里补一个新的依赖接口，例如：

```ts
enforceTargetDataCardModerationOutcome: (
  db: AppDrizzleDb,
  input: {
    cardId: string;
    reviewStatus: 'rejected';
    isPublic: -1;
    now: string;
  },
) => Promise<{ found: boolean; changed: boolean }>;
```

并在默认 runtime repo 中接到 `data-cards-core` 的新仓储函数。

- [x] **Step 3: 在众查最终违规写回后调用治理 helper**

在 `resolveAssignmentSummary()` 的 `violation/no_violation` 收敛分支里，保持顺序：

```ts
await applyCrowdReviewRoundResultToReportCase(...);

if (summaryPlan.nextResultCode === 'violation') {
  await enforceResolvedReportCaseTargetCard({
    db,
    targetEntityType: 'data_card',
    targetEntityId: assignments[0]?.targetEntityId ?? '',
    resolutionCode: 'confirmed_violation',
    now,
    enforceDataCardModerationOutcome: deps.repo.enforceTargetDataCardModerationOutcome,
  });
}

await syncFinalizedRoundAssignments(...);
```

要求：

- 只在 `violation` 时调用
- `no_violation` / `escalated` 不调用
- 卡片治理失败时抛错，不静默吞掉

- [x] **Step 4: 运行众查测试并确认转绿**

Run:

```bash
bun test tests/crowd-review-service.test.ts
```

Expected:

- 新增众查测试通过
- 原有众查测试继续通过

### Task 4: 扩展处理结果通知文案

**Files:**
- Modify: `lib/report-appeals/service.ts`
- Modify: `lib/messages/templates.ts`
- Modify: `tests/messages-templates.test.ts`
- Modify: `tests/report-appeals-service.test.ts`

- [x] **Step 1: 先写模板失败测试**

在 `tests/messages-templates.test.ts` 新增一个 `report_case_resolved` 模板测试，断言当 payload 包含自动治理标记时，正文包含：

```ts
expect(rendered.body).toContain('已自动将该数据卡设为未通过并封禁');
```

- [x] **Step 2: 在通知 payload 中补治理标记**

修改 `lib/report-appeals/service.ts` 的 `notifyReportCaseResolutionIfNeeded()`，发送消息时附带：

```ts
cardAutoRejected: true,
cardAutoBanned: true,
```

当前分支里这条通知只会在 `confirmed_violation` 众查结案后触发，因此可以直接带上这两个标记。

- [x] **Step 3: 修改模板文案**

在 `lib/messages/templates.ts` 的 `user.moderation.report_case_resolved` 中追加条件正文：

```ts
const cardAutoRejected = input.payload.cardAutoRejected === true;
const cardAutoBanned = input.payload.cardAutoBanned === true;

body: [
  resolutionLabel ? `当前处理结果：${resolutionLabel}` : '你的公开数据卡已完成处理。',
  cardAutoRejected || cardAutoBanned ? '系统已自动将该数据卡设为未通过并封禁。' : null,
  '如对处理结果有异议，可前往申诉页提交说明。',
]
```

- [x] **Step 4: 运行模板与申诉服务测试**

Run:

```bash
bun test tests/messages-templates.test.ts tests/report-appeals-service.test.ts
```

Expected:

- 模板测试通过
- 处理结果通知相关测试继续通过

### Task 5: 回归验证

**Files:**
- Reference: `tests/crowd-review-service.test.ts`
- Reference: `tests/messages-templates.test.ts`
- Reference: `tests/report-appeals-service.test.ts`

- [x] **Step 1: 运行聚焦测试集**

Run:

```bash
bun test tests/crowd-review-service.test.ts tests/messages-templates.test.ts tests/report-appeals-service.test.ts
```

Expected:

- 全部 PASS

- [x] **Step 2: 运行 lint**

Run:

```bash
bun run lint
```

Expected:

- 无错误退出

- [x] **Step 3: 如时间允许，跑一次构建**

Run:

```bash
bun run build
```

Expected:

- 构建成功

- [x] **Step 4: 更新计划勾选并整理结果**

把本文件中的执行状态勾选完成，并总结：

- 新增了哪些测试
- 哪条链路开始自动把卡设为 `rejected + -1`
- 哪些范围仍留给 `feat/admin`

执行结果：

- 新增众查服务测试，覆盖 `violation` 结案后自动把目标卡写成 `review_status='rejected'` 且 `is_public=-1`，并覆盖 `no_violation` 不触发治理。
- 扩展消息模板与申诉服务测试，覆盖处理结果通知正文包含“系统已自动将该数据卡设为未通过并封禁”，以及通知 payload 带上 `cardAutoRejected/cardAutoBanned`。
- 当前只有众查 `violation -> confirmed_violation` 链路会在案件写回后自动执行卡片治理；历史案件与当前分支外的管理员入口均不做回填或补接。
- `lib/data-card-reports/outcome-enforcement.ts` 中的 helper 已按“结果码 -> 卡片治理动作”抽象出来，供未来 `feat/admin` 分支复用。
