# Arena 多人 GMR-09 权威生成发布实施计划

日期：2026-08-28

状态：`implementing`

实现基线：`9d212014dc43ec2f8a39f18bfbea41187d03cbc8`

设计依据：[GMR-09 权威生成发布设计](../specs/2026-08-28_190500_Arena多人GMR-09权威生成发布设计.md)

## 1. 执行原则

- 先写失败测试，再做最小实现；每个 checkpoint 可独立回滚。
- 复用 G25R generation reservation/lease/replay/finalization，不复制 AI lifecycle、SSE parser 或 D1/R2 persistence。
- Room checkpoint/wire 只保存 frozen safe metadata；完整 host payload与 Provider secret只临时交给 generation service。
- 所有 mutation 均保持 epoch/current generation/capability fence；unknown 先对账，不自动重放。
- 每批运行受影响 package targeted tests + build/lint；跨 package完成后运行真实 Redis和 workspace gates。

## 2. Atomic checkpoints

### GMR09-A：contract 与 trusted owned-generation seam

新增 strict start/view DTO、routes、story sequence语义测试；在 hosted-api generation service 增加 server-only owned projection/
typed resume seam，复用现有 `resolveOwnedStateForActor`、replay 和 terminal fallback。Node runtime返回显式 app-internal capability，并
覆盖 PVP actor scope、D1/R2 fallback、无 reasoning/telemetry/provider diagnostic projection。

Checkpoint：`feat(arena): 增加多人生成恢复契约与内部端口`

### GMR09-B：Room transient story queue 与 publisher

先测试 RoomActor同一 bounded queue 的 story/current-generation/epoch/running/sequence/frame-size fence、transient no-checkpoint/
no-control-replay、terminal ordering和 idempotent duplicate no-fanout；再实现分栏 fanout和 WSS safe story发送。

新增 `RoomGenerationPublisher` 消费 typed generation event，忽略 reasoning/telemetry，bootstrap snapshot只作 baseline，markdown
批次映射连续 `chunkSeq`，terminal经 core mirror。

Checkpoint：`feat(api): 实现 Room generation 安全发布器`

### GMR09-C：generation coordinator 与 HTTP composition

新增 coordinator：host/epoch/revision、pending publish、全部 online ref revalidation、canonical digest/frozen snapshot、deterministic
PVP generation ID、opaque reservation、single-flight job、duplicate先reconcile、unknown no-POST、process recovery attach。接通 Hono
start/view routes、rate limit/error/no-store/body limit与 `main.ts` composition。

Checkpoint：`feat(api): 接通 Arena 多人权威生成入口`

### GMR09-D：Web generation cursor、resync 与共享 controller

扩展 client/controller：start intent、member-safe view、generation control reducer、0-based story cursor、duplicate/stale/gap、
single-flight GET resync、terminal authoritative replace、epoch/attempt/dispose fence、control+story reconnect cursor。

把 Room controller提升到 Arena page context；host生成走 room start且稳定 request ID，member/active/unknown禁止生成；单人和 feature-off
继续既有 flow。多人结果只显示 safe markdown/status，不写入 owner-local combatant/report side-effect model。

Checkpoint：`feat(web): 接通多人生成恢复与结果视图`

### GMR09-E：fault、真实边界与退出审查

覆盖 duplicate/response-loss、host disconnect/refresh、Redis unavailable、Hono restart+Redis survives、old epoch/attempt、story gap/
slow consumer、R2/D1 terminal、secret canary和 feature-off/single-player。扩展本地 Redis verifier，运行 contracts/core/hosted-api/
hosted-runtime/API/Web targeted/full suites、typecheck/lint/build、boundary/generated/naming、`pnpm ci:verify`、`git diff --check`。

Builder self-review 后使用独立上下文执行 architecture、security/authority、compatibility/replay/data和 test adequacy审查；修复全部
Critical/Important，Minor修复或记录 non-blocking依据。更新 Goal 指南、topic和实施日志。

Checkpoint：`docs(arena): 记录 GMR-09 权威生成退出证据`

## 3. Stop / blocker 规则

若证据要求放宽 server authority、PVP/actor ownership、secret隔离、generation single producer、epoch/attempt fence、D1/R2 final
authority或 unknown no-blind-replay，则停止实现并形成需人工裁决的完整提案，不移动验收标准。普通可逆接口、队列、projection、
恢复与测试选择自行决策。
