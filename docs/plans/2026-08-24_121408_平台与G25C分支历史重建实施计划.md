# 平台与 G25C 分支历史重建实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 删除事后补建但无法形成真实历史基线的 Phase 0 门禁资产，恢复绿色 platform 基线，并让暂停的 Phase 2.5C Agent 在独立 G25C 分支顶层继续使用启用中的 RED 合约。

**Architecture:** 以共同基点 `22c36730` 重建 `refactor/platform-rearchitecture`，只保留安全、runtime、文档状态和生成运行时迁移；将当前即可成立的 boundary 回归保留在绿色基线，将未来态 ownership/release RED 仅追加到 `refactor/g25c-apps-api` 顶层。两个原分支先建立本地备份，重建过程不触碰 stash、不 push、不部署。

**Tech Stack:** Git、pnpm workspace、Vitest、TypeScript、Dockerfile、Markdown。

---

### Task 1: 固定恢复点

**Files:**
- No file changes.

- [ ] **Step 1: 核实两个工作树均无未提交修改**

  Run: `git status --short --branch && git -C .worktrees/g25c-apps-api status --short --branch`

  Expected: 两个工作树只输出分支状态，不输出文件修改。

- [ ] **Step 2: 创建原始分支备份**

  Run: `git branch backup/platform-rearchitecture-before-phase0-cleanup-20260824 ac49b648 && git branch backup/g25c-apps-api-before-phase0-cleanup-20260824 c7a7c803`

  Expected: 两个备份分别固定原 platform 与 G25C tip。

### Task 2: 重建绿色 platform 提交链

**Files:**
- Modify: `docs/plans/2026-08-22_022700_平台重整分阶段实施计划.md`
- Modify: `docs/plans/2026-08-24_043641_G25B-2Creator与残余生成族shared-service收口实施计划.md`
- Modify: `docs/plans/2026-08-24_053840_G25C_apps-api真实应用激活与HonoTelemetry收口实施计划.md`
- Modify: `docs/topics/2026-08-22_022000_平台重整与本地优先架构.md`
- Do not create: `docs/logs/2026-08-24_065802_平台重整Phase0追溯式退出审计.md`
- Do not retain: `config/phase0-*.json`
- Do not retain: `scripts/audit-platform-phase0.mjs`
- Do not retain: `scripts/pvp90-query.mjs`
- Do not retain: `scripts/pvp90-report.mjs`
- Do not retain: `tests/platform-phase0-*.test.ts`

- [ ] **Step 1: 从 `22c36730` 建立临时重建分支**

  Run: `git switch -c rewrite/platform-rearchitecture-phase0-cleanup-20260824 22c36730`

  Expected: 新分支以共同基点为唯一祖先。

- [ ] **Step 2: 按原逻辑顺序保留安全与 runtime 提交**

  Run: `git cherry-pick e1a1d14f 1bfdaf0d 9d832e65 59d3c3a9 1227e2e4 272daf6c 3b5db94b`

  Expected: 七个提交无 Phase 0 config/script/test 资产。

- [ ] **Step 3: 重写状态文档提交**

  Cherry-pick `96cbdde1` 后，删除详细 Phase 0 追溯日志，只在 accepted 阶段计划保留“历史证据不完整、不得追溯宣称闭合”的事实；G25C 文档保持“执行中”，不得声称 future-state RED 已进入绿色基线。

- [ ] **Step 4: 保留其余 runtime 与生成迁移提交**

  Run: `git cherry-pick b3ee1b2f f735e5af 94a1773d 2d323fad 5af17dad a771f1dc fae02d3d 485e02c3 b10e1406 a616ff0b 7a25424e 2295351b`

  Expected: 保留优雅退出、D1、telemetry、签名、问卷、魔法少女与 Game Card 迁移，不包含四个 Phase 0 audit 提交。

### Task 3: 拆分 boundary 回归并修复 Docker manifest

**Files:**
- Modify: `tests/check-workspace-boundaries.test.ts`
- Modify: `Dockerfile.hono`
- Test: `tests/check-workspace-boundaries.test.ts`
- Test: `tests/workspace-structure.test.ts`

- [ ] **Step 1: 从 `71971a40` 只恢复当前即可成立的 boundary fixture 覆盖**

  将 legacy `pages/components/types` 直接导入矩阵及 workspace-package bridge 间接导入用例加入 `tests/check-workspace-boundaries.test.ts`；不恢复 `apps/api`、新 Docker 和新 deploy workflow 的未来态断言。

- [ ] **Step 2: 验证现有 Docker manifest 测试按预期失败**

  Run: `pnpm exec vitest run tests/workspace-structure.test.ts --reporter=dot`

  Expected: 仅报告 `packages/hosted-runtime/package.json` 未在依赖安装前复制的失败。

- [ ] **Step 3: 添加最小 Docker 修复**

  在 `Dockerfile.hono` 的 workspace manifest COPY 段中加入：

  ```dockerfile
  COPY packages/hosted-runtime/package.json ./packages/hosted-runtime/package.json
  ```

- [ ] **Step 4: 验证 GREEN**

  Run: `pnpm exec vitest run tests/workspace-structure.test.ts tests/check-workspace-boundaries.test.ts --reporter=dot`

  Expected: 两个文件全部通过。

### Task 4: 验证并固定绿色 platform 基线

**Files:**
- Create: `docs/plans/2026-08-24_121408_平台与G25C分支历史重建实施计划.md`

- [ ] **Step 1: 运行完整根测试和边界检查**

  Run: `pnpm test -- --reporter=dot`

  Run: `pnpm run check:workspace:boundaries`

  Expected: 无失败。

- [ ] **Step 2: 运行受影响 package 验证**

  Run: `pnpm --filter @mahoshojo/domain test && pnpm --filter @mahoshojo/domain lint && pnpm --filter @mahoshojo/domain build`

  Run: `pnpm --filter @mahoshojo/hosted-runtime test && pnpm --filter @mahoshojo/hosted-runtime lint && pnpm --filter @mahoshojo/hosted-runtime build`

  Run: `pnpm --filter @mahoshojo/hosted-api test && pnpm --filter @mahoshojo/hosted-api lint && pnpm --filter @mahoshojo/hosted-api build`

  Expected: 所有命令退出码为 0。

- [ ] **Step 3: 检查历史与工作树**

  Run: `git diff --check 22c36730..HEAD && git status --short --branch`

  Expected: diff check 无输出，工作树干净。

### Task 5: 重挂 G25C 分支并恢复启用中的 RED

**Files:**
- Modify: `tests/hono-deploy-workflow.test.ts`
- Modify: `tests/workspace-structure.test.ts`
- Create: `tests/hono-deploy-release-contract.test.ts`

- [ ] **Step 1: 让 G25C 工作树基于新的 platform tip**

  在 `.worktrees/g25c-apps-api` 中将 `refactor/g25c-apps-api` 重建为新的 `refactor/platform-rearchitecture` 后代；原 tip 已由 backup 分支固定。

- [ ] **Step 2: 追加 apps/api ownership RED**

  只恢复 `71971a40` 中 `tests/workspace-structure.test.ts` 与 `tests/hono-deploy-workflow.test.ts` 的 future-state 断言，提交为 `test(api): 建立 apps-api ownership RED`。

- [ ] **Step 3: 追加 release tuple RED**

  恢复 `2ee32644`，提交为 `test(deploy): 建立原子 release tuple RED`。

- [ ] **Step 4: 验证 RED 归因**

  Run: `pnpm exec vitest run tests/workspace-structure.test.ts tests/hono-deploy-workflow.test.ts tests/hono-deploy-release-contract.test.ts --reporter=dot`

  Expected: 只因 `apps/api` owner、apps/api Docker/workflow 与 content-addressed release tuple 尚未实现而失败；不得再出现 legacy `Dockerfile.hono` 缺少 hosted-runtime manifest 的失败。

### Task 6: 更新正式分支引用并复审

**Files:**
- No additional file changes.

- [ ] **Step 1: 将 `refactor/platform-rearchitecture` 指向验证通过的绿色重建 tip**

  保留 backup 分支，不 push。

- [ ] **Step 2: 复核提交拓扑与 Phase 0 清理结果**

  Run: `git log --oneline --decorate --graph --all --simplify-by-decoration`

  Run: `git ls-tree -r --name-only refactor/platform-rearchitecture | rg 'phase0|pvp90|Phase0追溯式退出审计'`

  Expected: platform tip 不包含 Phase 0 门禁资产；G25C 分支只比 platform 多两个 RED 提交。

- [ ] **Step 3: 只读代码审查**

  检查提交顺序、测试归属、文档当前事实以及两个分支的工作树状态；发现问题后回到对应任务修正并重新验证。
