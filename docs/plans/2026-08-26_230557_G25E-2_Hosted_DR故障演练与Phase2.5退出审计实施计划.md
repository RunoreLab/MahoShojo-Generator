# G25E-2 Hosted DR 故障演练与 Phase 2.5 退出审计实施计划

状态：`in-progress`（2026-08-26）

> **For agentic workers:** 本计划在当前 workspace 执行；不创建 worktree。每个 checkpoint 先按 TDD 写 RED test，再实现、验证、审查并形成可回滚原子提交。

**Goal:** 完成 G25E-2 Hosted DR 完整 fault matrix 与 Phase 2.5 总退出审计，保留生产演练为明确的 `DEFERRED`，不伪造外部控制面状态。

**Architecture:** 复用 `@mahoshojo/hosted-api` 的 shared DR selector/readiness/application contract、`@mahoshojo/hosted-runtime` provider/runtime、`apps/api` Hono/Redis/D1 seam、`apps/web` Next DR guard 和现有 Arena resumable fixture；新增纯 version gate、机器可读 drill matrix、fault evidence test/validator 及最终审计日志。root 只增加验证 orchestration，不成为 runtime owner。

**Tech Stack:** TypeScript、Vitest、Node `.mjs` validator、Hono、Redis 7 local fixture、Wrangler/local D1 已有工具、pnpm workspace。

## Atomic checkpoints

### Checkpoint 1：冻结 drill matrix 与 version gate

**Files:**

- Create: `config/hosted-dr-drills.json`
- Create: `packages/hosted-api/tests/hosted-dr-version-gate.test.ts`
- Modify: `packages/hosted-api/src/hosted-dr.ts`
- Modify: `packages/hosted-api/package.json`
- Modify: `scripts/check-hosted-dr-contract.mjs`
- Modify: `package.json`
- Test: `tests/hosted-dr-contract.test.ts`

- [ ] 写 RED tests：允许的 expand/rollout skew、未知/跨 family/过大偏差和 cleanup 阻断；矩阵缺 case、错误 status、伪 production evidence 必须失败。
- [ ] 实现最小纯 version gate 和 drill manifest validator；不做网络 I/O，不改变已有 request selector/replay 语义。
- [ ] 将 `verify:hosted-dr` 或等价 targeted command 纳入 root 验证链。
- [ ] 运行 package targeted test、root contract test、validator、lint/build 和 `git diff --check`。
- [ ] 原子提交：`feat(platform): 冻结 G25E-2 演练契约`。

### Checkpoint 2：完成 control-plane、Redis、Gateway、D1、secret fault evidence

**Files:**

- Create/modify: `apps/api/tests/hosted-dr-fault-matrix.test.ts`
- Create/modify: `apps/web/tests/hosted-dr-fault-matrix.test.ts`
- Modify: `packages/hosted-api/tests/hosted-dr.test.ts`
- Modify: `apps/api/tests/health.test.ts` / `apps/api/tests/redis-runtime.test.ts`（仅必要回归）
- Modify: `apps/web/tests/hosted-dr-capability-guard.test.ts`（仅必要回归）
- Modify: `config/hosted-dr-capabilities.json`

- [ ] 先写 Hono unavailable、Gateway unavailable、D1 unavailable、DR secret missing、Redis unavailable/empty 与 cutback RED cases。
- [ ] 用真实 Hono application/adapter、provider/session、Redis runtime seam 或隔离 Redis fixture 验证固定状态、authority、调用次数和 no-replay；mock 只用于不可达边界的确定性注入。
- [ ] 验证 Hono/Next readiness public wire parity、stable-origin-only client projection、production guard 在 handler 前 fail closed、Redis 空态不改变 D1/DO/R2 authority。
- [ ] 仅把实际覆盖的 capability 标为 `verified`，未覆盖的 capability 保持 `pending-g25e-2` 并在矩阵 scope 明示。
- [ ] targeted package/app tests、lint/build、`verify:hosted-dr` 通过后原子提交：`test(platform): 覆盖 Hosted DR 基础故障矩阵`。

### Checkpoint 3：闭合 mid-flight generation/disconnect 与 replay/data evidence

**Files:**

- Modify: `apps/web/tests/arena-generation-fault-injection.test.ts`
- Modify: `packages/hosted-api/tests/arena-generation-service.test.ts`（如需最小 contract fixture）
- Modify: `config/hosted-dr-drills.json`
- Modify: `docs/specs/2026-08-26_230557_G25E-2_Hosted_DR故障演练与Phase2.5退出审计设计.md`

- [ ] 先新增 accepted/mid-flight disconnect 与 primary recovery RED assertions。
- [ ] 证明稳定 `generationRequestId`/`generationId`、Provider start count ≤ 1、无第二 POST/settlement、Redis replay/terminal 与 D1/R2 finalization 语义保持既有约束。
- [ ] 覆盖 Redis replay empty/unavailable 与 D1/R2 authority fail-closed；不把 local cache 或 Redis 结果写成业务事实。
- [ ] 运行 Arena fault test、相关 service/runtime tests、lint/build；原子提交：`test(platform): 证明 Hosted generation 中断不重放`。

### Checkpoint 4：Phase 2.5 总退出审计与文档收口

**Files:**

- Create: `docs/logs/2026-08-26_230557_平台重整G25E-2_Hosted_DR故障演练与Phase2.5总退出审计.md`
- Modify: `docs/topics/2026-08-22_022000_平台重整与本地优先架构.md`
- Modify: `docs/plans/2026-08-23_235300_平台重整Goal纵切实施计划.md`
- Modify: `docs/plans/2026-08-22_022700_平台重整分阶段实施计划.md`（仅同步当前事实）
- Modify: `apps/README.md` / `packages/README.md`（仅必要职责/状态校正）

- [ ] 逐项审计 Phase 2.5 exit gate、ACCEPT-012–017、apps/root ownership、authority/security/compatibility/replay/data、Legacy/Better Auth、Arena v1 wire 与资源约束。
- [ ] 运行 Builder self-review：`git diff`、validator、route/boundary、targeted tests、workspace test/lint/build、Next/OpenNext build、built Hono + local Redis/D1 runtime verifier（可执行部分）。
- [ ] 将生产控制面/真实生产 fault drill 写成 `DEFERRED` + runbook/授权前置条件，不执行生产动作。
- [ ] 原子提交：`docs(platform): 完成 G25E-2 Phase 2.5 退出审计`。

### Checkpoint 5：独立审查、整改与最终验证

- [ ] 在最终 diff 上并行启动四个独立 review context：architecture/dependency、security/authority、compatibility/replay/data、test adequacy。
- [ ] 逐项分类 Critical/Important/Minor；Critical/Important 必须修复并重跑受影响验证，Minor 修复或写清不阻塞理由。
- [ ] 最终运行完整 `pnpm run ci:verify`、`pnpm run build:cf`、`pnpm run build:server`、`pnpm run verify:hosted-dr`、必要的 local Redis/D1 fault command、`git diff --check`。
- [ ] 更新最终日志的实际命令/结果、review closure、状态矩阵、rollback、影响、remaining Phase 2.5 与下一 Goal 重估。
- [ ] 只有 stopping condition 全部满足时将 Goal 标记完成；否则保留准确的 `DEFERRED`/`BLOCKED`，不提前交付。

## 验证命令基线

```bash
pnpm run check:hosted-dr
pnpm run verify:hosted-dr
pnpm --filter @mahoshojo/hosted-api test
pnpm --filter @mahoshojo/api test
pnpm --filter @mahoshojo/web test
pnpm run check:workspace:boundaries
pnpm run ci:verify
pnpm run build:server
pnpm run build:cf
git diff --check
```

## Rollback

每个 checkpoint 是独立提交；按提交粒度 revert 即可。回滚不得触碰现有 D1 schema、Redis 数据、secret、Access、
生产流量或 release。若只回滚演练契约，保留 G25E-1 `g25e1-v1` runtime contract 不变。
