# Arena 多人生产激活与回滚实施计划

**Goal:** 在不改变 Hono + Redis single-writer、Room v1 排除 Cloudflare DR 与默认关闭边界的前提下，按
reader-first 顺序安全启用 production Arena 多人，并保留可验证的三层回滚路径。

**Architecture:** `api.mahoshojo.colanns.me` 作为 production Room logical HTTPS/WSS origin，只路由到当前唯一的
production Hono 实例。Hono release tuple 决定 writer 能力，`.env.hono` request flag 决定运行时是否公开，Web
build flag 最后决定客户端曝光；任一层未就绪都保持 fail closed。

**Tech Stack:** GitHub Actions、Hono/Node、Redis、Caddy、Cloudflare Workers、Vitest、curl。

---

状态：`PREPARED / BLOCKED_ON_EXTERNAL_PROVISIONING`

本计划只准备仓库与执行顺序，不授权 production DNS、Caddy、secret、`.env.hono`、Redis、Cloudflare variables、
Hono/Worker deploy 或业务 canary 写入。

## 1. 当前生产事实（2026-08-30 只读核实）

- `/opt/mahoshojo-hono` 已使用 `release-tuple-v2`，manifest 摘要通过，`.env.hono` 为 `root:root 0600`，没有悬挂事务；
- production Hono/Redis 均 healthy，Hono 只绑定 `127.0.0.1:8080`，Redis 不暴露宿主端口；
- `https://homura.colanns.me/api/health/ready` 为 200，Redis 与 D1 Gateway 均为 required/ready；
- production current tuple 经 manifest 与 schema 验证，`checkpointContract` 已是
  `arena-room-authority-v2-generation-payload-digest-v1`，且 `writerActivation=disabled`；它已满足 compatible reader
  baseline，无需仅因与 Preview 的二进制摘要不同就重复发布；
- `api.mahoshojo.colanns.me` 没有 DNS，Caddy 没有对应 site/TLS，Room manifest 正确保持 `not-provisioned`；
- production `.env.hono` 已有精确 Web origin，但 request 未启用，logical origin、reader attestation 与 go/no-go
  尚未配置；
- GitHub/Cloudflare production Environment 的 Arena public variables、finalization secret 一致性与 required reviewer
  仍需由平台管理员核实，不从服务器状态猜测。

## 2. 角色与 go/no-go

- **执行人：** 用户指定的平台管理员，负责 DNS/Caddy、GitHub Environment、`.env.hono` 与 workflow dispatch；
- **观察与回滚人：** 执行人在每个阶段同时承担观察和回滚，不在无人观察时开启 Web flag；
- **变更授权：** 每次 production mutation 前由用户单独确认；“准备完成”不等于 deploy 授权；
- **可接受降级：** 任一 Room 门禁失败时，多人入口保持关闭，现有单人 Arena 与其他 Hosted API 必须继续可用。

## 3. Task 1：准备显式 production 发布合同

**Files:**

- Modify: `.github/workflows/hono-deploy.yml`
- Modify: `.github/workflows/cloudflare-deploy.yml`
- Modify: `tests/hono-deploy-workflow.test.ts`
- Modify: `apps/api/env.example`

- [x] **Step 1：用失败测试锁定 production 发布行为**

  Hono 的普通 push 只允许 build/verify，deploy 只允许手工 dispatch，并要求显式选择
  `arena_room_writer_activation=disabled|enabled`。Cloudflare backend probe 必须同时校验 HTTP 401、错误码、exact ACAO
  与 WSS 401；Web deploy 后只执行 `/arena` 和 Hono readiness 两个 smoke。

- [x] **Step 2：运行 RED**

  ```bash
  pnpm exec vitest run --config vitest.config.ts tests/hono-deploy-workflow.test.ts
  ```

  Expected：workflow 尚无手工 writer tuple、production ACAO 与 post-deploy smoke 时 FAIL。

- [x] **Step 3：最小实现**

  production Hono build 使用 `scripts/prepare-arena-room-release-gate.mjs` 生成选择的 tuple；普通 push 默认只生成
  disabled artifact，不触发 deploy。Cloudflare 不增加版本状态机，只补 exact ACAO 与两个公开 smoke。

- [x] **Step 4：运行 GREEN**

  ```bash
  pnpm exec vitest run --config vitest.config.ts tests/hono-deploy-workflow.test.ts
  ```

  Expected：18/18 PASS。

## 4. Task 2：Provision production logical Room origin

**外部状态：** DNS、Caddy 与 TLS；仓库在公网证据通过前不得提前写 `provisioned`。

- [ ] **Step 1：配置唯一入口**

  平台管理员把 `api.mahoshojo.colanns.me` 指向与 `homura.colanns.me` 相同的 production Hono 主机；Caddy 新增独立
  site，反向代理到 `127.0.0.1:8080`。不得增加 response buffer，不得引入第二个 room-capable replica。

- [ ] **Step 2：验证 DNS/TLS/readiness**

  ```bash
  curl --fail --show-error https://api.mahoshojo.colanns.me/api/health/ready
  ```

  Expected：TLS 校验通过、HTTP 200、`X-Backend-Runtime: hono-node`；logical origin 与 physical origin 指向同一
  room-capable Hono authority。

- [ ] **Step 3：验证 disabled baseline**

  以 `Origin: https://mahoshojo.colanns.me` 请求 Room HTTP/WSS。writer/request 仍关闭时不得返回 2xx/101，也不得影响
  retained Hosted routes 与 Hono readiness。

- [ ] **Step 4：同步 manifest**

  只有上述证据真实通过后，才把 `config/arena-room-origins.json` 的 production `provisioning` 改为 `provisioned`，并让
  `scripts/generate-arena-room-client-config.mjs` 接受已 provision 的 production 状态，重新生成
  `apps/web/config/arena-room-origins.generated.ts`。

  ```bash
  pnpm generate:arena-room-client
  pnpm exec vitest run --config vitest.config.ts tests/arena-room-origin-contract.test.ts
  ```

  Expected：projection 无 drift，production/preview logical origin 均精确，Room 仍为 Hono/Redis single writer 且
  `cloudflareDr=excluded`。

## 5. Task 3：确认 compatible reader（writer disabled）

- [ ] **Step 1：保持 runtime request 关闭**

  对 `/opt/mahoshojo-hono/.env.hono` 做权限不变的原子更新，保持：

  ```dotenv
  ARENA_MULTIPLAYER_ENABLED=false
  ARENA_ROOM_ALLOWED_ORIGINS=https://mahoshojo.colanns.me
  ARENA_ROOM_LOGICAL_ORIGIN=https://api.mahoshojo.colanns.me
  ARENA_ROOM_READER_ROLLOUT_CONTRACT=arena-room-authority-v2-generation-payload-digest-v1
  ```

  `ARENA_ROOM_PRODUCTION_GO_NO_GO` 在最终授权前保持未设置；不输出或改写其他 secret；完成后 owner/mode 仍必须为
  `root:root 0600`。

- [x] **Step 2：核对 current reader contract**

  production current gate 已经受 release manifest 保护，精确 contract 与 writer-disabled 状态已通过只读核对。
  若执行时 current gate 变为缺失、malformed 或 contract 不匹配，才需对待发布 SHA 手工 dispatch
  `arena_room_writer_activation=disabled`；普通 push 不执行 production Hono deploy。

- [ ] **Step 3：验证 reader baseline**

  确认 current tuple manifest 通过、gate 为 disabled、容器 healthy、public/local readiness 为 200；若触发了
  条件性 reader 重发，再额外核对新 current release ID 与 workflow artifact 一致。不得把这一阶段描述为多人已启用。
  在任何 writer 变更前立即记录此时的 immutable release ID，作为 writer-disabled reader 回滚基线。

## 6. Task 4：激活 Hono writer，Web 继续关闭

- [ ] **Step 1：取得 go/no-go 并写入 attestation**

  用户明确授权 production writer 变更窗口后，才把 `ARENA_ROOM_PRODUCTION_GO_NO_GO=approved` 原子写入
  `.env.hono`；若授权撤回或窗口结束，保持 request false 并停止。

- [ ] **Step 2：发布 writer-enabled tuple**

  保持 `ARENA_MULTIPLAYER_ENABLED=false`，对同一分支 SHA 手工 dispatch `Deploy Hono`，选择
  `arena_room_writer_activation=enabled`。部署脚本必须验证 previous compatible reader 与 rollback gate；执行记录实际
  candidate release ID 与部署输出的 `ROLLBACK_BASELINE_RELEASE_ID`，后者应指向已记录的 reader 基线。

- [ ] **Step 3：确认 writer capability 未公开**

  release gate 应为 enabled，但 request 仍为 false；Room HTTP/WSS 不得返回 2xx/101，其他 Hosted routes/readiness 不退化。

- [ ] **Step 4：开启 request 并重跑同一 enabled tuple**

  原子把 `ARENA_MULTIPLAYER_ENABLED` 改为 `true`，保持文件权限与其他值不变，然后对同一 SHA 再次 dispatch enabled。
  发布交易必须经 logical origin 验证后才能 promote；再次记录实际 candidate 与 rollback baseline，
  不根据“同一 SHA”猜测 release ID。

- [ ] **Step 5：执行无写入 backend canary**

  对 `https://api.mahoshojo.colanns.me` 验证：

  - HTTP `/api/arena/rooms/v1` 返回 401、`ROOM_AUTHENTICATION_REQUIRED` 与 exact ACAO；
  - WSS `/api/arena/rooms/v1/ws` 使用合法 RFC 6455 key 和 `mahoshojo.arena-room.v1`，无 ticket 返回 401；
  - Hono readiness 继续为 200。

## 7. Task 5：最后公开 Web

- [ ] **Step 0：记录 Worker 回滚基线**

  在首次曝光前记录 Cloudflare current Worker version ID。Hono 回滚基线使用 Task 3 在 writer 变更前
  记录的 immutable reader release ID。精确查询和回滚命令复用通用 Hono 部署指南。

- [ ] **Step 1：核实 Cloudflare production Environment**

  平台管理员核实 finalization secret 已存在且与 Hono 同一独立 secret，并设置以下公开 variables：

  ```text
  NEXT_PUBLIC_ARENA_MULTIPLAYER_ENABLED=true
  NEXT_PUBLIC_ARENA_ROOM_ORIGIN=https://api.mahoshojo.colanns.me
  NEXT_PUBLIC_ARENA_ROOM_WRITER_ACTIVATION=enabled
  NEXT_PUBLIC_ARENA_ROOM_READER_CONTRACT=arena-room-authority-v2-generation-payload-digest-v1
  NEXT_PUBLIC_ARENA_ROOM_GO_NO_GO=approved
  ```

- [ ] **Step 2：手工 dispatch Cloudflare production**

  运行 `Deploy to Cloudflare Workers`。公开变量是曝光授权，workflow 会在构建前执行 exact
  HTTP/WSS/CORS backend probe；激活后的普通 production push 沿用同一探针，不再重复要求一次性手工确认。

- [ ] **Step 3：公开可达性 smoke**

  workflow 必须在 deploy 后验证 `/arena` 可达与 Hono readiness 200；随后用无登录浏览器确认
  `[data-arena-multiplayer="v1"]` 可见。workflow smoke 只证明公开可达性，不代替 UI marker 验收。

## 8. 观察、停止与回滚

公开后至少观察一轮真实操作，关注 Hono CPU/RSS/event-loop、Redis latency/memory/eviction、Room actor queue、snapshot
bytes/latency、socket close、reconnect/resync 与 publisher backlog。没有生产 SLA 时只记录基线，不伪造阈值。

命中以下任一项立即停止：readiness 非 200、HTTP/WSS 认证/CORS contract 不符、Redis/D1 required dependency degraded、
出现第二个 room-capable writer、rollback target 不兼容、terminal/finalization 重复或用户出现整包延迟。

先做曝光收缩，再按需做代码回滚：

1. 把 Web public flag 改为 false 并发布隐藏 UI；
2. 原子把 Hono `ARENA_MULTIPLAYER_ENABLED` 改为 false，并重跑当前 enabled tuple确认 backend 不再公开；
3. 手工 dispatch Hono `arena_room_writer_activation=disabled`；部署脚本会拒绝 request 仍开启时回退；
4. 若问题来自新代码而非开关，按通用部署指南把 Worker 回退到记录的 version，并把 Hono 回退到
   记录的 immutable writer-disabled reader release；
5. 保留 logical origin DNS/Caddy 供诊断，不执行 Redis flush，不删除 D1 审计事实；
6. 验证单人 Arena、retained Hosted routes、Hono readiness 与 production Web 均恢复正常。

## 9. 完成条件

- production logical origin DNS/TLS/Caddy/WSS 已有真实证据，manifest 与 generated projection 同步；
- 已验证 writer-disabled compatible reader，再以 writer-enabled/request-off 与 request-on 两阶段激活；
- backend canary、Web exposure、观察与三层 rollback 都有 release ID/workflow run 证据；
- production canary 写入均有单独授权；secret 值未进入仓库、日志或交付说明；
- 最终由用户给出 production go/no-go，不能由本计划或 Preview 成功自动推断。
