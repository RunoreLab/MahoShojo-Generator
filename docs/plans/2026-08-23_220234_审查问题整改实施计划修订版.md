# 审查问题整改修订版 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 按已批准设计修复 D1 透明重放与未知提交结果传播、契约不变量、跨 runtime 可加载性门禁、Hono Docker/CI 回归路径及两处文档事实错误。

**Architecture:** D1 transport 默认单次发送，未分类/mutation 遇到可能已到达权威层的失败时传播稳定 indeterminate error，只有 safe-read 显式 opt in retry 且每个 attempt 重新签名。Provider Header、Local Card provenance 各自在 schema 边界拒绝矛盾输入；contracts 通过 AST runtime/browser-global gate、全 exports bundle 和 Node 实际 import 三重防退化；Docker 和两条部署 workflow 复用同一根/workspace 验证入口。所有行为修改先写失败测试，再做最小实现，并按数据库、Provider、local-library、contracts portability、CI/容器、事实文档六个主题提交。

**Tech Stack:** TypeScript、Vitest 4、Zod 4、pnpm workspace、esbuild、GitHub Actions、Docker/Compose、Next.js/OpenNext、Hono Node。

**Approved design:** `docs/specs/2026-08-23_214103_审查问题整改设计.md`

---

## 文件结构与责任

- `lib/database/core.ts`：D1 retry option、默认 no-replay、indeterminate error 传播与逐 attempt 鉴权。
- `server/health.ts`：明确的 safe-read opt-in consumer。
- `tests/database-core-query.test.ts`：mutation/batch no-replay、legacy helper unknown outcome 传播、safe-read retry 与逐 attempt 签名行为。
- `packages/contracts/src/provider-profile.ts`：HTTP header canonical collision。
- `packages/contracts/tests/provider-profile.test.ts`：Header 大小写冲突矩阵。
- `packages/local-library/src/record.ts`：provenance discriminated union。
- `packages/local-library/tests/record.test.ts`：三态组合与 legacy 签名兼容。
- `scripts/check-workspace-boundaries.mjs`：contracts runtime import 与窄 browser-only global gate。
- `tests/check-workspace-boundaries.test.ts`：contracts runtime import、裸 global 与 `globalThis` fixture。
- `packages/contracts/tests/bundle.test.ts`：自动枚举全部 public exports 的三 target bundle 与 Node 实际 import。
- `docs/architecture/2026-08-22_022500_平台重整目标架构.md`：provenance 稳定模型。
- `docs/specs/2026-08-22_022600_平台重整实施规格.md`：Header 与 provenance 规范性要求。
- `Dockerfile.hono`：install 前完整 workspace manifests。
- `package.json`：统一 `ci:verify` 入口。
- `.github/workflows/hono-deploy.yml`、`.github/workflows/cloudflare-deploy.yml`：统一验证调用及 Docker smoke。
- `tests/workspace-structure.test.ts`、`tests/hono-deploy-workflow.test.ts`：Docker/CI/workflow 回归门禁。
- `server/README.md`、`docs/logs/2026-08-23_194020_平台重整Phase2本地库契约实施日志.md`：事实校正。

## Task 1：禁止 D1 mutation/batch 透明重放并传播未知结果

**Files:**

- Modify: `tests/database-core-query.test.ts`
- Modify: `lib/database/core.ts`
- Modify: `server/health.ts`

- [ ] **Step 1：写 mutation、batch 与 safe-read 的失败测试**

把 `vi`、`queryD1BatchPayload`、`createWithCustomId`、`updateById` 和 `saveToD1` 加入测试 import。扩展 `EnvSnapshot`/read/restore 夹具：

```ts
type EnvSnapshot = {
  CLOUDFLARE_API_TOKEN?: string;
  CLOUDFLARE_ACCOUNT_ID?: string;
  D1_DATABASE_ID?: string;
  D1_GATEWAY_URL?: string;
  D1_GATEWAY_HMAC_SECRET?: string;
};

const readEnvSnapshot = (): EnvSnapshot => ({
  CLOUDFLARE_API_TOKEN: process.env.CLOUDFLARE_API_TOKEN,
  CLOUDFLARE_ACCOUNT_ID: process.env.CLOUDFLARE_ACCOUNT_ID,
  D1_DATABASE_ID: process.env.D1_DATABASE_ID,
  D1_GATEWAY_URL: process.env.D1_GATEWAY_URL,
  D1_GATEWAY_HMAC_SECRET: process.env.D1_GATEWAY_HMAC_SECRET,
});

const restoreEnvSnapshot = (snapshot: EnvSnapshot): void => {
  for (const name of Object.keys(snapshot) as Array<keyof EnvSnapshot>) {
    const value = snapshot[name];
    if (value === undefined) delete process.env[name];
    else process.env[name] = value;
  }
};

const setMinimalEnv = (): void => {
  delete process.env.D1_GATEWAY_URL;
  delete process.env.D1_GATEWAY_HMAC_SECRET;
  process.env.CLOUDFLARE_API_TOKEN = 'token_x';
  process.env.CLOUDFLARE_ACCOUNT_ID = 'account_x';
  process.env.D1_DATABASE_ID = 'db_x';
};

const setGatewayEnv = (): void => {
  process.env.D1_GATEWAY_URL = 'https://gateway.example.test';
  process.env.D1_GATEWAY_HMAC_SECRET = 'gateway-test-secret';
};
```

然后在 `database/core queryD1Payload` suite 中加入以下测试。每个测试沿用现有 `readEnvSnapshot`、`setMinimalEnv`、`withSilencedConsoleError` 与 `try/finally` 清理模式：

```ts
test('mutation 收到可重试响应时也只发送一次请求', async () => {
  const envSnapshot = readEnvSnapshot();
  const originalFetch = globalThis.fetch;

  try {
    setMinimalEnv();
    let calls = 0;
    globalThis.fetch = (async () => {
      calls += 1;
      return new Response('response lost after possible commit', {
        status: 503,
        statusText: 'Service Unavailable',
        headers: { 'Retry-After': '0' },
      });
    }) as typeof globalThis.fetch;

    let error: unknown;
    await withSilencedConsoleError(async () => {
      error = await queryD1Payload('UPDATE counters SET value = value + 1').catch(
        (caught: unknown) => caught,
      );
    });
    expect(error).toMatchObject({
      name: 'D1IndeterminateOutcomeError',
      code: 'D1_INDETERMINATE_OUTCOME',
      status: 503,
    });
    expect(calls).toBe(1);
  } finally {
    globalThis.fetch = originalFetch;
    restoreEnvSnapshot(envSnapshot);
  }
});

test('mutation 收到可重试 fetch error 时也只发送一次请求', async () => {
  const envSnapshot = readEnvSnapshot();
  const originalFetch = globalThis.fetch;
  vi.useFakeTimers();

  try {
    setMinimalEnv();
    let calls = 0;
    globalThis.fetch = (async () => {
      calls += 1;
      throw new TypeError('fetch failed');
    }) as typeof globalThis.fetch;

    let error: unknown;
    const assertion = withSilencedConsoleError(async () => {
      error = await queryD1Payload('DELETE FROM sessions WHERE expires_at < ?').catch(
        (caught: unknown) => caught,
      );
    });
    await vi.runAllTimersAsync();
    await assertion;
    expect(error).toMatchObject({
      name: 'D1IndeterminateOutcomeError',
      code: 'D1_INDETERMINATE_OUTCOME',
    });
    expect(calls).toBe(1);
  } finally {
    vi.useRealTimers();
    globalThis.fetch = originalFetch;
    restoreEnvSnapshot(envSnapshot);
  }
});

test('包含 mutation 的 batch 收到可重试响应时只发送一次请求', async () => {
  const envSnapshot = readEnvSnapshot();
  const originalFetch = globalThis.fetch;

  try {
    setMinimalEnv();
    let calls = 0;
    globalThis.fetch = (async () => {
      calls += 1;
      return new Response('unknown batch result', {
        status: 503,
        statusText: 'Service Unavailable',
        headers: { 'Retry-After': '0' },
      });
    }) as typeof globalThis.fetch;

    let error: unknown;
    await withSilencedConsoleError(async () => {
      error = await queryD1BatchPayload([
        { sql: 'SELECT 1' },
        { sql: 'INSERT INTO audit_log (message) VALUES (?)', params: ['once'] },
      ]).catch((caught: unknown) => caught);
    });
    expect(error).toMatchObject({
      name: 'D1IndeterminateOutcomeError',
      code: 'D1_INDETERMINATE_OUTCOME',
      status: 503,
    });
    expect(calls).toBe(1);
  } finally {
    globalThis.fetch = originalFetch;
    restoreEnvSnapshot(envSnapshot);
  }
});

test('显式 safe-read 在可重试响应后继续重试', async () => {
  const envSnapshot = readEnvSnapshot();
  const originalFetch = globalThis.fetch;

  try {
    setMinimalEnv();
    let calls = 0;
    globalThis.fetch = (async () => {
      calls += 1;
      if (calls === 1) {
        return new Response('transient', {
          status: 503,
          statusText: 'Service Unavailable',
          headers: { 'Retry-After': '0' },
        });
      }
      return new Response(JSON.stringify({ success: true, result: [{ ok: 1 }] }), {
        status: 200,
        headers: { 'Content-Type': 'application/json' },
      });
    }) as typeof globalThis.fetch;

    await expect(queryD1Payload('SELECT 1 AS ok', [], { retry: 'safe-read' })).resolves.toEqual({
      success: true,
      result: [{ ok: 1 }],
    });
    expect(calls).toBe(2);
  } finally {
    globalThis.fetch = originalFetch;
    restoreEnvSnapshot(envSnapshot);
  }
});

test.each([
  ['createWithCustomId', () => createWithCustomId('{}', 'shojo')],
  ['updateById', () => updateById('record-1', '{}', 'shojo')],
  ['saveToD1', () => saveToD1({ id: 'record-1' })],
])('%s 不把未知提交结果吞成 null/false', async (_name, execute) => {
  const envSnapshot = readEnvSnapshot();
  const originalFetch = globalThis.fetch;

  try {
    setMinimalEnv();
    let calls = 0;
    globalThis.fetch = (async () => {
      calls += 1;
      return new Response('unknown write result', {
        status: 503,
        statusText: 'Service Unavailable',
        headers: { 'Retry-After': '0' },
      });
    }) as typeof globalThis.fetch;

    let error: unknown;
    await withSilencedConsoleError(async () => {
      error = await execute().catch((caught: unknown) => caught);
    });
    expect(error).toMatchObject({
      name: 'D1IndeterminateOutcomeError',
      code: 'D1_INDETERMINATE_OUTCOME',
      status: 503,
    });
    expect(calls).toBe(1);
  } finally {
    globalThis.fetch = originalFetch;
    restoreEnvSnapshot(envSnapshot);
  }
});

test('safe-read 的每个 Gateway attempt 使用新 timestamp、nonce 和 signature', async () => {
  const envSnapshot = readEnvSnapshot();
  const originalFetch = globalThis.fetch;
  const nowSpy = vi.spyOn(Date, 'now')
    .mockReturnValueOnce(1_800_000_000_000)
    .mockReturnValueOnce(1_800_000_000_001);

  try {
    setGatewayEnv();
    const authHeaders: Array<Record<string, string>> = [];
    globalThis.fetch = (async (_input, init) => {
      const headers = new Headers(init?.headers);
      authHeaders.push({
        timestamp: headers.get('X-Mahoshojo-Timestamp') ?? '',
        nonce: headers.get('X-Mahoshojo-Nonce') ?? '',
        signature: headers.get('X-Mahoshojo-Signature') ?? '',
      });
      if (authHeaders.length === 1) {
        return new Response('transient', {
          status: 503,
          statusText: 'Service Unavailable',
          headers: { 'Retry-After': '0' },
        });
      }
      return new Response(JSON.stringify({ success: true, result: [{ ok: 1 }] }), {
        status: 200,
        headers: { 'Content-Type': 'application/json' },
      });
    }) as typeof globalThis.fetch;

    await expect(queryD1Payload('SELECT 1 AS ok', [], { retry: 'safe-read' })).resolves.toMatchObject({
      success: true,
    });
    expect(authHeaders).toHaveLength(2);
    expect(authHeaders[0].timestamp).not.toBe(authHeaders[1].timestamp);
    expect(authHeaders[0].nonce).not.toBe(authHeaders[1].nonce);
    expect(authHeaders[0].signature).not.toBe(authHeaders[1].signature);
    expect(authHeaders.every(({ timestamp, nonce, signature }) =>
      timestamp !== '' && nonce !== '' && signature !== '',
    )).toBe(true);
  } finally {
    nowSpy.mockRestore();
    globalThis.fetch = originalFetch;
    restoreEnvSnapshot(envSnapshot);
  }
});
```

- [ ] **Step 2：运行 RED，确认失败原因是现有统一 5 次重试**

Run:

```bash
pnpm exec vitest run tests/database-core-query.test.ts --reporter=verbose
```

Expected: retryable response mutation、fetch error mutation 与 batch 现在会尝试 5 次且抛出普通 error；三个 legacy helper 会解析为 `null`/`false`；Gateway safe-read 的两个 attempt 复用同一组鉴权 header。

- [ ] **Step 3：实现显式 retry option、稳定 indeterminate error 与逐 attempt request factory**

在 `lib/database/core.ts` 增加公共 option：

```ts
export type D1QueryRetryMode = 'none' | 'safe-read';

export type D1QueryOptions = {
  retry?: D1QueryRetryMode;
};

const retryAttempts = (options?: D1QueryOptions): number =>
  options?.retry === 'safe-read' ? 5 : 1;

export const D1_INDETERMINATE_OUTCOME_ERROR_CODE = 'D1_INDETERMINATE_OUTCOME' as const;

export class D1IndeterminateOutcomeError extends Error {
  readonly code = D1_INDETERMINATE_OUTCOME_ERROR_CODE;
  readonly status?: number;

  constructor(message: string, status?: number) {
    super(message);
    this.name = 'D1IndeterminateOutcomeError';
    this.status = status;
  }
}
```

把 `fetchWithRetry` 的第二个参数从固定 `RequestInit` 改为 async factory：

```ts
const fetchWithRetry = async (
  url: string,
  createInit: () => Promise<RequestInit>,
  options?: { attempts?: number; baseDelayMs?: number; maxDelayMs?: number },
): Promise<Response> => {
  const attempts = Math.max(1, options?.attempts ?? 3);
  const baseDelayMs = Math.max(0, options?.baseDelayMs ?? 300);
  const maxDelayMs = Math.max(0, options?.maxDelayMs ?? 10_000);

  let lastError: unknown;
  for (let i = 0; i < attempts; i++) {
    const init = await createInit();
    try {
      const response = await fetch(url, init);
      if (response.ok || i === attempts - 1 || !isRetryableStatus(response.status)) {
        return response;
      }

      const retryAfterMs = parseRetryAfterMs(response);
      const backoffMs = Math.min(maxDelayMs, baseDelayMs * Math.pow(2, i));
      const delayMs = retryAfterMs ?? backoffMs;
      try {
        await response.body?.cancel();
      } catch {
        // ignore
      }
      await sleep(jitterMs(delayMs));
    } catch (error) {
      lastError = error;
      if (!isRetryableFetchError(error) || i === attempts - 1) throw error;
      await sleep(jitterMs(Math.min(maxDelayMs, baseDelayMs * Math.pow(2, i))));
    }
  }

  throw lastError;
};
```

`createInit()` 必须在 fetch `try` 之前，保证鉴权构建失败不会被误当成已 dispatch，且 safe-read 每个 attempt 都重新构建 header。

把内部 `query` 改为接收 option，只对显式 safe-read 使用 5 次尝试：

```ts
async function query(
  body: Record<string, unknown>,
  endpoint: D1QueryEndpoint = 'query',
  options?: D1QueryOptions,
): Promise<Response> {
  const config = assertD1Config();
  const targetUrl = endpoint === 'raw' ? config.rawUrl : config.queryUrl;
  const bodyText = JSON.stringify(body);
  const safeRead = options?.retry === 'safe-read';

  try {
    const response = await fetchWithRetry(
      targetUrl,
      async () => ({
        method: 'POST',
        headers: await buildD1Headers(config, targetUrl, bodyText),
        body: bodyText,
      }),
      { attempts: retryAttempts(options), baseDelayMs: 500, maxDelayMs: 8000 },
    );

    if (!safeRead && isRetryableStatus(response.status)) {
      try {
        await response.body?.cancel();
      } catch {
        // ignore
      }
      throw new D1IndeterminateOutcomeError(
        `D1 操作结果未知：请求可能已到达权威数据库（HTTP ${response.status}）`,
        response.status,
      );
    }

    return response;
  } catch (error) {
    if (!safeRead && isRetryableFetchError(error)) {
      throw new D1IndeterminateOutcomeError(
        'D1 操作结果未知：请求已发送但未收到确定响应',
      );
    }
    throw error;
  }
}
```

三个 payload 函数和兼容别名增加可选参数。只替换各函数的 declaration 和现有 `query(...)` 调用，错误解析、日志与 JSON 返回分支逐行保持不变：

```diff
-export async function queryD1Payload(sql: string, params: unknown[] = []): Promise<unknown> {
+export async function queryD1Payload(sql: string, params: unknown[] = [], options?: D1QueryOptions): Promise<unknown> {
   try {
-    const response = await query({ sql, params }, 'query');
+    const response = await query({ sql, params }, 'query', options);

-export async function queryD1BatchPayload(batch: Array<{ sql: string; params?: unknown[] }>): Promise<unknown> {
+export async function queryD1BatchPayload(batch: Array<{ sql: string; params?: unknown[] }>, options?: D1QueryOptions): Promise<unknown> {
   try {
-    const response = await query({ batch }, 'query');
+    const response = await query({ batch }, 'query', options);

-export async function queryD1RawPayload(sql: string, params: unknown[] = []): Promise<unknown> {
+export async function queryD1RawPayload(sql: string, params: unknown[] = [], options?: D1QueryOptions): Promise<unknown> {
   try {
-    const response = await query({ sql, params }, 'raw');
+    const response = await query({ sql, params }, 'raw', options);

-export async function queryFromD1(sql: string, params: unknown[] = []): Promise<unknown> {
-  return queryD1Payload(sql, params);
+export async function queryFromD1(sql: string, params: unknown[] = [], options?: D1QueryOptions): Promise<unknown> {
+  return queryD1Payload(sql, params, options);
 }
```

不要按 SQL 前缀自动选择 retry，不要增加没有 operation record/unique/CAS 证明的 write retry 模式。

在 `createWithCustomId`、`updateById` 与 `saveToD1` 的 `catch` 块最前都加入：

```ts
if (error instanceof D1IndeterminateOutcomeError) throw error;
```

其他确定失败继续保持原有 `null`/`false` 兼容行为；不改 helper 返回类型。

- [ ] **Step 4：让 Hono readiness 健康查询显式 opt in safe-read**

将 `server/health.ts` 中调用改为：

```ts
const payload = await queryD1Payload('SELECT 1 AS ok', [], { retry: 'safe-read' });
```

通用 HTTP D1 adapter、普通 mutation 和 batch 不 opt in。

- [ ] **Step 5：运行 GREEN 与受影响 server 验证**

Run:

```bash
pnpm exec vitest run tests/database-core-query.test.ts tests/d1-http-client.test.ts tests/server/d1-gateway-client.test.ts --reporter=verbose
pnpm exec vitest run tests/server --reporter=dot
pnpm run build:server
```

Expected: 所有命令 exit 0；mutation/batch 均只调用一次并传播稳定 indeterminate error，legacy helper 不再吞该 error，safe-read 调用两次后成功且刷新 Gateway 鉴权元数据，Hono bundle 构建成功且没有新增 warning。

- [ ] **Step 6：提交数据库修复**

```bash
git add lib/database/core.ts server/health.ts tests/database-core-query.test.ts
git diff --cached --check
git commit -m "fix(database): 禁止 D1 写操作透明重放"
```

## Task 2：拒绝 Provider Header canonical 冲突

**Files:**

- Modify: `packages/contracts/tests/provider-profile.test.ts`
- Modify: `packages/contracts/src/provider-profile.ts`
- Modify: `docs/specs/2026-08-22_022600_平台重整实施规格.md`

- [ ] **Step 1：写 Provider Header collision RED**

在 provider profile tests 增加：

```ts
it('rejects case-insensitive header collisions within and across collections', () => {
  expect(DirectProviderProfileV1Schema.safeParse({
    ...validProfile,
    publicHeaders: {
      'X-Client-Name': 'first',
      'x-client-name': 'second',
    },
  }).success).toBe(false);

  expect(DirectProviderProfileV1Schema.safeParse({
    ...validProfile,
    secretHeaderRefs: {
      'X-Custom-Token': 'vault:one',
      'x-custom-token': 'vault:two',
    },
  }).success).toBe(false);

  expect(DirectProviderProfileV1Schema.safeParse({
    ...validProfile,
    publicHeaders: { 'X-Custom-Token': 'public-value' },
    secretHeaderRefs: { 'x-custom-token': 'vault:secret' },
  }).success).toBe(false);
});
```

- [ ] **Step 2：运行 Header RED**

Run:

```bash
pnpm --filter @mahoshojo/contracts exec vitest run --config vitest.config.ts tests/provider-profile.test.ts --reporter=verbose
```

Expected: 三种 collision 输入仍被当前 schema 接受，测试失败。

- [ ] **Step 3：实现 Header canonical uniqueness**

在 `provider-profile.ts` 增加辅助函数：

```ts
const addCanonicalHeaderDuplicateIssues = (
  headers: Record<string, unknown>,
  context: z.RefinementCtx,
): void => {
  const originalNames = new Map<string, string>();
  for (const headerName of Object.keys(headers)) {
    const canonicalName = headerName.toLowerCase();
    const previousName = originalNames.get(canonicalName);
    if (previousName !== undefined) {
      context.addIssue({
        code: 'custom',
        path: [headerName],
        message: `duplicates ${previousName} because HTTP header names are case-insensitive`,
      });
      continue;
    }
    originalNames.set(canonicalName, headerName);
  }
};
```

在 `SecretHeaderRefsSchema`、`PublicHeadersSchema` 各自的 `superRefine` 首先调用它。然后在 `DirectProviderProfileV1Schema.superRefine` 中加入跨集合检查：

```ts
const publicHeaderNames = new Map(
  Object.keys(profile.publicHeaders ?? {}).map((headerName) => [headerName.toLowerCase(), headerName]),
);
for (const secretHeaderName of Object.keys(profile.secretHeaderRefs ?? {})) {
  const publicHeaderName = publicHeaderNames.get(secretHeaderName.toLowerCase());
  if (publicHeaderName === undefined) continue;
  context.addIssue({
    code: 'custom',
    path: ['secretHeaderRefs', secretHeaderName],
    message: `conflicts with publicHeaders.${publicHeaderName} because HTTP header names are case-insensitive`,
  });
}
```

保留输入 casing，不做 transform，不定义覆盖优先级。

- [ ] **Step 4：同步 AI-008 与 future adapter credential 约束**

在 `AI-008 URL 与传输` 的 Native Executor 要求中新增：

```md
- HTTP header 名按大小写不敏感比较；单个 `publicHeaders` / `secretHeaderRefs` 内及两个集合之间的 canonical 重名必须 fail closed，不定义覆盖优先级；
- 每个 adapter 在激活前必须显式定义 `apiKeyRef` 的 credential placement；Executor 必须对 adapter-managed credential、`publicHeaders` 与 `secretHeaderRefs` 合并后的 effective headers 做 canonical collision 检查并 fail closed。
```

同时明确本批不猜测 adapter 映射、不因 `apiKeyRef` 收紧 v1 schema，保留当前正例兼容。

- [ ] **Step 5：运行 Provider GREEN**

Run:

```bash
pnpm --filter @mahoshojo/contracts exec vitest run --config vitest.config.ts tests/provider-profile.test.ts --reporter=verbose
pnpm --filter @mahoshojo/contracts lint
pnpm --filter @mahoshojo/contracts build
```

Expected: 全部 exit 0；输入 casing 保留，三类 canonical collision 被拒绝，无新增 warning。

- [ ] **Step 6：提交 Provider 修复**

```bash
git add packages/contracts/src/provider-profile.ts packages/contracts/tests/provider-profile.test.ts docs/specs/2026-08-22_022600_平台重整实施规格.md
git diff --cached --check
git commit -m "fix(contracts): 拒绝 Provider Header canonical 冲突"
```

## Task 3：冻结 Local Card provenance 状态不变量

**Files:**

- Modify: `packages/local-library/tests/record.test.ts`
- Modify: `packages/local-library/src/record.ts`
- Modify: `docs/architecture/2026-08-22_022500_平台重整目标架构.md`
- Modify: `docs/specs/2026-08-22_022600_平台重整实施规格.md`

- [ ] **Step 1：写 provenance 三态 RED 与 legacy 正例**

在 local-library record tests 增加：

```ts
it('enforces signature evidence for every provenance kind', () => {
  expect(LocalCardRecordV1Schema.safeParse({
    ...createLocalCardRecord(),
    provenance: { kind: 'official-signed' },
  }).success).toBe(false);

  expect(LocalCardRecordV1Schema.safeParse({
    ...createLocalCardRecord(),
    provenance: { kind: 'signature-invalid', execution: 'edited' },
  }).success).toBe(false);

  for (const extra of [
    { signature: 'unexpected' },
    { signatureVersion: 1 },
    { signatureKeyId: 'unexpected-key' },
  ]) {
    expect(LocalCardRecordV1Schema.safeParse({
      ...createLocalCardRecord(),
      provenance: { kind: 'unsigned', ...extra },
    }).success).toBe(false);
  }
});

it('accepts legacy signed evidence without version or key id', () => {
  for (const kind of ['official-signed', 'signature-invalid'] as const) {
    expect(LocalCardRecordV1Schema.safeParse(createLocalCardRecord({
      provenance: {
        kind,
        signature: 'legacy-hmac-signature',
        execution: kind === 'official-signed' ? 'downloaded' : 'edited',
      },
    })).success).toBe(true);
  }
});
```

- [ ] **Step 2：运行 provenance RED**

Run:

```bash
pnpm --filter @mahoshojo/local-library exec vitest run --config vitest.config.ts tests/record.test.ts --reporter=verbose
```

Expected: signed/invalid 缺 signature 与 unsigned 携带签名证据的负例失败；legacy 无 version/key ID 正例通过。

- [ ] **Step 3：实现 provenance discriminated union**

把当前宽松 object 替换为：

```ts
const LocalCardSignatureEvidenceShape = {
  signature: z.string().min(1).max(16 * 1024),
  signatureVersion: z.number().int().positive().optional(),
  signatureKeyId: OptionalNonBlankStringSchema(256).optional(),
  execution: LocalCardExecutionProvenanceSchema.optional(),
};

const OfficialSignedLocalCardProvenanceSchema = z
  .object({
    kind: z.literal('official-signed'),
    ...LocalCardSignatureEvidenceShape,
  })
  .strict();

const SignatureInvalidLocalCardProvenanceSchema = z
  .object({
    kind: z.literal('signature-invalid'),
    ...LocalCardSignatureEvidenceShape,
  })
  .strict();

const UnsignedLocalCardProvenanceSchema = z
  .object({
    kind: z.literal('unsigned'),
    execution: LocalCardExecutionProvenanceSchema.optional(),
  })
  .strict();

export const LocalCardProvenanceSchema = z.discriminatedUnion('kind', [
  OfficialSignedLocalCardProvenanceSchema,
  UnsignedLocalCardProvenanceSchema,
  SignatureInvalidLocalCardProvenanceSchema,
]);
```

保留 `LocalCardProvenanceKindSchema` export 和 inferred public type。

- [ ] **Step 4：同步 accepted architecture/spec**

把目标架构中的 provenance object 改成结构等价的 union：

```ts
provenance:
  | {
      kind: 'official-signed' | 'signature-invalid';
      signature: string;
      signatureVersion?: number;
      signatureKeyId?: string;
      execution?: 'downloaded' | 'direct-local' | 'direct-remote' | 'imported' | 'edited';
    }
  | {
      kind: 'unsigned';
      execution?: 'downloaded' | 'direct-local' | 'direct-remote' | 'imported' | 'edited';
    };
```

在 `AUTHORITY-003` 后补充：legacy 有效签名允许没有 version/key ID；signed/invalid 必须保留非空 signature；unsigned 不携带 provenance 签名证据；这些结构标签不替代服务器验签，legacy `data.signature` 不因本次 schema 收紧而被改写。

- [ ] **Step 5：运行 local-library GREEN**

Run:

```bash
pnpm --filter @mahoshojo/local-library test
pnpm --filter @mahoshojo/local-library lint
pnpm --filter @mahoshojo/local-library build
```

Expected: 全部 exit 0；三态矩阵与 legacy compatibility 通过，无新增 warning。

- [ ] **Step 6：提交 provenance 修复**

```bash
git add packages/local-library/src/record.ts packages/local-library/tests/record.test.ts docs/architecture/2026-08-22_022500_平台重整目标架构.md docs/specs/2026-08-22_022600_平台重整实施规格.md
git diff --cached --check
git commit -m "fix(local-library): 冻结 provenance 状态不变量"
```

## Task 4：加固 contracts 跨 runtime 可移植性门禁

**Files:**

- Modify: `tests/check-workspace-boundaries.test.ts`
- Modify: `scripts/check-workspace-boundaries.mjs`
- Modify: `packages/contracts/tests/bundle.test.ts`

- [ ] **Step 1：写 contracts runtime gate RED**

在根 boundary tests 中加入：

```ts
it('rejects framework, runtime, and database imports from contracts', async () => {
  const rootDir = await createWorkspaceFixture({
    'packages/contracts/package.json': manifest('@mahoshojo/contracts'),
    'packages/contracts/src/index.ts': [
      "import 'next';",
      "import 'react';",
      "import 'hono';",
      "import 'node:fs';",
      "import 'drizzle-orm';",
      "import '@tauri-apps/api';",
      "import 'cloudflare:workers';",
      "import 'zod';",
    ].join('\n'),
  });

  const violations = checkWorkspaceBoundaries(rootDir).filter(
    (violation) => violation.rule === 'MONO-005-CONTRACTS-RUNTIME',
  );
  expect(violations).toHaveLength(7);
  expect(violations.some((violation) => violation.module === 'zod')).toBe(false);
});


it('rejects browser-only globals from contracts without rejecting shared Web Standards', async () => {
  const rootDir = await createWorkspaceFixture({
    'packages/contracts/package.json': manifest('@mahoshojo/contracts'),
    'packages/contracts/src/index.ts': [
      'const title = window.document.title;',
      'const rootDocument = globalThis.document;',
      "const storage = globalThis['localStorage'];",
      "const url = new URL('https://example.test');",
      "const blob = new Blob(['portable']);",
      'function shadowed(globalThis: { document: string }) { return globalThis.document; }',
      'void title; void rootDocument; void storage; void url; void blob; void shadowed;',
    ].join('\n'),
  });

  const violations = checkWorkspaceBoundaries(rootDir).filter(
    (violation) => violation.rule === 'MONO-005-CONTRACTS-BROWSER-GLOBAL',
  );
  expect(violations.map((violation) => violation.module)).toEqual([
    'window',
    'document',
    'localStorage',
  ]);
  expect(violations.some((violation) => ['URL', 'Blob'].includes(violation.module))).toBe(false);
});
```

- [ ] **Step 2：运行 runtime/browser-global RED**

Run:

```bash
pnpm exec vitest run tests/check-workspace-boundaries.test.ts --reporter=verbose
```

Expected: 新的 runtime 与 browser-global violation 数量为 0，两个测试失败；`zod`、`URL`、`Blob` 不应被误报。

- [ ] **Step 3：实现 contracts runtime import gate**

在 dependency loop 中保留现有 domain 分支，并新增：

```js
if (
  isContractsPackage(unit)
  && (NODE_RUNTIME_MODULE.test(moduleSpecifier) || FRAMEWORK_RUNTIME_MODULE.test(moduleSpecifier))
) {
  addViolation(
    violations,
    'MONO-005-CONTRACTS-RUNTIME',
    sourceFile,
    moduleSpecifier,
    'contracts package must remain independent from framework, runtime, and database modules',
    line,
  );
}
```

不要给 contracts 追加 `MONO-005-DOMAIN-DOM`；env read gate 保持原样。

随后新增独立集合，不能直接复用包含 `Blob`、`FormData`、`WebSocket` 的 domain denylist：

```js
const CONTRACTS_BROWSER_ONLY_GLOBAL_IDENTIFIERS = new Set([
  'window',
  'document',
  'localStorage',
  'sessionStorage',
  'indexedDB',
  'location',
  'history',
  'screen',
  'HTMLElement',
  'Element',
  'Node',
  'MutationObserver',
  'ResizeObserver',
  'IntersectionObserver',
  'FileReader',
]);
```

在 `collectSourceDependencies` 的 scope 分析后增加：

```js
const browserOnlyGlobals = [];
const seenBrowserOnlyReferences = new Set();
const unresolvedGlobalThisReferences = new Set(
  (parsed.scopeManager?.globalScope?.through ?? [])
    .filter((reference) => reference.identifier?.name === 'globalThis' && !reference.resolved)
    .map((reference) => reference.identifier.range?.join(':'))
    .filter(Boolean),
);

const addBrowserOnlyGlobal = (node, name) => {
  if (!node || !CONTRACTS_BROWSER_ONLY_GLOBAL_IDENTIFIERS.has(name)) return;
  const key = node.range
    ? node.range.join(':')
    : `${node.loc?.start?.line ?? 1}:${node.loc?.start?.column ?? 0}:${name}`;
  if (seenBrowserOnlyReferences.has(key)) return;
  seenBrowserOnlyReferences.add(key);
  browserOnlyGlobals.push({ module: name, line: node.loc?.start?.line ?? 1 });
};

for (const reference of parsed.scopeManager?.globalScope?.through ?? []) {
  const identifier = reference.identifier;
  if (identifier?.type === 'Identifier') {
    addBrowserOnlyGlobal(identifier, identifier.name);
  }
}

const isUnresolvedGlobalThis = (node) => {
  const current = unwrapTransparentExpression(node);
  return current?.type === 'Identifier'
    && current.name === 'globalThis'
    && current.range
    && unresolvedGlobalThisReferences.has(current.range.join(':'));
};
```

在 AST `visit` 的 import/env 检查之后、递归 children 之前增加：

```js
const current = unwrapTransparentExpression(node);
if (current?.type === 'MemberExpression' && isUnresolvedGlobalThis(current.object)) {
  const propertyName = memberPropertyName(unwrapTransparentExpression(current.property));
  if (propertyName !== null) addBrowserOnlyGlobal(current.property, propertyName);
}
```

这同时支持 `globalThis.document` 和 `globalThis['localStorage']`；影子 `globalThis`、普通对象 property 和字符串不进入结果。

返回签名改为：

```js
return { imports, domGlobals, browserOnlyGlobals, environmentReads };
```

在 source loop 解构 `browserOnlyGlobals`，只对 `isContractsPackage(unit)` 逐项报告：

```js
for (const { module: browserGlobal, line } of browserOnlyGlobals) {
  addViolation(
    violations,
    'MONO-005-CONTRACTS-BROWSER-GLOBAL',
    sourceFile,
    browserGlobal,
    'contracts package must not reference browser-only globals',
    line,
  );
}
```

不得把 `URL`、`Blob`、`Headers`、`Request`、`Response`、`ReadableStream`、`TextEncoder` 加入该集合。

- [ ] **Step 4：把 bundle test 改为全 exports 枚举**

用下面的统一结构替代手工 symbol 列表：

```ts
import { Buffer } from 'node:buffer';
import { readFileSync } from 'node:fs';
import { build } from 'esbuild';

type ContractsManifest = {
  name: string;
  exports: Record<string, unknown>;
};

const manifest = JSON.parse(
  readFileSync(new URL('../package.json', import.meta.url), 'utf8'),
) as ContractsManifest;

const publicEntrypoints = Object.keys(manifest.exports).map((exportKey) =>
  exportKey === '.' ? manifest.name : `${manifest.name}${exportKey.slice(1)}`,
);

describe('public contracts entrypoint portability', () => {
  it.each(publicEntrypoints)('bundles the complete %s namespace for every target', async (entrypoint) => {
    for (const platform of ['node', 'browser', 'neutral'] as const) {
      const result = await build({
        absWorkingDir: process.cwd(),
        bundle: true,
        write: false,
        metafile: true,
        platform,
        format: 'esm',
        stdin: {
          contents: [
            `import * as publicApi from '${entrypoint}';`,
            'const publicKeys = Object.keys(publicApi);',
            'export { publicApi, publicKeys };',
          ].join('\n'),
          loader: 'ts',
          resolveDir: process.cwd(),
          sourcefile: `contracts-${platform}-${entrypoint.replace(/[^a-z0-9-]/giu, '-')}.ts`,
        },
      });

      const output = result.outputFiles[0]?.text ?? '';
      const inputs = Object.keys(result.metafile?.inputs ?? {});
      expect(output).not.toMatch(/process\.env|userProviderConfig/iu);
      expect(inputs.some((input) =>
        /(?:^|\/)(?:react|react-dom|next|hono|drizzle-orm|better-sqlite3|redis)(?:\/|$)|cloudflare:/iu.test(input),
      ).toBe(false);

      if (platform === 'node') {
        const moduleUrl = `data:text/javascript;base64,${Buffer.from(output).toString('base64')}`;
        await expect(import(moduleUrl)).resolves.toMatchObject({
          publicApi: expect.any(Object),
          publicKeys: expect.any(Array),
        });
      }
    }
  });
});
```

当前 manifest 会枚举出 7 个 exports，并在新增 export 时自动扩展。

在 `publicEntrypoints` 建立后增加 `expect(publicEntrypoints.length).toBeGreaterThan(0)`；不要硬编码 7，以便新增 export 时测试自动扩展。Node import 使用 data URL，只加载已经 bundle 的自包含测试产物，不写临时文件、不引入新 runtime 依赖。

- [ ] **Step 5：运行 portability GREEN**

Run:

```bash
pnpm exec vitest run tests/check-workspace-boundaries.test.ts --reporter=verbose
pnpm --filter @mahoshojo/contracts test
pnpm --filter @mahoshojo/contracts lint
pnpm --filter @mahoshojo/contracts build
pnpm run check:workspace:boundaries
```

Expected: 全部 exit 0；全部 manifest exports 在 node/browser/neutral 下 bundle，Node 产物实际 import 成功；runtime/browser-only fixtures 被拒绝，共享 Web Standards 不误报。

- [ ] **Step 6：提交 portability 门禁**

```bash
git add scripts/check-workspace-boundaries.mjs tests/check-workspace-boundaries.test.ts packages/contracts/tests/bundle.test.ts
git diff --cached --check
git commit -m "test(contracts): 加固跨 runtime 可移植性门禁"
```

## Task 5：恢复 legacy root、workspace 与 Hono Docker CI 门禁

**Files:**

- Modify: `tests/workspace-structure.test.ts`
- Modify: `tests/hono-deploy-workflow.test.ts`
- Modify: `Dockerfile.hono`
- Modify: `package.json`
- Modify: `.github/workflows/hono-deploy.yml`
- Modify: `.github/workflows/cloudflare-deploy.yml`

- [ ] **Step 1：写统一 CI 与 Docker manifest ordering RED**

在 workspace structure tests 中补 `readdirSync` import，并加入：

```ts
it('defines one CI entrypoint for workspace and legacy root verification', () => {
  const packageJson = JSON.parse(readFileSync(path.join(rootDirectory, 'package.json'), 'utf8')) as {
    scripts: Record<string, string>;
  };

  expect(packageJson.scripts['ci:verify']).toBe(
    'pnpm run workspace:verify && pnpm test && pnpm lint',
  );
});

it('copies every workspace manifest before the Hono dependency install layer', () => {
  const dockerfile = readFileSync(path.join(rootDirectory, 'Dockerfile.hono'), 'utf8');
  const installIndex = dockerfile.indexOf('RUN pnpm install --frozen-lockfile');
  expect(installIndex).toBeGreaterThan(-1);

  for (const parent of ['apps', 'packages']) {
    for (const entry of readdirSync(path.join(rootDirectory, parent), { withFileTypes: true })) {
      if (!entry.isDirectory()) continue;
      const relativeManifest = `${parent}/${entry.name}/package.json`;
      if (!existsSync(path.join(rootDirectory, relativeManifest))) continue;
      const copyIndex = dockerfile.indexOf(`COPY ${relativeManifest} ./${relativeManifest}`);
      expect(copyIndex, `${relativeManifest} must be copied before pnpm install`).toBeGreaterThan(-1);
      expect(copyIndex).toBeLessThan(installIndex);
    }
  }
});
```

- [ ] **Step 2：写两条 workflow 统一验证与 Docker smoke RED**

在 workflow tests 增加 Cloudflare path，并加入：

```ts
const CLOUDFLARE_WORKFLOW_PATH = resolve(process.cwd(), '.github/workflows/cloudflare-deploy.yml');

test('runs unified workspace and legacy verification in both deployment workflows', () => {
  for (const workflowPath of [WORKFLOW_PATH, CLOUDFLARE_WORKFLOW_PATH]) {
    const workflow = readFileSync(workflowPath, 'utf8');
    expect(workflow).toContain('run: pnpm run ci:verify');
  }
});

test('builds the supported Hono Docker image before publishing artifacts', () => {
  const workflow = readFileSync(WORKFLOW_PATH, 'utf8');
  const buildJob = getJob(workflow, 'build');
  const dockerStep = getStep(buildJob, 'Verify Hono container build');
  expect(dockerStep).toContain('run: docker build --file Dockerfile.hono .');
});
```

- [ ] **Step 3：运行 RED**

Run:

```bash
pnpm exec vitest run tests/workspace-structure.test.ts tests/hono-deploy-workflow.test.ts --reporter=verbose
```

Expected: 缺少 `ci:verify`、Docker manifest COPY、workflow 统一 command 和 Docker step 导致失败。

- [ ] **Step 4：修复 Docker dependency layer**

在根 manifests 与 install 之间按当前动态测试要求加入：

```dockerfile
# Phase 2.5 legacy-root 临时策略；apps/api 激活后改为复制其实际 workspace 依赖闭包。
COPY apps/d1-gateway/package.json ./apps/d1-gateway/package.json
COPY packages/ai-core/package.json ./packages/ai-core/package.json
COPY packages/ai-direct/package.json ./packages/ai-direct/package.json
COPY packages/config/package.json ./packages/config/package.json
COPY packages/contracts/package.json ./packages/contracts/package.json
COPY packages/domain/package.json ./packages/domain/package.json
COPY packages/hosted-api/package.json ./packages/hosted-api/package.json
COPY packages/local-library/package.json ./packages/local-library/package.json
COPY packages/multiplayer-core/package.json ./packages/multiplayer-core/package.json
RUN pnpm install --frozen-lockfile
```

保持 builder 的 `COPY . .` 和 server build 不变。注释是明确的删除门禁：`apps/api` 成为真实 server build/manifest 所有者后，Docker context 改为该 app 的依赖闭包，并删除“全部未来 workspace 都进入 Hono image”的动态测试。

- [ ] **Step 5：新增统一 CI script 并接入两条 workflow**

在 `package.json.scripts` 增加：

```json
"ci:verify": "pnpm run workspace:verify && pnpm test && pnpm lint"
```

把两条 workflow 的 workspace verification step 改为：

```yaml
- name: Verify workspace and legacy root
  run: pnpm run ci:verify
```

Hono workflow 在 targeted Hono test 后增加：

```yaml
- name: Verify Hono container build
  run: docker build --file Dockerfile.hono .
```

保持 `deploy.needs: build`、生产分支 `if`、Cloudflare production/preview 条件、artifact 和 secret 配置不变。

- [ ] **Step 6：运行 GREEN 与本地可执行的构建验证**

Run:

```bash
pnpm exec vitest run tests/workspace-structure.test.ts tests/hono-deploy-workflow.test.ts --reporter=verbose
pnpm run workspace:verify
pnpm test -- --reporter=dot
pnpm lint
pnpm run build:server
```

Expected: 全部 exit 0；无新增 warning。当前环境没有 Docker CLI 时，`docker build --file Dockerfile.hono .` 不在本步骤伪称通过，留到最终未验证项报告并由 Actions 持续执行。

- [ ] **Step 7：提交 CI/容器修复**

```bash
git add Dockerfile.hono package.json .github/workflows/hono-deploy.yml .github/workflows/cloudflare-deploy.yml tests/workspace-structure.test.ts tests/hono-deploy-workflow.test.ts
git diff --cached --check
git commit -m "chore(ci): 恢复根应用与 Hono 容器门禁"
```

## Task 6：校正文档事实

**Files:**

- Modify: `server/README.md`
- Modify: `docs/logs/2026-08-23_194020_平台重整Phase2本地库契约实施日志.md`

- [ ] **Step 1：修正 Hono Actions 说明**

将 README 的 GitHub Actions 段落改成以下事实：

```md
`.github/workflows/hono-deploy.yml` 会在任意分支的每次 push 后运行统一 CI 验证、Hono 定向测试、
容器 smoke 与单文件 bundle 构建，并把 bundle 上传为 GitHub Actions artifact。只有
`feature/v0.2.0_Battle_Growth_MahoShojo` 分支通过 build job 后，`deploy` job 才会把 artifact 上传到 VPS：
服务器先校验 SHA-256，并用当前 `/opt/mahoshojo-hono/.env.hono` 执行生产配置预检；预检成功后
才重建容器。如果新版本在两分钟内未就绪，脚本会恢复上一个 release。
```

保留 Environment secret/variable、host key 和生产切流说明；删除“如果只想让生产分支发布，可收窄 push.branches”的误导性表达，若保留收窄建议只能说明它减少非生产 build。

- [ ] **Step 2：修正 source commit 标题**

将日志第 5 行改为：

```md
- source commit：`f3eeadefa00a`（`fix(contracts): 收紧 AI 与 Profile 不可信输入边界`）
```

SHA 不变，不引入自动生成脚本。

- [ ] **Step 3：验证文档与机器事实一致**

Run:

```bash
git show -s --format='%H%n%s' f3eeadefa00a
pnpm exec vitest run tests/hono-deploy-workflow.test.ts --reporter=verbose
git diff --check
```

Expected: SHA/title 与日志一致；workflow test exit 0；无 whitespace error。

- [ ] **Step 4：提交文档修复**

```bash
git add server/README.md docs/logs/2026-08-23_194020_平台重整Phase2本地库契约实施日志.md
git diff --cached --check
git commit -m "docs(platform): 校正部署说明与实施证据"
```

## Task 7：全量验证、告警审计与独立代码审查

**Files:**

- Review only: commits after `9f91e20f`
- Modify only if verification/reviewer finds a confirmed in-scope defect

- [ ] **Step 1：运行统一验证与边界检查**

Run:

```bash
pnpm run ci:verify
pnpm run check:naming
pnpm run check:workspace:boundaries
pnpm run verify:server:runtime
```

Expected: `ci:verify`、workspace boundary、server runtime exit 0；`check:naming` 若仍输出 legacy report-only warning，记录其数量与是否由本次 diff 引入，不把存量报告误称为本次失败。

- [ ] **Step 2：运行所有生产构建路径**

Run:

```bash
pnpm run build:server
pnpm run build
pnpm run build:cf
```

Expected: exit 0。若 Cloudflare/Next build 因当前环境缺少公开 build-time 配置、网络或资源限制失败，保留完整错误并区分代码失败与环境限制；不得声称未通过。

- [ ] **Step 3：尝试本地 Docker/Compose 验证**

Run:

```bash
docker build --file Dockerfile.hono .
docker compose -f compose.hono.yml config
```

Expected: 有 Docker CLI/daemon 时 exit 0；当前已知环境没有 Docker CLI，因此预计记录为未执行成功的环境限制，不能因此改弱 CI smoke。

- [ ] **Step 4：检查依赖与输出警告**

Run:

```bash
pnpm audit --prod
git diff 9f91e20f..HEAD --check
git status --short --branch
```

Expected: diff check exit 0、工作树干净；audit 若需要网络而失败，记录网络限制；若报告生产依赖漏洞，按实际 severity、可利用路径和本次范围裁决，不为清零数字盲目升级 major。

- [ ] **Step 5：请求独立代码审查**

获取范围：

```bash
git rev-parse 9f91e20f
git rev-parse HEAD
git diff --stat 9f91e20f..HEAD
```

调度 reviewer，要求核验：

- D1 mutation/batch 是否在 retryable status 与 fetch error 下均不透明重放；
- `createWithCustomId`/`updateById`/`saveToD1` 是否向上传播稳定 indeterminate error，而不是 `null`/`false`；
- safe-read 是否只能显式 opt in，是否每个 attempt 刷新 Gateway 鉴权元数据，是否存在通用 adapter 错误 opt in；
- provenance 是否兼容 legacy、是否仍把客户端 kind 当权威；
- Header canonical collision 是否覆盖单集合与跨集合；
- contracts bundle 是否真的覆盖所有 exports 且 Node 产物被实际 import，browser-only global gate 是否覆盖裸 identifier/`globalThis` 且不误伤 Web Standards；
- accepted spec 是否仅冻结未来 effective credential collision fail-closed，没有猜测 `apiKeyRef` 到 Header 的映射；
- Docker manifest ordering、两条 workflow 和生产 deploy gate 是否一致；
- accepted docs、测试和实现是否同步。

- [ ] **Step 6：处理 reviewer finding**

Critical/Important finding 先复核证据，再按 RED -> GREEN 修复并追加到对应主题的独立 follow-up commit；错误或超出范围的建议用代码/规范/测试证据驳回。Minor 仅在不扩大范围且能独立解释时处理，否则记录。

- [ ] **Step 7：最终复验与交付检查**

任何 reviewer 修复后重新运行：

```bash
pnpm run ci:verify
pnpm run build:server
pnpm run verify:server:runtime
git diff 9f91e20f..HEAD --check
git status --short --branch
```

最后逐项对照已批准设计与三份审查文档，报告：实际提交、实际通过命令、未验证项、warning/存量问题、Docker/Cloudflare/香港 Hono 是否需要人工操作，以及 production/schema/secret/release 影响。
