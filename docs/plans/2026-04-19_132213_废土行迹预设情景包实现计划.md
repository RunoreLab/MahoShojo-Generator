# 废土行迹预设情景包 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增三张《废土行迹》预设情景卡，并通过测试锁定预设索引与通用情景 JSON 的基本契约。

**Architecture:** 复用现有 `public/scenario-presets/*.json` 静态资源与 `lib/scenario-presets.ts` 静态索引机制，不新增页面、分类或运行时代码。三张卡全部采用 `GeneralScenarioSchema` 可识别的 `templateId/title/content` 通用情景格式。

**Tech Stack:** Next.js Pages Router, Edge-compatible static preset list, TypeScript, Bun Test, ESLint, Next build

---

### Task 1: 先补失败测试，固定三张预设情景契约

**Files:**
- Create: `tests/scenario-presets.test.ts`
- Reference: `lib/scenario-presets.ts`
- Reference: `public/scenario-presets/S01_queen_will.json`

- [ ] **Step 1: 新增预设情景测试文件**

```ts
import { describe, expect, it } from 'bun:test';

import { GeneralScenarioSchema } from '@/lib/schemas/general-scenario';
import {
  SCENARIO_PRESET_LIST,
  getScenarioPresetByFilename,
  normalizeScenarioPresetFilename,
} from '@/lib/scenario-presets';
import wastetraceEncounter from '@/public/scenario-presets/S13_wastetrace_encounter.json';
import wastetraceRouteFailure from '@/public/scenario-presets/S14_wastetrace_route_failure.json';
import wastetraceBlockadeBreakout from '@/public/scenario-presets/S15_wastetrace_blockade_breakout.json';

const WASTETRACE_PRESETS = [
  {
    filename: 'S13_wastetrace_encounter.json',
    title: '废土行迹·偶遇：同路人未必同行',
    payload: wastetraceEncounter,
  },
  {
    filename: 'S14_wastetrace_route_failure.json',
    title: '废土行迹·调查探索：路标失效之后',
    payload: wastetraceRouteFailure,
  },
  {
    filename: 'S15_wastetrace_blockade_breakout.json',
    title: '废土行迹·战斗冲突：封路与突围',
    payload: wastetraceBlockadeBreakout,
  },
] as const;

describe('预设情景列表', () => {
  it('废土行迹预设情景已注册为通用情景卡', () => {
    for (const expected of WASTETRACE_PRESETS) {
      const preset = SCENARIO_PRESET_LIST.find((item) => item.filename === expected.filename);

      expect(preset).toEqual(expect.objectContaining({
        filename: expected.filename,
        title: expected.title,
        template: 'general-scenario',
      }));
      expect(preset?.description).toContain('【');
      expect(getScenarioPresetByFilename(expected.filename)?.title).toBe(expected.title);
      expect(normalizeScenarioPresetFilename(expected.filename.replace(/\.json$/, ''))).toBe(expected.filename);
    }
  });

  it('废土行迹预设情景 JSON 满足通用情景格式', () => {
    for (const expected of WASTETRACE_PRESETS) {
      const parsed = GeneralScenarioSchema.parse(expected.payload);

      expect(parsed.templateId).toBe('通用情景');
      expect(parsed.title).toBe(expected.title);
      expect(parsed.content).toContain('# ');
      expect(parsed.content).toContain('废土行迹');
      expect(parsed.content).toContain('使用提示');
    }
  });
});
```

- [ ] **Step 2: 运行定向测试确认 RED**

Run: `bun test tests/scenario-presets.test.ts`  
Expected: FAIL，失败原因来自 `S13_wastetrace_encounter.json`、`S14_wastetrace_route_failure.json` 或 `S15_wastetrace_blockade_breakout.json` 尚未存在，或 `SCENARIO_PRESET_LIST` 尚未注册。

### Task 2: 新增三张通用情景 JSON 并注册索引

**Files:**
- Create: `public/scenario-presets/S13_wastetrace_encounter.json`
- Create: `public/scenario-presets/S14_wastetrace_route_failure.json`
- Create: `public/scenario-presets/S15_wastetrace_blockade_breakout.json`
- Modify: `lib/scenario-presets.ts`
- Reference: `docs/specs/2026-04-19_131459_废土行迹预设情景包设计.md`

- [ ] **Step 1: 新增 `S13_wastetrace_encounter.json`**

JSON 顶层结构必须为：

```json
{
  "templateId": "通用情景",
  "title": "废土行迹·偶遇：同路人未必同行",
  "content": "# 废土行迹·偶遇：同路人未必同行\n\n..."
}
```

正文必须包含以下章节：

```markdown
## 情景简介
## 世界观锚点
## 开场局面
## 角色为何会在这里
## 潜在冲突与摩擦
## 推进钩子
## 可能走向
## 使用提示
```

内容重点必须覆盖临时偶遇、通路/中继点、是否共享情报、是否同行、学城/调查协会/商路/救世军背景摩擦。

- [ ] **Step 2: 新增 `S14_wastetrace_route_failure.json`**

JSON 顶层结构必须为：

```json
{
  "templateId": "通用情景",
  "title": "废土行迹·调查探索：路标失效之后",
  "content": "# 废土行迹·调查探索：路标失效之后\n\n..."
}
```

正文必须包含同一套 8 个章节。内容重点必须覆盖路线失效、旧标记复核、补给点/哨点失联、先遣试探、成果公开/收费/限制流通/封存。

- [ ] **Step 3: 新增 `S15_wastetrace_blockade_breakout.json`**

JSON 顶层结构必须为：

```json
{
  "templateId": "通用情景",
  "title": "废土行迹·战斗冲突：封路与突围",
  "content": "# 废土行迹·战斗冲突：封路与突围\n\n..."
}
```

正文必须包含同一套 8 个章节。内容重点必须覆盖封锁、误判、拦截、护送、突围、断后、战后通报与追责，且明确战斗不是唯一终点。

- [ ] **Step 4: 在 `lib/scenario-presets.ts` 追加三条元数据**

新增条目：

```ts
  {
    title: '废土行迹·偶遇：同路人未必同行',
    description: '【旅途遭遇】通路与中继点上的临时相遇，强调试探、立场摩擦与是否同行。',
    filename: 'S13_wastetrace_encounter.json',
    template: 'general-scenario',
  },
  {
    title: '废土行迹·调查探索：路标失效之后',
    description: '【调查探索】围绕失效路线、失联节点与异常记录展开复核，强调判断、记录与处置。',
    filename: 'S14_wastetrace_route_failure.json',
    template: 'general-scenario',
  },
  {
    title: '废土行迹·战斗冲突：封路与突围',
    description: '【冲突】因封锁、误判或争夺目标而升级的废土武装冲突，强调代价、撤离与后果。',
    filename: 'S15_wastetrace_blockade_breakout.json',
    template: 'general-scenario',
  },
```

- [ ] **Step 5: 运行定向测试确认 GREEN**

Run: `bun test tests/scenario-presets.test.ts`  
Expected: PASS

### Task 3: 回归验证与提交

**Files:**
- Verify: `tests/scenario-presets.test.ts`
- Verify: `public/scenario-presets/S13_wastetrace_encounter.json`
- Verify: `public/scenario-presets/S14_wastetrace_route_failure.json`
- Verify: `public/scenario-presets/S15_wastetrace_blockade_breakout.json`
- Verify: `lib/scenario-presets.ts`

- [ ] **Step 1: 运行预设相关测试**

Run: `bun test tests/scenario-presets.test.ts tests/questionnaires.test.ts`  
Expected: PASS

- [ ] **Step 2: 运行 lint**

Run: `bun run lint`  
Expected: exit 0

- [ ] **Step 3: 运行 build**

Run: `bun run build`  
Expected: exit 0

- [ ] **Step 4: 检查 git diff**

Run: `git diff --stat`  
Expected: 只包含本计划文件、一个测试文件、三张预设 JSON 与 `lib/scenario-presets.ts`。

- [ ] **Step 5: 提交实现**

```bash
git add docs/plans/2026-04-19_132213_废土行迹预设情景包实现计划.md tests/scenario-presets.test.ts lib/scenario-presets.ts public/scenario-presets/S13_wastetrace_encounter.json public/scenario-presets/S14_wastetrace_route_failure.json public/scenario-presets/S15_wastetrace_blockade_breakout.json
git commit -m "feat: 新增废土行迹预设情景包" -m "接入偶遇、调查探索与战斗冲突三张通用情景预设，并补充预设契约测试。"
```
