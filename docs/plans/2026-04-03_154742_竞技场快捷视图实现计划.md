# 竞技场快捷视图共享设置修复 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 `/battle` 进入后覆写 `/arena` 设置的问题，并让简洁版显式展示继承自完整版、但本页未提供编辑入口的高级上下文。

**Architecture:** 保持 `/battle` 与 `/arena` 继续共享同一个 `useBattleStore` 与同一份当前工作区，但把简洁版的“默认值收敛”从共享 store 中移除。新增一个纯 helper 和一个轻量只读提示组件，由 `/battle` 读取共享 state 后渲染“继承的完整版设置 / 高级上下文”摘要，不新增第二套 store、不新增新的 localStorage key，也不改 battle 生成请求的实际数据来源。

**Tech Stack:** Next.js Pages Router、React 19、TypeScript strict、Zustand persist、Tailwind 4、Bun test

---

## 执行前提

- 先通读已批准 spec：[docs/specs/2026-04-03_154150_竞技场快捷视图设置与隐藏上下文边界设计.md](/home/notuhao/code/MahoShojo-Generator/docs/specs/2026-04-03_154150_竞技场快捷视图设置与隐藏上下文边界设计.md)
- 本计划按 `@superpowers:test-driven-development` 执行：先写失败测试，再做最小实现，再验证，再提交
- 不新增 `/battle` 专属持久化 key，不复制第二份 battle store
- 不修改 battle API 请求体语义；`/battle` 与 `/arena` 的生成链路仍从同一个 store 读取
- 不在本轮引入“仅使用当前页可见内容生成”的新开关；本轮只修共享设置副作用与上下文可见性

## 文件结构与职责

### 新增文件

- `components/arena-lite/battle-lite-inherited-summary.ts`
  - 纯 helper，负责把共享 store 中的 `storyLength / selectedLanguage / settings / auxScenarios / selectedQuestionnaires / adjudicationEvents` 归一化为简洁版可展示的摘要结构
- `components/arena-lite/BattleLiteInheritedContextNotice.tsx`
  - 简洁版只读提示组件；渲染“沿用完整版设置”和“当前继承的高级上下文”，并提供跳转 `/arena` 的编辑入口
- `tests/battle-lite-inherited-summary.test.ts`
  - 纯 helper 测试，锁定摘要文案和计数规则
- `tests/battle-lite-inherited-context-notice.test.tsx`
  - 组件渲染测试，锁定 notice 的可见性和 `/arena` 入口文案

### 修改文件

- [`components/arena/stores/useBattleStore.ts`](/home/notuhao/code/MahoShojo-Generator/components/arena/stores/useBattleStore.ts)
  - 删除 `applyBattleLiteDefaults` 写共享 state 的逻辑，确保 `/battle` 不再通过 store 重置任何设置或高级上下文
- [`components/arena/types/index.ts`](/home/notuhao/code/MahoShojo-Generator/components/arena/types/index.ts)
  - 同步移除 `BattleStoreState` 中的 `applyBattleLiteDefaults` 接口声明
- [`components/arena-lite/BattleLitePage.tsx`](/home/notuhao/code/MahoShojo-Generator/components/arena-lite/BattleLitePage.tsx)
  - 删除 mount 时执行默认值收敛的 effect
  - 接入 `BattleLiteInheritedContextNotice`
- [`tests/battle-store-lite-defaults.test.ts`](/home/notuhao/code/MahoShojo-Generator/tests/battle-store-lite-defaults.test.ts)
  - 替换为“共享 store 不再暴露简洁页重置动作”的契约测试，必要时直接重命名为更准确的测试文件名

### 只读参考，不改逻辑

- [`components/arena/hooks/useBattleEngine.ts`](/home/notuhao/code/MahoShojo-Generator/components/arena/hooks/useBattleEngine.ts)
  - 用于确认 `/battle` 仍然从共享 store 读取 `auxScenarios / selectedQuestionnaires / adjudicationEvents / settings`
- [`components/arena/components/BattleActions.tsx`](/home/notuhao/code/MahoShojo-Generator/components/arena/components/BattleActions.tsx)
  - 用于确认生成按钮区域与新增 notice 的放置关系，避免重复信息或破坏现有高级工具折叠区

### 关键实现约束

- `storyLength`、`selectedLanguage`、`settings.*` 仍然是共享设置，简洁版只是不显示其中部分控件，不允许写回默认值
- `auxScenarios`、`selectedQuestionnaires`、`adjudicationEvents` 仍然是共享工作区，不允许在进入 `/battle` 时自动清空
- notice 必须是“只读可见性”，不是把完整版高级面板直接搬到 `/battle`
- notice 的摘要必须与真实请求语义一致，不能显示一套、请求实际用另一套

---

### Task 1: 移除简洁版对共享 store 的重置副作用

**Files:**
- Modify: [`components/arena/stores/useBattleStore.ts`](/home/notuhao/code/MahoShojo-Generator/components/arena/stores/useBattleStore.ts)
- Modify: [`components/arena/types/index.ts`](/home/notuhao/code/MahoShojo-Generator/components/arena/types/index.ts)
- Modify or Replace: [`tests/battle-store-lite-defaults.test.ts`](/home/notuhao/code/MahoShojo-Generator/tests/battle-store-lite-defaults.test.ts)

- [ ] **Step 1: 先写失败测试，锁定共享 store 契约**

将现有测试替换为“简洁版不再拥有共享 store 重置动作”的断言，至少覆盖：

```ts
import { beforeEach, describe, expect, test } from 'bun:test';

import { useBattleStore } from '@/components/arena/stores/useBattleStore';

describe('battle lite shared store contract', () => {
  beforeEach(() => {
    useBattleStore.setState((state) => ({
      ...state,
      selectedLanguage: 'en-US',
      storyLength: 'long',
      arenaFreeRankingEnabled: true,
      auxScenarios: [
        {
          id: 'aux-1',
          content: { title: '辅助情景' },
          fileName: 'aux.json',
          isNative: false,
        },
      ],
      selectedQuestionnaires: [
        {
          source: 'database',
          questionnaire: { id: 'q-demo', title: '测试问卷', questions: [] },
          selectionId: 'sel-demo',
          useLore: true,
        },
      ],
      adjudicationEvents: [{ id: 'evt-1', label: '判定事件' }],
      settings: {
        ...state.settings,
        readNarrativeHistory: true,
        writeNarrativeHistory: true,
        battleReportCardWidthMode: 'manual',
        battleReportCardWidthPx: 920,
      },
    }));
  });

  test('shared store 不再暴露 applyBattleLiteDefaults', () => {
    const state = useBattleStore.getState() as Record<string, unknown>;

    expect('applyBattleLiteDefaults' in state).toBe(false);
    expect(state.storyLength).toBe('long');
    expect(state.selectedLanguage).toBe('en-US');
  });
});
```

- [ ] **Step 2: 运行测试，确认当前失败**

Run: `bun test tests/battle-store-lite-defaults.test.ts`
Expected: FAIL，原因是当前 store 仍暴露 `applyBattleLiteDefaults`

- [ ] **Step 3: 做最小实现，移除共享 store 的简洁版重置入口**

实现要求：

```ts
// components/arena/types/index.ts
export interface BattleStoreState {
  // ...existing fields
  updateSettings: (settings: Partial<BattleSettings>) => void;
  // 删除 applyBattleLiteDefaults
}

// components/arena/stores/useBattleStore.ts
export const useBattleStore = create<BattleStoreState>()(
  persist(
    (set) => ({
      // ...existing state
      updateSettings: (incoming) =>
        set((state) => ({
          settings: {
            ...state.settings,
            ...incoming,
          },
        })),
      // 删除 applyBattleLiteDefaults implementation
    }),
    {
      name: 'arena-storage',
      // partialize / merge 不新增 lite 专属逻辑
    },
  ),
);
```

要求：

- 不新增替代性的 lite reset action
- 不借由其他 action 在 store 层清空 `auxScenarios / selectedQuestionnaires / adjudicationEvents`
- 现有 `partialize` 与 `merge` 逻辑保持共享 store 语义，不新增分支

- [ ] **Step 4: 重新运行测试，确认通过**

Run: `bun test tests/battle-store-lite-defaults.test.ts`
Expected: PASS

- [ ] **Step 5: 提交这一小步**

```bash
git add components/arena/stores/useBattleStore.ts components/arena/types/index.ts tests/battle-store-lite-defaults.test.ts
git commit -m "fix: 移除简洁竞技场共享状态重置"
```

---

### Task 2: 为简洁版建立“继承的完整版设置 / 高级上下文”摘要 helper

**Files:**
- Create: `components/arena-lite/battle-lite-inherited-summary.ts`
- Create: `tests/battle-lite-inherited-summary.test.ts`

- [ ] **Step 1: 先写失败测试，锁定摘要规则**

创建 `tests/battle-lite-inherited-summary.test.ts`，至少覆盖：

```ts
import { describe, expect, test } from 'bun:test';

import { buildBattleLiteInheritedSummary } from '@/components/arena-lite/battle-lite-inherited-summary';

describe('battle lite inherited summary', () => {
  test('会汇总共享设置与隐藏高级上下文数量', () => {
    expect(
      buildBattleLiteInheritedSummary({
        storyLength: 'long',
        selectedLanguage: 'en-US',
        settings: {
          userGuidance: '保持原样',
          readArenaHistory: true,
          readArenaHistoryLimit: 5,
          isArenaHistoryUnlimited: false,
          writeArenaHistory: true,
          readCurrentState: true,
          writeCurrentState: true,
          readNarrativeHistory: true,
          readNarrativeHistoryLimit: 12,
          isNarrativeHistoryUnlimited: false,
          writeNarrativeHistory: false,
          streamTransport: 'sse',
          battleReportCardWidthMode: 'manual',
          battleReportCardWidthPx: 920,
        },
        auxScenarios: [{ id: 'aux-1' }, { id: 'aux-2' }],
        selectedQuestionnaires: [{ selectionId: 'q-1' }],
        adjudicationEvents: [{ id: 'evt-1' }, { id: 'evt-2' }, { id: 'evt-3' }],
      } as any),
    ).toEqual({
      inheritedSettings: [
        '长度：long',
        '语言：en-US',
        '历战读取：开启（5 条）',
        '当前状态写回：开启',
        '叙事历史：开启（12 条）/ 不写回',
      ],
      hiddenContext: ['辅助情景 2 个', '问卷 1 张', '判定事件 3 条'],
      hasHiddenContext: true,
    });
  });

  test('没有隐藏高级上下文时返回空上下文提示', () => {
    expect(
      buildBattleLiteInheritedSummary({
        storyLength: 'default',
        selectedLanguage: 'zh-CN',
        settings: {
          userGuidance: '',
          readArenaHistory: true,
          readArenaHistoryLimit: 3,
          isArenaHistoryUnlimited: false,
          writeArenaHistory: true,
          readCurrentState: true,
          writeCurrentState: true,
          readNarrativeHistory: false,
          readNarrativeHistoryLimit: 10,
          isNarrativeHistoryUnlimited: false,
          writeNarrativeHistory: false,
          streamTransport: 'sse',
          battleReportCardWidthMode: 'auto',
          battleReportCardWidthPx: 680,
        },
        auxScenarios: [],
        selectedQuestionnaires: [],
        adjudicationEvents: [],
      } as any),
    ).toMatchObject({
      hiddenContext: [],
      hasHiddenContext: false,
    });
  });
});
```

- [ ] **Step 2: 运行测试，确认当前失败**

Run: `bun test tests/battle-lite-inherited-summary.test.ts`
Expected: FAIL，提示 helper 文件尚不存在

- [ ] **Step 3: 以最小实现补齐纯 helper**

在 `components/arena-lite/battle-lite-inherited-summary.ts` 中实现：

```ts
import type { BattleStoreState } from '@/components/arena/types';

type SummaryInput = Pick<
  BattleStoreState,
  'storyLength' | 'selectedLanguage' | 'settings' | 'auxScenarios' | 'selectedQuestionnaires' | 'adjudicationEvents'
>;

export type BattleLiteInheritedSummary = {
  inheritedSettings: string[];
  hiddenContext: string[];
  hasHiddenContext: boolean;
};

const formatArenaHistoryRead = (settings: SummaryInput['settings']): string => {
  if (!settings.readArenaHistory) return '历战读取：关闭';
  if (settings.isArenaHistoryUnlimited) return '历战读取：开启（不限）';
  return `历战读取：开启（${Math.max(1, settings.readArenaHistoryLimit)} 条）`;
};

const formatNarrativeHistory = (settings: SummaryInput['settings']): string => {
  if (!settings.readNarrativeHistory) {
    return settings.writeNarrativeHistory ? '叙事历史：关闭读取 / 写回开启' : '叙事历史：关闭';
  }
  const readPart = settings.isNarrativeHistoryUnlimited
    ? '开启（不限）'
    : `开启（${Math.max(1, settings.readNarrativeHistoryLimit)} 条）`;
  const writePart = settings.writeNarrativeHistory ? '写回开启' : '不写回';
  return `叙事历史：${readPart} / ${writePart}`;
};

export const buildBattleLiteInheritedSummary = (input: SummaryInput): BattleLiteInheritedSummary => {
  const hiddenContext: string[] = [];
  if (input.auxScenarios.length > 0) hiddenContext.push(`辅助情景 ${input.auxScenarios.length} 个`);
  if (input.selectedQuestionnaires.length > 0) hiddenContext.push(`问卷 ${input.selectedQuestionnaires.length} 张`);
  if (input.adjudicationEvents.length > 0) hiddenContext.push(`判定事件 ${input.adjudicationEvents.length} 条`);

  return {
    inheritedSettings: [
      `长度：${input.storyLength}`,
      `语言：${input.selectedLanguage}`,
      formatArenaHistoryRead(input.settings),
      `当前状态写回：${input.settings.writeCurrentState ? '开启' : '关闭'}`,
      formatNarrativeHistory(input.settings),
    ],
    hiddenContext,
    hasHiddenContext: hiddenContext.length > 0,
  };
};
```

要求：

- helper 必须是 pure function，不直接读 hook/store
- 只表达“当前真实生效状态”，不杜撰 `/battle` 页面上不存在的编辑能力
- 文案先以当前值直出为主，不做 i18n 扩展

- [ ] **Step 4: 重新运行测试，确认通过**

Run: `bun test tests/battle-lite-inherited-summary.test.ts`
Expected: PASS

- [ ] **Step 5: 提交这一小步**

```bash
git add components/arena-lite/battle-lite-inherited-summary.ts tests/battle-lite-inherited-summary.test.ts
git commit -m "feat: 增加简洁竞技场继承摘要 helper"
```

---

### Task 3: 在 `/battle` 接入只读 notice，并展示继承的高级上下文

**Files:**
- Create: `components/arena-lite/BattleLiteInheritedContextNotice.tsx`
- Modify: [`components/arena-lite/BattleLitePage.tsx`](/home/notuhao/code/MahoShojo-Generator/components/arena-lite/BattleLitePage.tsx)
- Create: `tests/battle-lite-inherited-context-notice.test.tsx`

- [ ] **Step 1: 先写失败测试，锁定 notice 文案与入口**

创建 `tests/battle-lite-inherited-context-notice.test.tsx`，至少覆盖：

```tsx
import React from 'react';
import { expect, test } from 'bun:test';
import { renderToStaticMarkup } from 'react-dom/server';

import { BattleLiteInheritedContextNotice } from '@/components/arena-lite/BattleLiteInheritedContextNotice';

test('存在隐藏高级上下文时显示继承提示和完整版编辑入口', () => {
  const html = renderToStaticMarkup(
    <BattleLiteInheritedContextNotice
      summary={{
        inheritedSettings: ['长度：long', '语言：en-US'],
        hiddenContext: ['辅助情景 2 个', '问卷 1 张', '判定事件 3 条'],
        hasHiddenContext: true,
      }}
    />,
  );

  expect(html).toContain('当前沿用完整版设置');
  expect(html).toContain('辅助情景 2 个');
  expect(html).toContain('/arena');
  expect(html).toContain('前往完整版编辑');
});

test('没有隐藏高级上下文时仍显示共享设置，但给出空状态说明', () => {
  const html = renderToStaticMarkup(
    <BattleLiteInheritedContextNotice
      summary={{
        inheritedSettings: ['长度：default', '语言：zh-CN'],
        hiddenContext: [],
        hasHiddenContext: false,
      }}
    />,
  );

  expect(html).toContain('当前沿用完整版设置');
  expect(html).toContain('当前未继承额外高级上下文');
});
```

- [ ] **Step 2: 运行测试，确认当前失败**

Run: `bun test tests/battle-lite-inherited-context-notice.test.tsx`
Expected: FAIL，提示组件文件尚不存在

- [ ] **Step 3: 实现 notice 组件并接入 `/battle`**

实现要点：

```tsx
// components/arena-lite/BattleLiteInheritedContextNotice.tsx
import Link from 'next/link';

import type { BattleLiteInheritedSummary } from './battle-lite-inherited-summary';

export function BattleLiteInheritedContextNotice(props: {
  summary: BattleLiteInheritedSummary;
}) {
  const { inheritedSettings, hiddenContext, hasHiddenContext } = props.summary;

  return (
    <div className="rounded-2xl border border-sky-200 bg-sky-50/85 px-4 py-3 text-sm text-slate-700">
      <div className="font-medium text-slate-900">当前沿用完整版设置</div>
      <div className="mt-2 flex flex-wrap gap-2">
        {inheritedSettings.map((item) => (
          <span key={item} className="rounded-full bg-white px-2.5 py-1 text-xs text-slate-700">
            {item}
          </span>
        ))}
      </div>

      <div className="mt-3 text-xs text-slate-600">
        {hasHiddenContext ? hiddenContext.join('｜') : '当前未继承额外高级上下文'}
      </div>

      <Link href="/arena" className="mt-3 inline-flex text-sm font-medium text-sky-700 hover:underline">
        前往完整版编辑
      </Link>
    </div>
  );
}
```

在 [`components/arena-lite/BattleLitePage.tsx`](/home/notuhao/code/MahoShojo-Generator/components/arena-lite/BattleLitePage.tsx) 中：

- 删除 `applyBattleLiteDefaults` selector 与 mount effect
- 读取 `storyLength / selectedLanguage / settings / auxScenarios / selectedQuestionnaires / adjudicationEvents`
- 使用 `buildBattleLiteInheritedSummary(...)`
- 在“🚀 开始生成”区块中、`<BattleActions />` 之前插入 notice，保证用户在点击生成前可见

要求：

- notice 默认始终可见，不折叠
- notice 只展示只读信息，不加入任何会写共享 store 的控件
- 保留现有 `BattleActions showAdvancedUtilities={false}` 与错误提示布局

- [ ] **Step 4: 运行组件测试与 battle 页面相关测试，确认通过**

Run: `bun test tests/battle-lite-inherited-context-notice.test.tsx tests/battle-lite-page.test.tsx tests/arena-shared-links.test.tsx`
Expected: PASS

- [ ] **Step 5: 提交这一小步**

```bash
git add components/arena-lite/BattleLiteInheritedContextNotice.tsx components/arena-lite/BattleLitePage.tsx tests/battle-lite-inherited-context-notice.test.tsx
git commit -m "feat: 显示简洁竞技场继承上下文提示"
```

---

### Task 4: 做完整验证，确保共享设置与快捷视图语义落地

**Files:**
- Verify only: [`components/arena/stores/useBattleStore.ts`](/home/notuhao/code/MahoShojo-Generator/components/arena/stores/useBattleStore.ts)
- Verify only: [`components/arena-lite/BattleLitePage.tsx`](/home/notuhao/code/MahoShojo-Generator/components/arena-lite/BattleLitePage.tsx)
- Verify only: `tests/battle-store-lite-defaults.test.ts`
- Verify only: `tests/battle-lite-inherited-summary.test.ts`
- Verify only: `tests/battle-lite-inherited-context-notice.test.tsx`

- [ ] **Step 1: 跑本轮聚焦测试**

Run:

```bash
bun test tests/battle-store-lite-defaults.test.ts tests/battle-lite-inherited-summary.test.ts tests/battle-lite-inherited-context-notice.test.tsx tests/battle-lite-page.test.tsx tests/arena-shared-links.test.tsx
```

Expected: PASS

- [ ] **Step 2: 跑全量单测**

Run: `bun test`
Expected: PASS

- [ ] **Step 3: 跑 lint**

Run: `bun run lint`
Expected: PASS

- [ ] **Step 4: 跑生产构建**

Run: `bun run build`
Expected: PASS

- [ ] **Step 5: 提交最终收尾**

```bash
git add components/arena/stores/useBattleStore.ts components/arena/types/index.ts components/arena-lite/BattleLitePage.tsx components/arena-lite/battle-lite-inherited-summary.ts components/arena-lite/BattleLiteInheritedContextNotice.tsx tests/battle-store-lite-defaults.test.ts tests/battle-lite-inherited-summary.test.ts tests/battle-lite-inherited-context-notice.test.tsx
git commit -m "fix: 修复简洁竞技场共享设置冲突"
```

---

## 完成定义

- `/arena` 中设置过的长度、语言、历战/当前状态/叙事历史读写、战报宽度等参数，在切换到 `/battle` 后保持不变
- `/battle` 不再通过页面进入动作覆写共享 store
- `/battle` 默认仍复用 `/arena` 当前工作区生成结果
- 当存在辅助情景、问卷、判定事件时，用户能在 `/battle` 直接看到只读摘要
- `bun test`、`bun run lint`、`bun run build` 全部通过

## 风险与回退点

- 若移除 `applyBattleLiteDefaults` 后发现还有别处依赖该 action，先通过 `rg -n "applyBattleLiteDefaults"` 清零调用点，再继续提交
- 若摘要组件文案过长挤压移动端布局，优先精简 badge 文案，不要把 notice 改成交互复杂的折叠面板
- 若构建期发现 `BattleLitePage` 对 store 字段读取过多导致类型冗长，可在组件内部先组装局部对象，但不要为此新增第二个 store

## 执行备注

- 计划执行时优先按 Task 顺序推进，不要跳过测试先改 UI
- 如需补强请求体层面的回归验证，优先在现有纯 helper 或请求组装 helper 上补测试，不要直接在 hook 中堆大量 mock
