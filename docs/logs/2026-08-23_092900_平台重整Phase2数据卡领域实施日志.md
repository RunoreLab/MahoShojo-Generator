# 平台重整 Phase 2 数据卡领域实施日志

日期：2026-08-23

- source commit：`7bd11b50`（`docs(design): 平台重整 Phase 2 分拆与补充`）
- 实施提交：`feat(domain): 提取数据卡领域分类`
- 实施范围：完成 Phase 2 的一个独立通用领域切片——建立最小 `@mahoshojo/domain`，提取现有数据卡模板标识、角色分类与旧数据模板推断，并让 Web/API/Arena 真实生产路径消费；未声称 Phase 2 整体完成。

## 本批完成

- 新增 `packages/domain`，采用现有 workspace source-export/bundler 约定，提供显式根入口与 `./data-cards` 子入口，以及独立 test/lint/build。
- 将 `GENERAL_CHARACTER_TEMPLATE_ID`、`GENERAL_SCENARIO_TEMPLATE_ID`、`inferCharacterKind`、`inferTemplateId` 的 canonical 实现从 legacy root 提取到纯 TypeScript package。
- `lib/schemas` 继续导出原有常量、函数和类型，保留既有调用方兼容；通用角色/情景 Zod schema 改为复用 package 中的模板常量，没有更改 schema 或解析顺序。
- Web Arena schema、Arena 逻辑/服务、数据卡 repository adapter 与 `generate-game-card` API 直接消费 `@mahoshojo/domain/data-cards`，避免 package 只被自身测试使用。
- root manifest 使用 `workspace:*` 显式声明生产依赖；`packages/README.md` 同步记录真实 package 职责。

## 规格状态

| 规格/决策 | 状态 | 证据/说明 |
| --- | --- | --- |
| Phase 2 最小 `domain` | 部分完成 | 已提取数据卡角色分类与兼容模板推断；完整数据卡纯校验、provenance 状态和其他领域规则仍待后续批次。 |
| MONO-004 | 完成本批范围 | `@mahoshojo/domain` 只通过 `.` 与 `./data-cards` 显式 exports 暴露公共 API。 |
| MONO-005 | 完成本批范围 | workspace boundary gate 确认 domain 不导入 app、框架、DOM、Node runtime、数据库或秘密模块。 |
| MONO-006 | 完成本批范围 | 提取保持既有分类优先级、模板字符串与 legacy import 兼容，不改变 API、schema、数据库或部署行为。 |
| Phase 2 真实 consumer | 部分完成 | legacy root Web/API/Arena 已有多个 runtime consumer；其他通用 package 与最终 `apps/web` 迁移仍未完成。 |

## TDD 与验证证据

- RED：首次执行 `pnpm --filter @mahoshojo/domain test` 时，测试因缺少 `@mahoshojo/domain/data-cards` 公共入口失败；此前一次执行只遇到 workspace 链接尚未刷新的 pnpm store 错误，不计作 RED。
- GREEN：实现最小领域模块后，domain 1 个 test file、4 个 tests 通过；覆盖显式模板优先级、旧数据字段推断、模板补全和根/子路径公共入口。
- Targeted 回归：`tests/data-card-converter.test.ts` 与 `tests/generate-game-card-handler.test.tsx` 共 2 个 test files、25 个 tests 通过。
- `pnpm install --frozen-lockfile`：5 个 workspace projects，lockfile 已是最新。
- `pnpm run workspace:verify`：boundary、4 个真实 package 的 test/lint/build 全部通过；`apps/*` 仍无真实 workspace project，pnpm 保留预期的 no-projects-matched 提示。
- `pnpm test`：316 个 test files、1668 个 tests 全部通过。
- `pnpm lint`：通过，无 ESLint warning/error。
- `pnpm build`：Next.js 15.3.8 生产构建通过，生成 187 个静态页面。
- `pnpm build:server`：生成 24 条 allowlisted routes，Hono server bundle 构建通过。
- `pnpm check:wrangler:d1`：3 个 D1 ID 配置项通过。
- `git diff --check`：退出码 0；Git 同时报告仓库大量既有 CRLF -> LF working-copy 提示，本批没有空白错误。

## 警告与未通过的额外诊断

- `pnpm check:naming` 为 report-only 并退出成功，仍报告全仓既有 463 条阻断级 snake_case 点访问与 546 条附加审计；本批新增 package 无命中。
- Hono 构建仍报告 `dist/server/index.mjs` 为 6.5 MB；本批只增加很小的纯领域函数，没有扩大 Hono 路由面，bundle 体积继续作为既有观察项。
- 额外执行的裸 `pnpm exec tsc --noEmit` 失败。根 `tsconfig.json` 会扫描 workspace/root 测试，但没有加载各 package 的 Vitest globals，并同时暴露多处既有测试类型错误；仓库 manifest 未把该命令定义为验证 gate。本批以各 package 独立 `tsc` 和 Next build 内置类型检查为权威结果，不把该失败描述为通过。

## 未完成与后续入口

- `packages/contracts` 的通用 card/provider request-result/API version/error contract 仍待提取。
- `packages/domain` 尚未承载完整数据卡 schema/纯校验与 provenance 状态语义。
- `packages/ai-core`、`ai-direct`、`local-library`、`cloud-client` 与 `AiExecutionPort` / `CardRepository` / `SecureVault` / `FilePort` / `UpdatePort` 仍未建立。
- Phase 2 退出门禁仍未满足；不得因本批建立 domain 就提前进入 Arena Room runtime 或宣称 MONO-002 完成。

## 兼容、发布与回滚

- 无 API/wire schema、数据库、Redis、D1、环境变量、secret、部署或生产开关变化。
- 未执行 Cloudflare/Hono production deploy、远端 D1、Docker 或远端 runtime smoke test；本批不需要人工生产操作。
- 如需回滚，回退本实施提交即可恢复 legacy root 的 canonical 分类实现；不涉及数据或部署回滚。
