# 平台重整 Phase 1 实施日志

- source commit：`e2cdb423418c02f189b730eb58235811cbbb8eeb`
- 实施提交：`chore(repo): 建立 pnpm workspace 骨架`
- 实施范围：只建立 pnpm workspace 编排、未来应用/共享 package 占位、exports PoC、依赖边界检查与 CI 验证入口；未搬迁根应用或 Arena 多人实现。

## 规格状态

| 规格 | 状态 | 证据/说明 |
| --- | --- | --- |
| MONO-001 | 完成 | 根 `pnpm-workspace.yaml` 声明 `apps/*`、`packages/*`，并保留既有 `allowBuilds` 与 `peerDependencyRules`。 |
| MONO-002 | 未完成（增加脚本 gate） | `apps/*` 目前是长期边界说明；检查器现在要求已有 manifest 的 workspace project 声明 `test`、`lint`、`build`，但没有声称每个应用已具备独立环境与发布入口。 |
| MONO-003 | 部分完成 | `scripts/check-workspace-boundaries.mjs` 检查未来 app-to-app 导入；当前仓库无跨 app 源码依赖。 |
| MONO-004 | 部分完成 | `@mahoshojo/config` 提供显式 `exports`；检查器拒绝未声明的 workspace package 深层导入。完整 package 提取留待后续阶段。 |
| MONO-005 | 部分完成 | `@typescript-eslint/parser` 的 scope manager unresolved-reference 检查覆盖 package→app、app→app、domain framework/runtime/DOM（含 `cloudflare:` scheme，且只报告未解析 DOM global）、client secret/signature/env/private、未声明深层导入，并有正/负 fixture。 |
| MONO-006 | 完成（本阶段范围） | 只增加结构与验证入口，未改写业务逻辑、schema 或部署运行时。 |
| MONO-007 | 完成 | 未引入 Turborepo/Nx。 |
| MONO-008 | 部分完成/延期 | 仅新增 `apps/arena-room` 路径说明；没有部署、迁移、第二套 `/arena` 产品，也没有改变 Service Binding/GenerationBridge 契约语义。 |

## 验证结果

- `pnpm install`：通过；lockfile importer 变化包括 root importer 新增边界 CLI 所需的 `@typescript-eslint/parser`，以及 `packages/config` importer 的 PoC 工具依赖（其中 `esbuild:^0.27.2` 用于固定 Vitest/Vite transform peer variant）；无业务依赖或非预期 peer 变体漂移。
- `pnpm run workspace:verify`：通过；边界 CLI、workspace test/lint/build 均成功。当前 `apps/arena-room` 是 README-only 占位，pnpm 对 `apps/*` 的 `--if-present` 产生 no-project informational 提示，不影响退出码。
- `pnpm test`：通过，316 个 test files、1664 个 tests 全部通过。
- `pnpm lint`：通过，无 ESLint warning/error。
- `pnpm build`：通过，Next 生产构建成功并生成 187 个静态页面；root legacy build 仍使用原命令。
- `pnpm build:server`：通过，Hono 单文件 bundle 成功生成。
- `node scripts/check-workspace-boundaries.mjs`：通过，当前 apps/packages 源文件无边界违规。
- 边界 fixture：通过（14 tests，含 DOM scope shadow、manifest script gate 与 TypeScript import type/import-equals）；workspace 结构测试通过（4 tests，含精确生成物 ignore 与 source-export PoC 断言）。
- `git diff --check`：通过。

## 兼容与发布记录

- root legacy 临时兼容：根 `package.json`、根源码、旧 build/test/lint/deploy/runtime 命令保持原位；新增脚本只用 `./apps/*` 与 `./packages/*` filter，不递归 root legacy app。
- 生成物隔离：为 workspace 子目录增加精确的 `node_modules/`、`dist/`、`coverage/`、`build/`、`out/`、`.open-next/` ignore；`@mahoshojo/config` 采用 source-export，build 为独立的 `tsc --noEmit`，不提交 dist 生成物。
- 生产开关：无新增开关；Cloudflare 与 Hono workflow 仅在旧 build/test/deploy 路径之前增加 `pnpm run workspace:verify`，没有改变触发分支、environment、SSH 或 Wrangler 命令。
- 数据迁移：无 schema、D1、Redis 或业务数据迁移。
- 新增秘密及 owner：无。
- Arena 多人契约兼容：未修改现有 Arena 多人实现、DTO、事件、GenerationBridge 或 `/arena` 路径；现有 root 测试回归通过。
- 回滚点：回退本实施提交即可恢复 Phase 1 之前的 workspace/CI/docs 状态；不会涉及数据回滚。

## 未完成项与下一阶段

MONO-002 的独立应用 manifests、运行命令、环境说明、部署/health 入口，以及真实 `contracts/domain/ports` 提取均未在本批实现。下一阶段入口为 `packages/contracts`、`packages/domain` 与能力 ports 的提取，并继续使用边界检查和现有 Arena 兼容夹具验证。
