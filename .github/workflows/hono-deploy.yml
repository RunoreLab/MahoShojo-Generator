name: Deploy Hono

on:
  push:
    branches:
      - '**'
  workflow_dispatch:

concurrency:
  group: hono-production
  cancel-in-progress: false

permissions:
  contents: read

env:
  VPS_HOST: ${{ vars.VPS_HOST }}
  VPS_USER: ${{ vars.VPS_USER }}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify workspace and legacy root
        run: pnpm run ci:verify

      - name: Verify Hono authentication and runtime
        run: |
          pnpm --filter @mahoshojo/api run test
          pnpm exec vitest run tests/apps-api-source-ownership.test.ts tests/hosted-runtime-ownership.test.ts tests/hono-deploy-workflow.test.ts tests/hono-deploy-release-contract.test.ts tests/hono-deploy-script.test.ts

      - name: Verify Hono container build
        run: docker build --file apps/api/Dockerfile .

      - name: Build single-file server
        run: |
          pnpm --filter @mahoshojo/api run deploy:prepare
          cp apps/api/deploy/compose.yml apps/api/dist/compose.yml
          cp apps/api/deploy/deploy-bundle.sh apps/api/dist/deploy-bundle.sh
          cd apps/api/dist
          sha256sum index.mjs compose.yml deploy-bundle.sh > release.manifest
          sha256sum release.manifest > release.sha256

      - name: Upload bundle
        uses: actions/upload-artifact@v4
        with:
          name: hono-server-${{ github.sha }}
          path: |
            apps/api/dist/index.mjs
            apps/api/dist/compose.yml
            apps/api/dist/deploy-bundle.sh
            apps/api/dist/release.manifest
            apps/api/dist/release.sha256
          if-no-files-found: error
          retention-days: 7

  deploy:
    needs: build
    if: github.ref == 'refs/heads/feature/v0.2.0_Battle_Growth_MahoShojo'
    runs-on: ubuntu-latest
    environment: hono-production
    steps:
      - name: Download bundle
        uses: actions/download-artifact@v5
        with:
          name: hono-server-${{ github.sha }}
          path: artifact

      - name: Configure SSH
        env:
          VPS_SSH_PRIVATE_KEY: ${{ secrets.VPS_SSH_PRIVATE_KEY }}
        run: |
          install -m 700 -d ~/.ssh
          printf '%s\n' "$VPS_SSH_PRIVATE_KEY" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          printf '%s %s\n' "$VPS_HOST" 'ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIARFAFVUCsUdVM8PtbbiOhkUD9zn5Q6MeK47hxEr/XuC' > ~/.ssh/known_hosts

      - name: Upload release
        run: |
          release_id="$(awk '{print $1}' artifact/release.sha256)"
          ssh -i ~/.ssh/deploy_key "$VPS_USER@$VPS_HOST" "install -d -m 755 /opt/mahoshojo-hono/releases/$release_id"
          scp -i ~/.ssh/deploy_key artifact/index.mjs artifact/compose.yml artifact/deploy-bundle.sh artifact/release.manifest artifact/release.sha256 "$VPS_USER@$VPS_HOST:/opt/mahoshojo-hono/releases/$release_id/"
          ssh -i ~/.ssh/deploy_key "$VPS_USER@$VPS_HOST" "cd /opt/mahoshojo-hono/releases/$release_id && printf '%s  release.manifest\\n' '$release_id' | sha256sum -c - && sha256sum -c release.manifest && chmod 755 deploy-bundle.sh"
          ssh -i ~/.ssh/deploy_key "$VPS_USER@$VPS_HOST" "/opt/mahoshojo-hono/releases/$release_id/deploy-bundle.sh $release_id https://homura.colanns.me"
