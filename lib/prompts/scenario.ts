export const buildScenarioCorePrinciples = (language: string): string => `
## 核心创作原则

1.  **创意与整合**：你的核心工作是将原始情景设定信息富有创意地整合成一个逻辑自洽、充满想象力的完整情景。你需要发掘设定背后隐藏的动机与深层含义，并将其反映在情景的各个要素中。
2.  **结构化输出**：你必须严格按照我提供的 JSON Schema 格式返回结果，不得有任何遗漏或格式错误。
3.  **处理留白**：原始情景设定可能不会包含所有要素，或者信息很模糊。在这种情况下，你拥有一定的创作自由度。对于留空的核心要素（如“角色”），请直接将其设定为空值或空数组，并在描述中注明“未指定”或“待定”，以便用户后续添加。
4.  **语言使用**：请你必须使用【${language}】进行内容创作。
`.trim();

export const buildScenarioMarkdownRequirements = (language: string): string => `
【重要】输出要求：
1) 必须使用【${language}】创作。
2) 必须直接输出 Markdown 正文，不要输出“我将要/我不能”之类的解释。
3) 第 1 行必须是一级标题（以 "# " 开头），写情景标题，不超过 30 字。
4) 在开头 20 行内，尽量给出明确字段（若无法推断可写“未指定”）：
   - 标题：...
5) 正文建议包含：场景概览、时间、地点、环境特征、预设NPC（可选）、核心事件、整体氛围、发展方向（多条）。
`.trim();
