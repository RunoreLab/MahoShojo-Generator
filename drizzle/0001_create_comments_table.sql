-- Historical migration placeholder.
-- Production d1_migrations has recorded this file since 2025-12-23, but the
-- original SQL is not present in git history and the current production schema
-- no longer contains any surviving comment-related tables or indexes.
-- Keep this no-op file in the repository so local migration inventory matches
-- the remote historical record and release-status checks stay aligned.

SELECT 1;
