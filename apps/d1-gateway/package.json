{
  "name": "@mahoshojo/d1-gateway",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "test": "vitest run --config vitest.config.ts",
    "lint": "eslint index.ts tests --config eslint.config.mjs",
    "build": "tsc --noEmit -p tsconfig.json && wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@typescript-eslint/parser": "^8.50.0",
    "esbuild": "^0.28.1",
    "eslint": "^9",
    "typescript": "^5",
    "vitest": "^4.1.7",
    "wrangler": "^4.125.0"
  }
}
