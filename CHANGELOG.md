# 更新日志

本文件记录对用户和贡献者值得关注的重要变化。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)；发布版本按时间倒序排列，空分类不保留。

版本约定：本项目参考 [语义化版本](https://semver.org/lang/zh-CN/)。当前处于 0.x 阶段：minor 版本号用于重要功能或显著产品变化，patch 版本号用于兼容性修复和较小改进；1.0.0 留给产品与主要行为进入相对稳定阶段之后。

## [Unreleased]

## [0.9.0] - 2026-09-03

> 竞技场多人协作 × 平台可靠性升级

### 新增

- **【重点】竞技场多人模式**：现在可以在 `/arena` 打开多人房间，邀请朋友进入同一房间，一起配置这一局并实时围观同一份战报。
  - 房主维护房间权威配置并发起生成；成员可在同步房间配置后编辑自己的工作副本，并把修改以**提案**形式发给房主逐项审阅，接受后才会进入本局配置。
  - 一个有趣的玩法是“多人跑团”：每位成员提案自己的角色，并在角色的「行动」里写下本轮行动引导，由房主汇总大家提案后统一生成战报。
  - 只想围观战报直播也完全可以：加入房间后不提交任何提案，等房主开始生成即可。
  - 房间支持公开目录发现或仅凭房间码加入，最多 16 人；加入房间后可查看本房间近期历史战报。
  - 多人房间需登录使用；成员提案经服务器安全投影与校验，本地文件和密钥不会进入房间。
- **多人玩法指南**：百科新增「竞技场多人模式」条目（`/encyclopedia/arena-multiplayer`），多人房间入口旁提供「玩法说明」直达链接，房间内空状态也会给出下一步引导。
- **长生成断线恢复**：竞技场生成过程中如果网络短暂中断，客户端会尝试重新连接并恢复原来的生成任务与已生成内容，不必一断线就从头再来；战报缺口会自动从服务器权威状态恢复。
- **主服务容灾备用路径**：发起支持容灾的生成请求时，客户端会先检查主服务（Hono API）；主服务暂时不可用时，符合条件的请求可以自动改用备用服务路径。已发出但结果不确定的写入/生成操作不会被盲目重复提交，避免重复生成。
- **房间历史战报**：房间成员可查看本房间近期生成的权威战报，加入房间后也会自动展示最近一场。
- **战后角色修复**：战后角色更新异常时，可在战报结果区查看并修复角色变化：支持 AI 重新生成修复草稿或手动编辑，应用后进入可编辑版本；修复结果不会伪造原生签名，源角色也不会被自动覆盖。

### 调整

- **长战报不再被客户端读流超时直接中止**：竞技场流式战报的客户端超时改为“提示并继续等待”，较长的战报不会仅因为达到浏览器等待时间就被切断；服务端仍保留必要的资源与安全保护。
- **竞技场默认使用流式生成**：新会话默认以流式模式实时输出战报，可随时切换回非流式。
- **平台架构重整**：生成主链路迁移至独立 Hono API 主服务 + Redis 房间运行时，Web（Cloudflare Pages/Workers）承担页面与容灾备用路径；共享领域、契约与运行时代码拆分到 `packages/*`（领域、契约、Hosted 服务等），应用按 `apps/*` 边界独立部署。
- **房间人数上限放宽至 16 人**。
- **更新日志制度**：本文件起采用固定分类（新增/调整/弃用/移除/修复/安全），开发中的变化先积累在 `[Unreleased]`，发布时再整体搬入版本章节。

### 修复

- 修复战报输入/输出 token 统计在跨服务边界处丢失的问题。
- 修复竞技场多处按钮与弹窗的视觉回归（预设下载、角色/分队/情景/素材列表、数据卡选择弹窗、分队折叠区等），触控热区不受影响。
- 修复竞技场设置在页面刷新后回退的问题。

---

## [v0.8.2] - 万途生态互通 × 素材注入

### 重大更新
- **万途生态互通**：新增万途 Card adapter，支持识别 `character/location/event/setting/item/story/faction` 等万途通用卡协议，为后续多世界资料卡流转打基础。
- **竞技场素材注入**：`/arena` 与简洁竞技场支持把参考素材加入普通战报和连续战报上下文；素材可来自万途 Card、本仓库数据卡或本地 JSON。
- **万途生态导航**：顶部导航新增“生态”分组，提供万途驿站、万途竞技场、废土车卡、废土旅途等外部入口。

### 新增
- **档案馆万途导入/导出**：`/character-manager` 可把本站角色卡导出为万途 `character` JSON；万途角色卡可导入为本站通用角色卡。
- **万途往返保留**：导出支持互通与往返信息保留，导回时可在明确选择恢复时还原原始本站模板。
- **素材面板**：竞技场新增素材列表，支持上传、粘贴、浏览在线数据卡、排序、移除与清空，最多 10 个素材。
- **连续战报素材快照**：新建连续战报会话时会保存当前素材列表，续写、分支与重写时继续作为上下文参考。
- **严格排位保护**：使用素材注入的战报默认不计严格排位，预检与服务端结算均会拦截。

### 调整
- **Prompt 边界收口**：素材统一进入“参考素材”块，明确只作设定参考，不执行素材中对 AI 的指令。
- **在线数据卡选择器扩展**：素材选择可浏览角色、情景、历史、问卷等数据卡类型，不再局限于参战角色或情景。
- **原生性口径补充**：万途导入、手动编辑、素材注入属于跨生态或自定义创作流程，不会伪造本站原生签名。

## [v0.8.1] - 连续战报会话 × 章节规划

### 重大更新
- **连续战报会话（/arena）**：竞技场新增本地会话面板，可基于当前配置直接生成首章，并在同一故事链上继续续写、创建分支、重写最后一章。

### 新增
- **章节摘要与导出**：连续战报会为每章生成摘要，并在达到阈值后后台刷新会话摘要；支持导出整条会话为 Markdown。
- **本地持久化恢复**：会话与章节存放在浏览器 IndexedDB，刷新页面后可继续查看、切换和操作本地故事链。
- **连续战报专用接口与软限流**：新增 `/api/arena/session/generate-next` 与 `/api/arena/session/refresh-summary`，对同会话并发、冷却与摘要刷新做软保护。
- **连续战报章节规划**：新建会话支持自动 / 不限制 / 自定义总章节数（1-20 章），并可在面板内快速切换常用章节数。
- **情景卡章节规划扩展**：结构化情景与通用情景支持 `_battle_story` 配置，提供 `suggested` / `fixed` 两种模式；固定章节数会在创建连续战报时强制继承。

### 调整
- **竞技场页面布局**：战报结果下方新增连续战报面板，章节预览复用现有战报卡片与图片保存能力。
- **续写上下文编译策略**：续写/分支/重写时会综合会话摘要、最近章节、working combatants 与当前引导词，降低长篇续写跑偏概率。
- **章节定位与终章约束入 Prompt**：AI 现可接收“当前第几章 / 共几章 / 本章定位 / 剩余章节数”，非终章避免提前大结局，终章强调主线收束。
- **章节上限后的操作边界**：达到计划章节上限后将禁止继续续写与追加分支新章，但仍允许重写最后一章。
- **情景卡编辑器补齐**：`/character-manager` 可直接编辑情景卡的连续战报章节规划，且 `_battle_story` 不参与原生签名。
- **长线玩法说明补齐**：README、公告与百科同步补充连续战报的定位、限制与上手方式。

## [v0.8.0] - Auth + ORM 升级与账号迁移

### 重大更新
- **认证体系升级（Better Auth）**：服务端鉴权以会话优先，兼容旧版密钥登录过渡，完成用户系统主链路切换。
- **数据库访问层升级（Drizzle ORM）**：核心业务逐步迁移到 Drizzle Schema + Repository 分层，降低 SQL 拼接风险并统一数据访问口径。
- **D1 迁移体系落地**：新增版本化迁移脚本与发布状态检查脚本，支持更可控的生产发布流程。

### 新增
- **账号安全设置（/me）**：新增设置初始密码（迁移）、修改密码、修改邮箱、邮件令牌重置密码能力。
- **迁移状态与引导**：新增 `migration-status` 状态接口，并在角色管理页与个人页设置页提供迁移提醒。
- **迁移软阻断机制**：legacy 登录用户多次“稍后处理”后触发软阻断，要求先完成迁移设置。
- **认证审计日志**：新增 `auth_audit_logs`，覆盖注册、登录、设置初始密码、改密、改邮箱等关键操作。

### 调整
- **新注册流程收口**：新用户注册强制使用密码，不再提供旧版密钥注册分支。
- **登录错误语义统一**：密码登录错误统一返回“账号或密码错误”，避免暴露标识类型。
- **账号映射补齐能力**：补充 `user_auth_links` 回填与迁移链路，降低老账号迁移成本。

### 迁移指引
- 旧密钥用户请前往 `/me?tab=settings`，先完成“设置登录密码（迁移）”。
- 建议同步完成邮箱验证与密码更新，确保后续旧登录入口下线后仍可正常登录。
- 详细说明请见百科：`/encyclopedia/auth-migration`。

---

## [v0.7.2] - 问卷工具箱

### 新增
- **问卷编辑器（/questionnaire-editor）**：可视化创建/调整自定义问卷，支持导出 JSON 与保存为云端问卷数据卡
- **条件显示 / 跳题**：问卷题目支持条件展示与跳题规则，可在编辑器内配置
- **云端问卷库**：问卷可保存为数据卡（公开/私有），在 `/details` 与 `/canshou` 直接选用
- **多问卷串联**：允许按顺序加载多份问卷组成一套完整出题流程

### 调整
- **字数上限统一为 500 字**：超限不拦截生成，但会取消原生签名
- **问卷 Logo 外链安全校验**：仅允许站内路径或可信 HTTPS 外链

---

## [v0.7.1] - 魔法茶会×长期剧情会话

### 新增
- **魔法茶会（/magic-tea-party）**：基于角色卡/情景卡的长期对话页（本地会话，支持主情景+追加情景）
- **双输出模式**：JSONL（可解析旁白/对白/选项）与 Markdown（更自由但不保证稳定解析）
- **视觉小说选项**：一键生成 2~16 条下一步行动选项
- **自动摘要**：长对话支持自动摘要与 Token 预算提示（可在全局设置关闭）
- **角色更新（历战/状态）**：基于对话生成更新草案，并可写入 `arena_history` / `current_state`（写入会移除 `signature` 并标记为非原生）
- **协议适配提示**：支持 `notice` / `mtp_notice` 可解析提示/报错，用于强协议卡在茶会环境中的自救
- **百科补充**：新增魔法茶会分类与完整条目（功能介绍/设置说明/进阶协议/排查 FAQ）

---

## [v0.7.0] - 创作者工具箱

### 新增
- **通用情景卡（Markdown）**：`/scenario` 支持创建/转换通用情景卡，用于长线舞台与规则笔记
- **角色生成切换流式/非流式**：`/details`、`/canshou` 可按需选择结构化输出或实时 Markdown
- **自由生成（/free）**：任意提示词 + 选择 Schema 生成数据卡（可选参考附件）
- **酒馆生态联动（/tavern）**：SillyTavern 角色卡 PNG 导入/导出
- **角色组队卡（/character-party）**：把多张角色卡拼成一张“队伍卡”
- **百科入口（/encyclopedia）**：新手指引、规则说明与工具链教程

---

## [v0.6.0] - 排位×技术值×标签×百科×排行榜

### 新增
- **排位分 / 技术值体系**：对满足资格的 1v1 对局计算排位分并展示榜单
- **排行榜页面**：提供"最高值/近期"等筛选与标签维度浏览
- **竞技场/PVP 对局排位计入**：满足资格时计入排位（v0.6.0 默认仅计 1v1，宁可漏算不可错算）
- **定位标签**：引入标签库（系统维护口径）与数据卡标签选择
- **标签库**：支持筛选与展示
- **Markdown 百科**：可维护的百科系统，含新手指引与规则说明

### 优化
- **大对象外部化存储**：战报等大对象支持外部化存储（可选 R2），减少 D1 体积压力

---

## [v0.5.1] - PVP卡牌对决×个人页/个人资料卡

### 新增
- **PVP 房间卡牌对决**：支持大厅/房间/回合/投票/结算等功能
- **个人页**：展示个人战报与基础信息
- **个人资料卡生成**：方便展示个人战报与名片信息

> 注：该版本当时未同步更新 README/公告，现补记。

---

## [v0.5.0] - 流式生成×屏蔽词机制

### 新增
- **竞技场流式战报升级**：升级为 Markdown 文本流，实时输出战报生成过程
- **屏蔽词柔性和谐机制**：默认遮罩 `❀`，可定向替换，避免误触合规风险
- **输入备份功能**：强化敏感词触发后的"输入备份"，在逮捕页可直接预览与下载，避免丢稿

---

## [v0.4.3] - 通用角色扩展

### 新增
- **通用角色模板**：档案馆支持一键切换内容模板并自动生成 Markdown 描述
- **独立字段保持**：历战记录与签名在转换中保持独立字段
- **模板兼容性**：魔法少女/残兽/情景模板按需补齐嵌套字段，让空白卡片开箱即用

### 优化
- **竞技场兼容通用角色**：含流式模式，提供兜底提示词确保跨模板对战精彩

---

## [v0.4.0] - 云端档案×实时战报 🎉

### 重大更新
- **用户系统**：支持注册和登录（基于用户名+唯一密钥）
- **云端数据卡存储与管理**：将角色保存到云端，随时随地访问
- **徽章系统**：展示和管理通过特定活动或成就获得的徽章
- **兑换码功能**：使用兑换码增加数据卡存储槽位上限
- **实时流式战报生成模式**：更流畅的故事生成体验
- **公开分享**：浏览和使用其他用户分享的角色/情景

---

## [v0.3.0] - 命途未定

### 新增
- **随机性与可能性玩法**：引入全新的随机性玩法
- **随机组合生成**：快速生成角色，无需填写问卷
- **竞技场随机元素**：
  - 随机角色加入
  - 随机判定器

---

## [v0.2.0] - 战斗×成长

### 新增
- **角色成长循环**：角色可通过参与竞技场积累"历战记录"
- **成长升华功能**：角色可通过成长升华进化
- **自定义情景生成**：通过问卷快速生成用于竞技场"情景模式"的自定义故事场景
- **角色档案管理**：管理你的角色数据
- **竞技场模式扩展**：
  - 情景模式
  - 分队对抗

### 优化
- **问卷易用性改进**：优化问卷交互体验
